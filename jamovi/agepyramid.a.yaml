---
name:  agepyramid
title: Age Pyramid
menuGroup: Exploration
menuSubgroup: ClinicoPath Descriptive Plots
version: '1.1.0'
jas: '1.2'

options:
    - name: data
      type: Data
      description:
          R: >
            The data as a data frame.
          jamovi: >
            The data as a data frame.
    - name: age
      title: Age
      type: Variable
      suggested: [ continuous ]
      permitted: [ numeric ]
      description:
          R: >
            A string naming the variable from `data` that contains the continuous age values.
          jamovi: >
            A string naming the variable from `data` that contains the continuous age values.
    - name: gender
      title: Gender
      type: Variable
      suggested: [ ordinal, nominal ]
      permitted: [ factor ]
      description:
          R: >
            A string naming the variable from `data` that contains the categorical gender values.
          jamovi: >
            A string naming the variable from `data` that contains the categorical gender values.
    - name: female
      title: Select Female
      type: Level
      variable: (gender)
      description:
          R: >
            A string naming the level from `gender` that should be considered as 'Female'.
          jamovi: >
            A string naming the level from `gender` that should be considered as 'Female'.
    - name: bin_width
      title: Bin Width (Years)
      type: number
      default: 5
      description:
          R: >
            The width of the age bins in years. Adjust this to change the granularity of the age groups.
          jamovi: >
            The width of the age bins in years.
    - name: plot_title
      title: Plot Title
      type: string
      default: "Age Pyramid"
      description:
          R: >
            The title displayed on the age pyramid plot.
          jamovi: >
            The title displayed on the age pyramid plot.
...