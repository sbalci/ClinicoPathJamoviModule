---
name:  benford
title: Benford Analysis
menuGroup: ExplorationT
menuSubgroup: ClinicoPath Data Quality
version: '0.0.31'
jas: '1.2'

options:
    - name: data
      type: Data
      description:
          R: >
            The data as a data frame.
          jamovi: >
            The data as a data frame.

    - name: var
      title: Variable
      type: Variable
      suggested: [ continuous ]
      permitted: [ numeric ]
      description:
          R: >
            a string naming the variable from `data` that contains the
            continuous values used for the report
          jamovi: >
            a string naming the variable from `data` that contains the
            continuous values used for the report

    - name: digits
      title: Number of Digits
      type: Integer
      default: 2
      min: 1
      max: 4
      description:
          R: >
            Number of first digits to analyze (default: 2)
          jamovi: >
            Number of first digits to analyze

...
