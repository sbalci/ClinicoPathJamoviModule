exportPattern("^[[:alpha:]]+")
import(ggplot2)
import(arsenal)
import(survival)
import(xfun)
import(knitr)
import(dplyr)

export(tableoneClass)
export(tableoneOptions)

export(crosstableClass)
export(crosstableOptions)

export(writesummaryClass)
export(writesummaryOptions)

export(survivalClass)
export(survivalOptions)

export(survClass)
export(survOptions)

export(finalfitClass)
export(finalfitOptions)
