
# This file is automatically generated, you probably don't want to edit this

tsSUROptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "tsSUROptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            y1 = NULL,
            surmethod = "choose",
            numSUR = 1,
            Save = FALSE,
            savePath = "", ...) {

            super$initialize(
                package='ClinicoPath',
                name='tsSUR',
                requiresData=TRUE,
                ...)

            private$..y1 <- jmvcore::OptionVariable$new(
                "y1",
                y1)
            private$..surmethod <- jmvcore::OptionList$new(
                "surmethod",
                surmethod,
                options=list(
                    "choose",
                    "randshuffle",
                    "blockshuffle",
                    "randphase",
                    "randphase"),
                default="choose")
            private$..numSUR <- jmvcore::OptionInteger$new(
                "numSUR",
                numSUR,
                default=1)
            private$..Save <- jmvcore::OptionBool$new(
                "Save",
                Save,
                default=FALSE)
            private$..savePath <- jmvcore::OptionString$new(
                "savePath",
                savePath,
                default="")

            self$.addOption(private$..y1)
            self$.addOption(private$..surmethod)
            self$.addOption(private$..numSUR)
            self$.addOption(private$..Save)
            self$.addOption(private$..savePath)
        }),
    active = list(
        y1 = function() private$..y1$value,
        surmethod = function() private$..surmethod$value,
        numSUR = function() private$..numSUR$value,
        Save = function() private$..Save$value,
        savePath = function() private$..savePath$value),
    private = list(
        ..y1 = NA,
        ..surmethod = NA,
        ..numSUR = NA,
        ..Save = NA,
        ..savePath = NA)
)

tsSURResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        tsplot = function() private$.items[["tsplot"]],
        surplot = function() private$.items[["surplot"]],
        savemessage = function() private$.items[["savemessage"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Generate random surrogates")
            self$add(jmvcore::Image$new(
                options=options,
                name="tsplot",
                title="Time Series Plot",
                width=600,
                height=200,
                renderFun=".tsplot"))
            self$add(jmvcore::Image$new(
                options=options,
                name="surplot",
                title="Surrogate Series",
                width=600,
                height=600,
                renderFun=".surplot"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="savemessage",
                title="Notes"))}))

tsSURBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "tsSURBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'tsSUR',
                version = c(1,0,0),
                options = options,
                results = tsSURResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Generate random surrogates
#'
#' 
#' @param data .
#' @param y1 .
#' @param surmethod .
#' @param numSUR .
#' @param Save .
#' @param savePath .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$tsplot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$surplot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$savemessage} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
tsSUR <- function(
    data,
    y1,
    surmethod = "choose",
    numSUR = 1,
    Save = FALSE,
    savePath = "") {

    if ( ! requireNamespace('jmvcore'))
        stop('tsSUR requires jmvcore to be installed (restart may be required)')

    if ( ! missing(y1)) y1 <- jmvcore::resolveQuo(jmvcore::enquo(y1))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(y1), y1, NULL))


    options <- tsSUROptions$new(
        y1 = y1,
        surmethod = surmethod,
        numSUR = numSUR,
        Save = Save,
        savePath = savePath)

    analysis <- tsSURClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
