
# This file is automatically generated, you probably don't want to edit this

jjwithinstats2Options <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jjwithinstats2Options",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            pairs = NULL,
            excl = TRUE,
            originaltheme = FALSE, ...) {

            super$initialize(
                package='ClinicoPath',
                name='jjwithinstats2',
                requiresData=TRUE,
                ...)

            private$..pairs <- jmvcore::OptionPairs$new(
                "pairs",
                pairs,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..excl <- jmvcore::OptionBool$new(
                "excl",
                excl,
                default=TRUE)
            private$..originaltheme <- jmvcore::OptionBool$new(
                "originaltheme",
                originaltheme,
                default=FALSE)

            self$.addOption(private$..pairs)
            self$.addOption(private$..excl)
            self$.addOption(private$..originaltheme)
        }),
    active = list(
        pairs = function() private$..pairs$value,
        excl = function() private$..excl$value,
        originaltheme = function() private$..originaltheme$value),
    private = list(
        ..pairs = NA,
        ..excl = NA,
        ..originaltheme = NA)
)

jjwithinstats2Results <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Violin Plots to Compare Within Groups",
                refs=list(
                    "ggplot2",
                    "ggstatsplot"),
                clearWith=list(
                    "dep",
                    "group",
                    "grvar",
                    "direction",
                    "originaltheme"))
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="To Do"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Violin Plots",
                width=800,
                height=600,
                renderFun=".plot",
                requiresData=TRUE))}))

jjwithinstats2Base <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jjwithinstats2Base",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'jjwithinstats2',
                version = c(1,0,0),
                options = options,
                results = jjwithinstats2Results$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Violin Plots to Compare Within Groups
#'
#' 
#'
#' @examples
#' \dontrun{
#' # example will be added
#'}
#' @param data The data as a data frame.
#' @param pairs .
#' @param excl .
#' @param originaltheme .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
jjwithinstats2 <- function(
    data,
    pairs,
    excl = TRUE,
    originaltheme = FALSE) {

    if ( ! requireNamespace('jmvcore'))
        stop('jjwithinstats2 requires jmvcore to be installed (restart may be required)')

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- jjwithinstats2Options$new(
        pairs = pairs,
        excl = excl,
        originaltheme = originaltheme)

    analysis <- jjwithinstats2Class$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
