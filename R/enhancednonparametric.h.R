
# This file is automatically generated, you probably don't want to edit this

enhancednonparametricOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "enhancednonparametricOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dependent = NULL,
            grouping = NULL,
            mann_whitney = TRUE,
            kruskal_wallis = TRUE,
            posthoc_dunn = TRUE,
            correction_method = "bonferroni",
            assumptions = TRUE,
            effect_sizes = TRUE,
            descriptives = TRUE,
            show_plots = TRUE,
            plot_type = "both",
            confidence_level = 0.95,
            bootstrap_samples = 1000, ...) {

            super$initialize(
                package="ClinicoPath",
                name="enhancednonparametric",
                requiresData=TRUE,
                ...)

            private$..dependent <- jmvcore::OptionVariable$new(
                "dependent",
                dependent,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..grouping <- jmvcore::OptionVariable$new(
                "grouping",
                grouping,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..mann_whitney <- jmvcore::OptionBool$new(
                "mann_whitney",
                mann_whitney,
                default=TRUE)
            private$..kruskal_wallis <- jmvcore::OptionBool$new(
                "kruskal_wallis",
                kruskal_wallis,
                default=TRUE)
            private$..posthoc_dunn <- jmvcore::OptionBool$new(
                "posthoc_dunn",
                posthoc_dunn,
                default=TRUE)
            private$..correction_method <- jmvcore::OptionList$new(
                "correction_method",
                correction_method,
                options=list(
                    "bonferroni",
                    "holm",
                    "hochberg",
                    "bh",
                    "by",
                    "none"),
                default="bonferroni")
            private$..assumptions <- jmvcore::OptionBool$new(
                "assumptions",
                assumptions,
                default=TRUE)
            private$..effect_sizes <- jmvcore::OptionBool$new(
                "effect_sizes",
                effect_sizes,
                default=TRUE)
            private$..descriptives <- jmvcore::OptionBool$new(
                "descriptives",
                descriptives,
                default=TRUE)
            private$..show_plots <- jmvcore::OptionBool$new(
                "show_plots",
                show_plots,
                default=TRUE)
            private$..plot_type <- jmvcore::OptionList$new(
                "plot_type",
                plot_type,
                options=list(
                    "boxplot",
                    "violin",
                    "both"),
                default="both")
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.5,
                max=0.99,
                default=0.95)
            private$..bootstrap_samples <- jmvcore::OptionInteger$new(
                "bootstrap_samples",
                bootstrap_samples,
                min=100,
                max=10000,
                default=1000)

            self$.addOption(private$..dependent)
            self$.addOption(private$..grouping)
            self$.addOption(private$..mann_whitney)
            self$.addOption(private$..kruskal_wallis)
            self$.addOption(private$..posthoc_dunn)
            self$.addOption(private$..correction_method)
            self$.addOption(private$..assumptions)
            self$.addOption(private$..effect_sizes)
            self$.addOption(private$..descriptives)
            self$.addOption(private$..show_plots)
            self$.addOption(private$..plot_type)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..bootstrap_samples)
        }),
    active = list(
        dependent = function() private$..dependent$value,
        grouping = function() private$..grouping$value,
        mann_whitney = function() private$..mann_whitney$value,
        kruskal_wallis = function() private$..kruskal_wallis$value,
        posthoc_dunn = function() private$..posthoc_dunn$value,
        correction_method = function() private$..correction_method$value,
        assumptions = function() private$..assumptions$value,
        effect_sizes = function() private$..effect_sizes$value,
        descriptives = function() private$..descriptives$value,
        show_plots = function() private$..show_plots$value,
        plot_type = function() private$..plot_type$value,
        confidence_level = function() private$..confidence_level$value,
        bootstrap_samples = function() private$..bootstrap_samples$value),
    private = list(
        ..dependent = NA,
        ..grouping = NA,
        ..mann_whitney = NA,
        ..kruskal_wallis = NA,
        ..posthoc_dunn = NA,
        ..correction_method = NA,
        ..assumptions = NA,
        ..effect_sizes = NA,
        ..descriptives = NA,
        ..show_plots = NA,
        ..plot_type = NA,
        ..confidence_level = NA,
        ..bootstrap_samples = NA)
)

enhancednonparametricResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "enhancednonparametricResults",
    inherit = jmvcore::Group,
    active = list(
        instructions = function() private$.items[["instructions"]],
        descriptives = function() private$.items[["descriptives"]],
        assumptions = function() private$.items[["assumptions"]],
        mannwhitney = function() private$.items[["mannwhitney"]],
        kruskalwallis = function() private$.items[["kruskalwallis"]],
        posthoc = function() private$.items[["posthoc"]],
        boxplot = function() private$.items[["boxplot"]],
        interpretation = function() private$.items[["interpretation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Enhanced Non-Parametric Tests")
            self$add(jmvcore::Html$new(
                options=options,
                name="instructions",
                title="Getting Started",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="descriptives",
                title="Descriptive Statistics",
                columns=list(
                    list(
                        `name`="group", 
                        `title`="Group", 
                        `type`="text"),
                    list(
                        `name`="n", 
                        `title`="N", 
                        `type`="integer"),
                    list(
                        `name`="median", 
                        `title`="Median", 
                        `type`="number"),
                    list(
                        `name`="iqr", 
                        `title`="IQR", 
                        `type`="number"),
                    list(
                        `name`="min_val", 
                        `title`="Min", 
                        `type`="number"),
                    list(
                        `name`="max_val", 
                        `title`="Max", 
                        `type`="number"),
                    list(
                        `name`="mean_rank", 
                        `title`="Mean Rank", 
                        `type`="number")),
                visible="(descriptives)",
                clearWith=list(
                    "dependent",
                    "grouping")))
            self$add(jmvcore::Table$new(
                options=options,
                name="assumptions",
                title="Assumption Checking",
                columns=list(
                    list(
                        `name`="assumption", 
                        `title`="Assumption", 
                        `type`="text"),
                    list(
                        `name`="test", 
                        `title`="Test", 
                        `type`="text"),
                    list(
                        `name`="statistic", 
                        `title`="Statistic", 
                        `type`="text"),
                    list(
                        `name`="p_value", 
                        `title`="p", 
                        `type`="text"),
                    list(
                        `name`="conclusion", 
                        `title`="Conclusion", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text")),
                visible="(assumptions)",
                clearWith=list(
                    "dependent",
                    "grouping")))
            self$add(jmvcore::Table$new(
                options=options,
                name="mannwhitney",
                title="Mann-Whitney U Test Results",
                columns=list(
                    list(
                        `name`="comparison", 
                        `title`="Comparison", 
                        `type`="text"),
                    list(
                        `name`="n1", 
                        `title`="N\u2081", 
                        `type`="text"),
                    list(
                        `name`="n2", 
                        `title`="N\u2082", 
                        `type`="text"),
                    list(
                        `name`="u_statistic", 
                        `title`="U", 
                        `type`="number"),
                    list(
                        `name`="z_statistic", 
                        `title`="Z", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="p", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="r_rb", 
                        `title`="r_rb", 
                        `type`="number"),
                    list(
                        `name`="r_rb_ci", 
                        `title`="95% CI", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Effect Size", 
                        `type`="text")),
                visible="(mann_whitney)",
                clearWith=list(
                    "dependent",
                    "grouping",
                    "effect_sizes",
                    "confidence_level")))
            self$add(jmvcore::Table$new(
                options=options,
                name="kruskalwallis",
                title="Kruskal-Wallis Test Results",
                columns=list(
                    list(
                        `name`="test", 
                        `title`="Test", 
                        `type`="text"),
                    list(
                        `name`="df", 
                        `title`="df", 
                        `type`="integer"),
                    list(
                        `name`="chi_square", 
                        `title`="\u03C7\u00B2", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="p", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="eta_squared", 
                        `title`="\u03B7\u00B2", 
                        `type`="number"),
                    list(
                        `name`="interpretation", 
                        `title`="Effect Size", 
                        `type`="text"),
                    list(
                        `name`="conclusion", 
                        `title`="Conclusion", 
                        `type`="text")),
                visible="(kruskal_wallis)",
                clearWith=list(
                    "dependent",
                    "grouping",
                    "effect_sizes")))
            self$add(jmvcore::Table$new(
                options=options,
                name="posthoc",
                title="Post Hoc Pairwise Comparisons (Dunn's Test)",
                columns=list(
                    list(
                        `name`="comparison", 
                        `title`="Comparison", 
                        `type`="text"),
                    list(
                        `name`="z_statistic", 
                        `title`="Z", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="p", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="p_adjusted", 
                        `title`="p_adj", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="effect_size", 
                        `title`="r_rb", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Effect Size", 
                        `type`="text"),
                    list(
                        `name`="significance", 
                        `title`="Result", 
                        `type`="text")),
                visible="(posthoc_dunn)",
                clearWith=list(
                    "dependent",
                    "grouping",
                    "posthoc_dunn",
                    "correction_method",
                    "effect_sizes")))
            self$add(jmvcore::Image$new(
                options=options,
                name="boxplot",
                title="Distribution Comparison Plot",
                width=600,
                height=500,
                visible="(show_plots)",
                requiresData=TRUE,
                clearWith=list(
                    "dependent",
                    "grouping",
                    "show_plots",
                    "plot_type")))
            self$add(jmvcore::Html$new(
                options=options,
                name="interpretation",
                title="Clinical Interpretation and Guidelines",
                visible=TRUE))}))

enhancednonparametricBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "enhancednonparametricBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "enhancednonparametric",
                version = c(1,0,0),
                options = options,
                results = enhancednonparametricResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Enhanced Non-Parametric Tests
#'
#' 
#' @param data the data as a data frame
#' @param dependent The dependent variable from \code{data}, variable must be
#'   numeric
#' @param grouping The grouping variable from \code{data}, variable must be a
#'   factor
#' @param mann_whitney Perform Mann-Whitney U test for two-group comparisons
#' @param kruskal_wallis Perform Kruskal-Wallis test for multiple-group
#'   comparisons
#' @param posthoc_dunn Perform Dunn's test for pairwise comparisons following
#'   significant Kruskal-Wallis
#' @param correction_method Method for multiple comparison correction in post
#'   hoc tests
#' @param assumptions Check and report assumptions for non-parametric tests
#' @param effect_sizes Calculate and report effect sizes with confidence
#'   intervals
#' @param descriptives Show descriptive statistics for each group
#' @param show_plots Generate boxplots and violin plots for group comparisons
#' @param plot_type Type of distribution plot to generate
#' @param confidence_level Confidence level for effect size confidence
#'   intervals
#' @param bootstrap_samples Number of bootstrap samples for confidence
#'   interval estimation
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$instructions} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$descriptives} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$assumptions} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$mannwhitney} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$kruskalwallis} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$posthoc} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$boxplot} \tab \tab \tab \tab \tab Box plots with violin plots showing group distributions \cr
#'   \code{results$interpretation} \tab \tab \tab \tab \tab Clinical context and interpretation guidelines for non-parametric tests \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$descriptives$asDF}
#'
#' \code{as.data.frame(results$descriptives)}
#'
#' @export
enhancednonparametric <- function(
    data,
    dependent,
    grouping,
    mann_whitney = TRUE,
    kruskal_wallis = TRUE,
    posthoc_dunn = TRUE,
    correction_method = "bonferroni",
    assumptions = TRUE,
    effect_sizes = TRUE,
    descriptives = TRUE,
    show_plots = TRUE,
    plot_type = "both",
    confidence_level = 0.95,
    bootstrap_samples = 1000) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("enhancednonparametric requires jmvcore to be installed (restart may be required)")

    if ( ! missing(dependent)) dependent <- jmvcore::resolveQuo(jmvcore::enquo(dependent))
    if ( ! missing(grouping)) grouping <- jmvcore::resolveQuo(jmvcore::enquo(grouping))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dependent), dependent, NULL),
            `if`( ! missing(grouping), grouping, NULL))

    for (v in grouping) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- enhancednonparametricOptions$new(
        dependent = dependent,
        grouping = grouping,
        mann_whitney = mann_whitney,
        kruskal_wallis = kruskal_wallis,
        posthoc_dunn = posthoc_dunn,
        correction_method = correction_method,
        assumptions = assumptions,
        effect_sizes = effect_sizes,
        descriptives = descriptives,
        show_plots = show_plots,
        plot_type = plot_type,
        confidence_level = confidence_level,
        bootstrap_samples = bootstrap_samples)

    analysis <- enhancednonparametricClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

