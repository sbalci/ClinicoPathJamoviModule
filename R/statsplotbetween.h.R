
# This file is automatically generated, you probably don't want to edit this

statsplotbetweenOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "statsplotbetweenOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dep = NULL,
            group = NULL, ...) {

            super$initialize(
                package='ClinicoPath',
                name='statsplotbetween',
                requiresData=TRUE,
                ...)

            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group)

            self$.addOption(private$..dep)
            self$.addOption(private$..group)
        }),
    active = list(
        dep = function() private$..dep$value,
        group = function() private$..group$value),
    private = list(
        ..dep = NA,
        ..group = NA)
)

statsplotbetweenResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        text1 = function() private$.items[["text1"]],
        text2 = function() private$.items[["text2"]],
        plothtml = function() private$.items[["plothtml"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Stats Plot Between")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Stats Plot Between"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text1",
                title="Text1"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text2",
                title="Text2"))
            self$add(jmvcore::Html$new(
                options=options,
                name="plothtml",
                title="plothtml"))
            self$add(jmvcore::Image$new(
                options=options,
                title="D3",
                name="plot",
                width=600,
                height=450,
                renderFun=".plot",
                requiresData=TRUE,
                clearWith=list(
                    "dep",
                    "group")))}))

statsplotbetweenBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "statsplotbetweenBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'statsplotbetween',
                version = c(1,0,0),
                options = options,
                results = statsplotbetweenResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Stats Plot Between
#'
#' 
#' @param data .
#' @param dep .
#' @param group .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$text1} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$text2} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$plothtml} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
statsplotbetween <- function(
    data,
    dep,
    group) {

    if ( ! requireNamespace('jmvcore'))
        stop('statsplotbetween requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL),
            `if`( ! missing(group), group, NULL))


    options <- statsplotbetweenOptions$new(
        dep = dep,
        group = group)

    analysis <- statsplotbetweenClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
