
# This file is automatically generated, you probably don't want to edit this

jjcoefstatsOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jjcoefstatsOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dep = NULL,
            group = NULL, ...) {

            super$initialize(
                package='ClinicoPath',
                name='jjcoefstats',
                requiresData=TRUE,
                ...)

            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group)

            self$.addOption(private$..dep)
            self$.addOption(private$..group)
        }),
    active = list(
        dep = function() private$..dep$value,
        group = function() private$..group$value),
    private = list(
        ..dep = NA,
        ..group = NA)
)

jjcoefstatsResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Dot-and-whisker",
                refs=list(
                    "ggplot2",
                    "ggstatsplot"))
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="To Do",
                clearWith=list(
                    "dep",
                    "group",
                    "grvar")))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="`Pie Chart ${group} - {dep}`",
                width=600,
                height=450,
                renderFun=".plot",
                requiresData=TRUE,
                clearWith=list(
                    "dep",
                    "group",
                    "grvar")))}))

jjcoefstatsBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jjcoefstatsBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'jjcoefstats',
                version = c(1,0,0),
                options = options,
                results = jjcoefstatsResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Dot-and-whisker
#'
#' 
#' @param data .
#' @param dep .
#' @param group .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
jjcoefstats <- function(
    data,
    dep,
    group) {

    if ( ! requireNamespace('jmvcore'))
        stop('jjcoefstats requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL),
            `if`( ! missing(group), group, NULL))


    options <- jjcoefstatsOptions$new(
        dep = dep,
        group = group)

    analysis <- jjcoefstatsClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
