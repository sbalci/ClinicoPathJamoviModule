
# This file is automatically generated, you probably don't want to edit this

jjradarplotOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jjradarplotOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            vars = NULL,
            categoryVar = NULL,
            splitBy = NULL,
            scaleData = TRUE,
            aggregationMethod = "mean",
            alpha = 0.25,
            lineSize = 1,
            pointSize = 2,
            title = "Radar Plot",
            legendPosition = "right",
            axisLabelSize = 10,
            facetLabelSize = 11,
            excl = FALSE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="jjradarplot",
                requiresData=TRUE,
                ...)

            private$..vars <- jmvcore::OptionVariables$new(
                "vars",
                vars,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..categoryVar <- jmvcore::OptionVariable$new(
                "categoryVar",
                categoryVar,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..splitBy <- jmvcore::OptionVariable$new(
                "splitBy",
                splitBy,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor"),
                default=NULL)
            private$..scaleData <- jmvcore::OptionBool$new(
                "scaleData",
                scaleData,
                default=TRUE)
            private$..aggregationMethod <- jmvcore::OptionList$new(
                "aggregationMethod",
                aggregationMethod,
                options=list(
                    "mean",
                    "median"),
                default="mean")
            private$..alpha <- jmvcore::OptionNumber$new(
                "alpha",
                alpha,
                min=0,
                max=1,
                default=0.25)
            private$..lineSize <- jmvcore::OptionNumber$new(
                "lineSize",
                lineSize,
                min=0.1,
                max=3,
                default=1)
            private$..pointSize <- jmvcore::OptionNumber$new(
                "pointSize",
                pointSize,
                min=0.5,
                max=5,
                default=2)
            private$..title <- jmvcore::OptionString$new(
                "title",
                title,
                default="Radar Plot")
            private$..legendPosition <- jmvcore::OptionList$new(
                "legendPosition",
                legendPosition,
                options=list(
                    "right",
                    "left",
                    "top",
                    "bottom",
                    "none"),
                default="right")
            private$..axisLabelSize <- jmvcore::OptionNumber$new(
                "axisLabelSize",
                axisLabelSize,
                min=6,
                max=20,
                default=10)
            private$..facetLabelSize <- jmvcore::OptionNumber$new(
                "facetLabelSize",
                facetLabelSize,
                min=6,
                max=20,
                default=11)
            private$..excl <- jmvcore::OptionBool$new(
                "excl",
                excl,
                default=FALSE)

            self$.addOption(private$..vars)
            self$.addOption(private$..categoryVar)
            self$.addOption(private$..splitBy)
            self$.addOption(private$..scaleData)
            self$.addOption(private$..aggregationMethod)
            self$.addOption(private$..alpha)
            self$.addOption(private$..lineSize)
            self$.addOption(private$..pointSize)
            self$.addOption(private$..title)
            self$.addOption(private$..legendPosition)
            self$.addOption(private$..axisLabelSize)
            self$.addOption(private$..facetLabelSize)
            self$.addOption(private$..excl)
        }),
    active = list(
        vars = function() private$..vars$value,
        categoryVar = function() private$..categoryVar$value,
        splitBy = function() private$..splitBy$value,
        scaleData = function() private$..scaleData$value,
        aggregationMethod = function() private$..aggregationMethod$value,
        alpha = function() private$..alpha$value,
        lineSize = function() private$..lineSize$value,
        pointSize = function() private$..pointSize$value,
        title = function() private$..title$value,
        legendPosition = function() private$..legendPosition$value,
        axisLabelSize = function() private$..axisLabelSize$value,
        facetLabelSize = function() private$..facetLabelSize$value,
        excl = function() private$..excl$value),
    private = list(
        ..vars = NA,
        ..categoryVar = NA,
        ..splitBy = NA,
        ..scaleData = NA,
        ..aggregationMethod = NA,
        ..alpha = NA,
        ..lineSize = NA,
        ..pointSize = NA,
        ..title = NA,
        ..legendPosition = NA,
        ..axisLabelSize = NA,
        ..facetLabelSize = NA,
        ..excl = NA)
)

jjradarplotResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jjradarplotResults",
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Radar Plot")
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="",
                visible=TRUE))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Radar Plot",
                width=600,
                height=600,
                renderFun=".plot",
                visible=TRUE))}))

jjradarplotBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jjradarplotBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "jjradarplot",
                version = c(0,0,31),
                options = options,
                results = jjradarplotResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Radar Plot
#'
#' 'Create radar plots (spider plots) for multivariate data visualization.
#' This function allows comparison of multiple variables across different 
#' categories.'
#' 
#'
#' @examples
#' \donttest{
#' # example will be added
#'}
#' @param data The data as a data frame.
#' @param vars A list of numeric variables to be used as radar axes (minimum 3
#'   required).
#' @param categoryVar A categorical variable that defines different radar
#'   polygons.
#' @param splitBy Optional variable to create separate radar plots in facets.
#' @param scaleData Whether to scale all variables to 0-1 range for better
#'   comparison.
#' @param aggregationMethod How to aggregate data for each category.
#' @param alpha Transparency level for polygon fill (0 = transparent, 1 =
#'   opaque).
#' @param lineSize Width of polygon outline.
#' @param pointSize Size of points at vertices.
#' @param title Title for the plot.
#' @param legendPosition Position of the legend.
#' @param axisLabelSize Size of axis labels.
#' @param facetLabelSize Size of facet labels (when using Split By).
#' @param excl TRUE or FALSE (default), exclude cases analysis by analysis
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
jjradarplot <- function(
    data,
    vars,
    categoryVar,
    splitBy = NULL,
    scaleData = TRUE,
    aggregationMethod = "mean",
    alpha = 0.25,
    lineSize = 1,
    pointSize = 2,
    title = "Radar Plot",
    legendPosition = "right",
    axisLabelSize = 10,
    facetLabelSize = 11,
    excl = FALSE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("jjradarplot requires jmvcore to be installed (restart may be required)")

    if ( ! missing(vars)) vars <- jmvcore::resolveQuo(jmvcore::enquo(vars))
    if ( ! missing(categoryVar)) categoryVar <- jmvcore::resolveQuo(jmvcore::enquo(categoryVar))
    if ( ! missing(splitBy)) splitBy <- jmvcore::resolveQuo(jmvcore::enquo(splitBy))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(vars), vars, NULL),
            `if`( ! missing(categoryVar), categoryVar, NULL),
            `if`( ! missing(splitBy), splitBy, NULL))

    for (v in categoryVar) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])
    for (v in splitBy) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- jjradarplotOptions$new(
        vars = vars,
        categoryVar = categoryVar,
        splitBy = splitBy,
        scaleData = scaleData,
        aggregationMethod = aggregationMethod,
        alpha = alpha,
        lineSize = lineSize,
        pointSize = pointSize,
        title = title,
        legendPosition = legendPosition,
        axisLabelSize = axisLabelSize,
        facetLabelSize = facetLabelSize,
        excl = excl)

    analysis <- jjradarplotClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

