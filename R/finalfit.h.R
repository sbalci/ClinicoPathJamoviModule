
# This file is automatically generated, you probably don't want to edit this

finalfitOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "finalfitOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            explanatory = NULL,
            outcome = NULL,
            overalltime = NULL, ...) {

            super$initialize(
                package='ClinicoPath',
                name='finalfit',
                requiresData=TRUE,
                ...)

            private$..explanatory <- jmvcore::OptionVariable$new(
                "explanatory",
                explanatory)
            private$..outcome <- jmvcore::OptionVariable$new(
                "outcome",
                outcome)
            private$..overalltime <- jmvcore::OptionVariable$new(
                "overalltime",
                overalltime)

            self$.addOption(private$..explanatory)
            self$.addOption(private$..outcome)
            self$.addOption(private$..overalltime)
        }),
    active = list(
        explanatory = function() private$..explanatory$value,
        outcome = function() private$..outcome$value,
        overalltime = function() private$..overalltime$value),
    private = list(
        ..explanatory = NA,
        ..outcome = NA,
        ..overalltime = NA)
)

finalfitResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="FinalFit")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="FinalFit"))}))

finalfitBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "finalfitBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'finalfit',
                version = c(1,0,0),
                options = options,
                results = finalfitResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE)
        }))

#' FinalFit
#'
#' 
#' @param data .
#' @param explanatory .
#' @param outcome .
#' @param overalltime .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
finalfit <- function(
    data,
    explanatory,
    outcome,
    overalltime) {

    if ( ! requireNamespace('jmvcore'))
        stop('finalfit requires jmvcore to be installed (restart may be required)')

    if ( ! missing(explanatory)) explanatory <- jmvcore::resolveQuo(jmvcore::enquo(explanatory))
    if ( ! missing(outcome)) outcome <- jmvcore::resolveQuo(jmvcore::enquo(outcome))
    if ( ! missing(overalltime)) overalltime <- jmvcore::resolveQuo(jmvcore::enquo(overalltime))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(explanatory), explanatory, NULL),
            `if`( ! missing(outcome), outcome, NULL),
            `if`( ! missing(overalltime), overalltime, NULL))


    options <- finalfitOptions$new(
        explanatory = explanatory,
        outcome = outcome,
        overalltime = overalltime)

    analysis <- finalfitClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
