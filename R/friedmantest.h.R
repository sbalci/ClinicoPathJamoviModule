
# This file is automatically generated, you probably don't want to edit this

friedmantestOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "friedmantestOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dependent = NULL,
            subject = NULL,
            within = NULL,
            method = "asymptotic",
            alpha = 0.05,
            posthoc = TRUE,
            posthoc_method = "nemenyi",
            correction = "bonferroni",
            effect_size = TRUE,
            confidence_level = 0.95,
            show_ranks = TRUE,
            show_descriptives = TRUE,
            show_assumptions = TRUE,
            clinical_interpretation = TRUE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="friedmantest",
                requiresData=TRUE,
                ...)

            private$..dependent <- jmvcore::OptionVariable$new(
                "dependent",
                dependent,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..subject <- jmvcore::OptionVariable$new(
                "subject",
                subject,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..within <- jmvcore::OptionVariable$new(
                "within",
                within,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "asymptotic",
                    "exact",
                    "monte_carlo"),
                default="asymptotic")
            private$..alpha <- jmvcore::OptionNumber$new(
                "alpha",
                alpha,
                min=0.01,
                max=0.1,
                default=0.05)
            private$..posthoc <- jmvcore::OptionBool$new(
                "posthoc",
                posthoc,
                default=TRUE)
            private$..posthoc_method <- jmvcore::OptionList$new(
                "posthoc_method",
                posthoc_method,
                options=list(
                    "nemenyi",
                    "wilcoxon_signed_rank",
                    "sign_test"),
                default="nemenyi")
            private$..correction <- jmvcore::OptionList$new(
                "correction",
                correction,
                options=list(
                    "bonferroni",
                    "holm",
                    "fdr",
                    "none"),
                default="bonferroni")
            private$..effect_size <- jmvcore::OptionBool$new(
                "effect_size",
                effect_size,
                default=TRUE)
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.8,
                max=0.99,
                default=0.95)
            private$..show_ranks <- jmvcore::OptionBool$new(
                "show_ranks",
                show_ranks,
                default=TRUE)
            private$..show_descriptives <- jmvcore::OptionBool$new(
                "show_descriptives",
                show_descriptives,
                default=TRUE)
            private$..show_assumptions <- jmvcore::OptionBool$new(
                "show_assumptions",
                show_assumptions,
                default=TRUE)
            private$..clinical_interpretation <- jmvcore::OptionBool$new(
                "clinical_interpretation",
                clinical_interpretation,
                default=TRUE)

            self$.addOption(private$..dependent)
            self$.addOption(private$..subject)
            self$.addOption(private$..within)
            self$.addOption(private$..method)
            self$.addOption(private$..alpha)
            self$.addOption(private$..posthoc)
            self$.addOption(private$..posthoc_method)
            self$.addOption(private$..correction)
            self$.addOption(private$..effect_size)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..show_ranks)
            self$.addOption(private$..show_descriptives)
            self$.addOption(private$..show_assumptions)
            self$.addOption(private$..clinical_interpretation)
        }),
    active = list(
        dependent = function() private$..dependent$value,
        subject = function() private$..subject$value,
        within = function() private$..within$value,
        method = function() private$..method$value,
        alpha = function() private$..alpha$value,
        posthoc = function() private$..posthoc$value,
        posthoc_method = function() private$..posthoc_method$value,
        correction = function() private$..correction$value,
        effect_size = function() private$..effect_size$value,
        confidence_level = function() private$..confidence_level$value,
        show_ranks = function() private$..show_ranks$value,
        show_descriptives = function() private$..show_descriptives$value,
        show_assumptions = function() private$..show_assumptions$value,
        clinical_interpretation = function() private$..clinical_interpretation$value),
    private = list(
        ..dependent = NA,
        ..subject = NA,
        ..within = NA,
        ..method = NA,
        ..alpha = NA,
        ..posthoc = NA,
        ..posthoc_method = NA,
        ..correction = NA,
        ..effect_size = NA,
        ..confidence_level = NA,
        ..show_ranks = NA,
        ..show_descriptives = NA,
        ..show_assumptions = NA,
        ..clinical_interpretation = NA)
)

friedmantestResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "friedmantestResults",
    inherit = jmvcore::Group,
    active = list(
        instructions = function() private$.items[["instructions"]],
        dataInfo = function() private$.items[["dataInfo"]],
        descriptiveStats = function() private$.items[["descriptiveStats"]],
        friedmanTest = function() private$.items[["friedmanTest"]],
        effectSize = function() private$.items[["effectSize"]],
        rankAnalysis = function() private$.items[["rankAnalysis"]],
        pairwiseComparisons = function() private$.items[["pairwiseComparisons"]],
        assumptionAssessment = function() private$.items[["assumptionAssessment"]],
        clinicalInterpretation = function() private$.items[["clinicalInterpretation"]],
        boxplotByCondition = function() private$.items[["boxplotByCondition"]],
        meanRankPlot = function() private$.items[["meanRankPlot"]],
        pairwiseComparisonPlot = function() private$.items[["pairwiseComparisonPlot"]],
        profilePlot = function() private$.items[["profilePlot"]],
        methodExplanation = function() private$.items[["methodExplanation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Friedman Test for Non-Parametric Repeated Measures")
            self$add(jmvcore::Html$new(
                options=options,
                name="instructions",
                title="Analysis Instructions",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="dataInfo",
                title="Data Summary",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "dependent",
                    "subject",
                    "within"),
                columns=list(
                    list(
                        `name`="characteristic", 
                        `title`="Characteristic", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="descriptiveStats",
                title="Descriptive Statistics",
                visible="(show_descriptives)",
                rows=1,
                clearWith=list(
                    "show_descriptives",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="condition", 
                        `title`="Condition", 
                        `type`="text"),
                    list(
                        `name`="n", 
                        `title`="N", 
                        `type`="integer"),
                    list(
                        `name`="median", 
                        `title`="Median", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="q1", 
                        `title`="Q1", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="q3", 
                        `title`="Q3", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="iqr", 
                        `title`="IQR", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="mean_rank", 
                        `title`="Mean Rank", 
                        `type`="number", 
                        `format`="zto:2"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="friedmanTest",
                title="Friedman Test Results",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "dependent",
                    "subject",
                    "within",
                    "method"),
                columns=list(
                    list(
                        `name`="statistic", 
                        `title`="Chi-square", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="df", 
                        `title`="df", 
                        `type`="integer"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="significance", 
                        `title`="Significance", 
                        `type`="text"),
                    list(
                        `name`="conclusion", 
                        `title`="Conclusion", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="effectSize",
                title="Effect Size Measures",
                visible="(effect_size)",
                rows=1,
                clearWith=list(
                    "effect_size",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="measure", 
                        `title`="Effect Size", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="confidence_interval", 
                        `title`="Confidence Interval", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="rankAnalysis",
                title="Rank Analysis",
                visible="(show_ranks)",
                rows=1,
                clearWith=list(
                    "show_ranks",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="condition", 
                        `title`="Condition", 
                        `type`="text"),
                    list(
                        `name`="mean_rank", 
                        `title`="Mean Rank", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="rank_sum", 
                        `title`="Rank Sum", 
                        `type`="number", 
                        `format`="zto:1"),
                    list(
                        `name`="expected_rank", 
                        `title`="Expected Rank", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="deviation", 
                        `title`="Deviation", 
                        `type`="number", 
                        `format`="zto:2"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="pairwiseComparisons",
                title="Post-hoc Pairwise Comparisons",
                visible="(posthoc)",
                rows=1,
                clearWith=list(
                    "posthoc",
                    "posthoc_method",
                    "correction",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="comparison", 
                        `title`="Comparison", 
                        `type`="text"),
                    list(
                        `name`="mean_rank_diff", 
                        `title`="Mean Rank Difference", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="test_statistic", 
                        `title`="Test Statistic", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="p_value_raw", 
                        `title`="Raw p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="p_value_adjusted", 
                        `title`="Adjusted p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="significance", 
                        `title`="Significance", 
                        `type`="text"),
                    list(
                        `name`="effect_size", 
                        `title`="Effect Size", 
                        `type`="number", 
                        `format`="zto:3"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="assumptionAssessment",
                title="Assumption Assessment",
                visible="(show_assumptions)",
                rows=1,
                clearWith=list(
                    "show_assumptions",
                    "dependent",
                    "subject",
                    "within"),
                columns=list(
                    list(
                        `name`="assumption", 
                        `title`="Assumption", 
                        `type`="text"),
                    list(
                        `name`="status", 
                        `title`="Status", 
                        `type`="text"),
                    list(
                        `name`="details", 
                        `title`="Details", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Recommendation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="clinicalInterpretation",
                title="Clinical Interpretation Guide",
                visible="(clinical_interpretation)",
                rows=1,
                clearWith=list(
                    "clinical_interpretation",
                    "dependent",
                    "within",
                    "alpha"),
                columns=list(
                    list(
                        `name`="aspect", 
                        `title`="Clinical Aspect", 
                        `type`="text"),
                    list(
                        `name`="finding", 
                        `title`="Statistical Finding", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Clinical Interpretation", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Clinical Recommendation", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="boxplotByCondition",
                title="Box Plot by Condition",
                visible=TRUE,
                requiresData=TRUE,
                width=800,
                height=600,
                renderFun=".plotBoxplotByCondition",
                clearWith=list(
                    "dependent",
                    "within")))
            self$add(jmvcore::Image$new(
                options=options,
                name="meanRankPlot",
                title="Mean Rank Comparison",
                visible="(show_ranks)",
                requiresData=TRUE,
                width=700,
                height=500,
                renderFun=".plotMeanRanks",
                clearWith=list(
                    "show_ranks",
                    "dependent",
                    "within")))
            self$add(jmvcore::Image$new(
                options=options,
                name="pairwiseComparisonPlot",
                title="Post-hoc Comparison Matrix",
                visible="(posthoc)",
                requiresData=TRUE,
                width=600,
                height=600,
                renderFun=".plotPairwiseComparisons",
                clearWith=list(
                    "posthoc",
                    "correction",
                    "dependent",
                    "within")))
            self$add(jmvcore::Image$new(
                options=options,
                name="profilePlot",
                title="Individual Profile Plot",
                visible=TRUE,
                requiresData=TRUE,
                width=800,
                height=600,
                renderFun=".plotProfiles",
                clearWith=list(
                    "dependent",
                    "subject",
                    "within")))
            self$add(jmvcore::Html$new(
                options=options,
                name="methodExplanation",
                title="Method and Clinical Context",
                visible=TRUE))}))

friedmantestBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "friedmantestBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "friedmantest",
                version = c(1,0,0),
                options = options,
                results = friedmantestResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Friedman Test for Non-Parametric Repeated Measures
#'
#' Friedman test for non-parametric analysis of repeated measures data with 
#' continuous  or ordinal outcomes. This is the non-parametric alternative to 
#' repeated measures  ANOVA when normality assumptions are violated. It tests 
#' whether the median values  differ significantly across multiple related 
#' measurements or time points.
#' 
#'
#' @examples
#' data \%>\%
#' friedmantest(
#'     dependent = "measurement_value",
#'     subject = "patient_id",
#'     within = "time_point",
#'     posthoc = TRUE
#' )
#'
#' @param data the data as a data frame
#' @param dependent Continuous or ordinal dependent variable measured
#'   repeatedly
#' @param subject Variable identifying subjects/cases for repeated
#'   measurements
#' @param within Factor variable indicating the repeated measure
#'   conditions/time points
#' @param method Method for calculating p-values
#' @param alpha Alpha level for hypothesis testing
#' @param posthoc Perform pairwise post-hoc comparisons when Friedman test is
#'   significant
#' @param posthoc_method Method for post-hoc pairwise comparisons
#' @param correction Multiple comparison correction method for post-hoc tests
#' @param effect_size Include effect size measures (Kendall's W coefficient of
#'   concordance)
#' @param confidence_level Confidence level for confidence intervals
#' @param show_ranks Show detailed rank analysis and mean rank comparisons
#' @param show_descriptives Show descriptive statistics for each condition
#' @param show_assumptions Assess assumptions and provide recommendations
#' @param clinical_interpretation Provide clinical interpretation guidance for
#'   results
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$instructions} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$dataInfo} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$descriptiveStats} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$friedmanTest} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$effectSize} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$rankAnalysis} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$pairwiseComparisons} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$assumptionAssessment} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$clinicalInterpretation} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$boxplotByCondition} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$meanRankPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$pairwiseComparisonPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$profilePlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$methodExplanation} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$dataInfo$asDF}
#'
#' \code{as.data.frame(results$dataInfo)}
#'
#' @export
friedmantest <- function(
    data,
    dependent,
    subject,
    within,
    method = "asymptotic",
    alpha = 0.05,
    posthoc = TRUE,
    posthoc_method = "nemenyi",
    correction = "bonferroni",
    effect_size = TRUE,
    confidence_level = 0.95,
    show_ranks = TRUE,
    show_descriptives = TRUE,
    show_assumptions = TRUE,
    clinical_interpretation = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("friedmantest requires jmvcore to be installed (restart may be required)")

    if ( ! missing(dependent)) dependent <- jmvcore::resolveQuo(jmvcore::enquo(dependent))
    if ( ! missing(subject)) subject <- jmvcore::resolveQuo(jmvcore::enquo(subject))
    if ( ! missing(within)) within <- jmvcore::resolveQuo(jmvcore::enquo(within))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dependent), dependent, NULL),
            `if`( ! missing(subject), subject, NULL),
            `if`( ! missing(within), within, NULL))

    for (v in within) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- friedmantestOptions$new(
        dependent = dependent,
        subject = subject,
        within = within,
        method = method,
        alpha = alpha,
        posthoc = posthoc,
        posthoc_method = posthoc_method,
        correction = correction,
        effect_size = effect_size,
        confidence_level = confidence_level,
        show_ranks = show_ranks,
        show_descriptives = show_descriptives,
        show_assumptions = show_assumptions,
        clinical_interpretation = clinical_interpretation)

    analysis <- friedmantestClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

