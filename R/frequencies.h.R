
# This file is automatically generated, you probably don't want to edit this

frequenciesOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "frequenciesOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            vars = NULL,
            show_na = FALSE,
            na_lev = FALSE, ...) {

            super$initialize(
                package='ClinicoPath',
                name='frequencies',
                requiresData=TRUE,
                ...)

            private$..vars <- jmvcore::OptionVariables$new(
                "vars",
                vars,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..show_na <- jmvcore::OptionBool$new(
                "show_na",
                show_na,
                default=FALSE)
            private$..na_lev <- jmvcore::OptionBool$new(
                "na_lev",
                na_lev,
                default=FALSE)

            self$.addOption(private$..vars)
            self$.addOption(private$..show_na)
            self$.addOption(private$..na_lev)
        }),
    active = list(
        vars = function() private$..vars$value,
        show_na = function() private$..show_na$value,
        na_lev = function() private$..na_lev$value),
    private = list(
        ..vars = NA,
        ..show_na = NA,
        ..na_lev = NA)
)

frequenciesResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Frequencies",
                refs="janitor")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Frequencies"))}))

frequenciesBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "frequenciesBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'frequencies',
                version = c(1,0,0),
                options = options,
                results = frequenciesResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE)
        }))

#' Frequencies
#'
#' 
#' @param data .
#' @param vars .
#' @param show_na .
#' @param na_lev .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
frequencies <- function(
    data,
    vars,
    show_na = FALSE,
    na_lev = FALSE) {

    if ( ! requireNamespace('jmvcore'))
        stop('frequencies requires jmvcore to be installed (restart may be required)')

    if ( ! missing(vars)) vars <- jmvcore::resolveQuo(jmvcore::enquo(vars))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(vars), vars, NULL))

    for (v in vars) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- frequenciesOptions$new(
        vars = vars,
        show_na = show_na,
        na_lev = na_lev)

    analysis <- frequenciesClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
