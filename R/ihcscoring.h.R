
# This file is automatically generated, you probably don't want to edit this

ihcscoringOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ihcscoringOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            intensity_var = NULL,
            proportion_var = NULL,
            sample_id_var = NULL,
            group_var = NULL,
            scoring_method = "both",
            binary_cutpoint = 100,
            allred_cutpoint = 3,
            intensity_scale = "standard",
            biomarker_type = "other",
            show_plots = TRUE,
            show_agreement_plots = TRUE,
            include_statistics = TRUE,
            include_digital_validation = FALSE,
            agreement_analysis = TRUE,
            quality_control = TRUE,
            clinical_interpretation = TRUE,
            export_results = FALSE,
            multiple_cutoffs = FALSE,
            cutoff_values = "1, 5, 10, 25, 50",
            cps_analysis = FALSE,
            immune_cells_var = NULL,
            tumor_cells_var = NULL,
            cutoff_comparison = TRUE,
            confidence_level = 0.95,
            bootstrap_n = 1000,
            automated_analysis = FALSE,
            segmentation_method = "manual",
            color_deconvolution = TRUE,
            dab_thresholds = "0.1, 0.3, 0.6",
            minimum_nuclear_area = 50,
            maximum_nuclear_area = 2000,
            batch_processing = FALSE,
            image_format = "tiff",
            validation_metrics = TRUE,
            molecular_classification = FALSE,
            classification_system = "bladder_mibc",
            primary_marker1 = NULL,
            primary_marker2 = NULL,
            secondary_marker = NULL,
            classification_cutoffs = "20, 20, 70",
            pd1_marker = NULL,
            pdl1_marker = NULL,
            checkpoint_cutoffs = "1, 10",
            subtype_statistics = TRUE,
            subtype_visualization = TRUE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="ihcscoring",
                requiresData=TRUE,
                ...)

            private$..intensity_var <- jmvcore::OptionVariable$new(
                "intensity_var",
                intensity_var,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..proportion_var <- jmvcore::OptionVariable$new(
                "proportion_var",
                proportion_var,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..sample_id_var <- jmvcore::OptionVariable$new(
                "sample_id_var",
                sample_id_var,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor",
                    "id"))
            private$..group_var <- jmvcore::OptionVariable$new(
                "group_var",
                group_var,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..scoring_method <- jmvcore::OptionList$new(
                "scoring_method",
                scoring_method,
                options=list(
                    "hscore",
                    "allred",
                    "both"),
                default="both")
            private$..binary_cutpoint <- jmvcore::OptionNumber$new(
                "binary_cutpoint",
                binary_cutpoint,
                min=0,
                max=300,
                default=100)
            private$..allred_cutpoint <- jmvcore::OptionNumber$new(
                "allred_cutpoint",
                allred_cutpoint,
                min=0,
                max=8,
                default=3)
            private$..intensity_scale <- jmvcore::OptionList$new(
                "intensity_scale",
                intensity_scale,
                options=list(
                    "standard",
                    "extended",
                    "custom"),
                default="standard")
            private$..biomarker_type <- jmvcore::OptionList$new(
                "biomarker_type",
                biomarker_type,
                options=list(
                    "er",
                    "pr",
                    "her2",
                    "ki67",
                    "pdl1",
                    "other"),
                default="other")
            private$..show_plots <- jmvcore::OptionBool$new(
                "show_plots",
                show_plots,
                default=TRUE)
            private$..show_agreement_plots <- jmvcore::OptionBool$new(
                "show_agreement_plots",
                show_agreement_plots,
                default=TRUE)
            private$..include_statistics <- jmvcore::OptionBool$new(
                "include_statistics",
                include_statistics,
                default=TRUE)
            private$..include_digital_validation <- jmvcore::OptionBool$new(
                "include_digital_validation",
                include_digital_validation,
                default=FALSE)
            private$..agreement_analysis <- jmvcore::OptionBool$new(
                "agreement_analysis",
                agreement_analysis,
                default=TRUE)
            private$..quality_control <- jmvcore::OptionBool$new(
                "quality_control",
                quality_control,
                default=TRUE)
            private$..clinical_interpretation <- jmvcore::OptionBool$new(
                "clinical_interpretation",
                clinical_interpretation,
                default=TRUE)
            private$..export_results <- jmvcore::OptionBool$new(
                "export_results",
                export_results,
                default=FALSE)
            private$..multiple_cutoffs <- jmvcore::OptionBool$new(
                "multiple_cutoffs",
                multiple_cutoffs,
                default=FALSE)
            private$..cutoff_values <- jmvcore::OptionString$new(
                "cutoff_values",
                cutoff_values,
                default="1, 5, 10, 25, 50")
            private$..cps_analysis <- jmvcore::OptionBool$new(
                "cps_analysis",
                cps_analysis,
                default=FALSE)
            private$..immune_cells_var <- jmvcore::OptionVariable$new(
                "immune_cells_var",
                immune_cells_var,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..tumor_cells_var <- jmvcore::OptionVariable$new(
                "tumor_cells_var",
                tumor_cells_var,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..cutoff_comparison <- jmvcore::OptionBool$new(
                "cutoff_comparison",
                cutoff_comparison,
                default=TRUE)
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.8,
                max=0.99,
                default=0.95)
            private$..bootstrap_n <- jmvcore::OptionInteger$new(
                "bootstrap_n",
                bootstrap_n,
                min=100,
                max=5000,
                default=1000)
            private$..automated_analysis <- jmvcore::OptionBool$new(
                "automated_analysis",
                automated_analysis,
                default=FALSE)
            private$..segmentation_method <- jmvcore::OptionList$new(
                "segmentation_method",
                segmentation_method,
                options=list(
                    "manual",
                    "stardist",
                    "cellpose",
                    "watershed"),
                default="manual")
            private$..color_deconvolution <- jmvcore::OptionBool$new(
                "color_deconvolution",
                color_deconvolution,
                default=TRUE)
            private$..dab_thresholds <- jmvcore::OptionString$new(
                "dab_thresholds",
                dab_thresholds,
                default="0.1, 0.3, 0.6")
            private$..minimum_nuclear_area <- jmvcore::OptionNumber$new(
                "minimum_nuclear_area",
                minimum_nuclear_area,
                min=10,
                max=1000,
                default=50)
            private$..maximum_nuclear_area <- jmvcore::OptionNumber$new(
                "maximum_nuclear_area",
                maximum_nuclear_area,
                min=100,
                max=10000,
                default=2000)
            private$..batch_processing <- jmvcore::OptionBool$new(
                "batch_processing",
                batch_processing,
                default=FALSE)
            private$..image_format <- jmvcore::OptionList$new(
                "image_format",
                image_format,
                options=list(
                    "tiff",
                    "jpg",
                    "png",
                    "svs",
                    "ndpi"),
                default="tiff")
            private$..validation_metrics <- jmvcore::OptionBool$new(
                "validation_metrics",
                validation_metrics,
                default=TRUE)
            private$..molecular_classification <- jmvcore::OptionBool$new(
                "molecular_classification",
                molecular_classification,
                default=FALSE)
            private$..classification_system <- jmvcore::OptionList$new(
                "classification_system",
                classification_system,
                options=list(
                    "bladder_mibc",
                    "breast_cancer",
                    "lung_cancer",
                    "custom"),
                default="bladder_mibc")
            private$..primary_marker1 <- jmvcore::OptionVariable$new(
                "primary_marker1",
                primary_marker1,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..primary_marker2 <- jmvcore::OptionVariable$new(
                "primary_marker2",
                primary_marker2,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..secondary_marker <- jmvcore::OptionVariable$new(
                "secondary_marker",
                secondary_marker,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..classification_cutoffs <- jmvcore::OptionString$new(
                "classification_cutoffs",
                classification_cutoffs,
                default="20, 20, 70")
            private$..pd1_marker <- jmvcore::OptionVariable$new(
                "pd1_marker",
                pd1_marker,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..pdl1_marker <- jmvcore::OptionVariable$new(
                "pdl1_marker",
                pdl1_marker,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..checkpoint_cutoffs <- jmvcore::OptionString$new(
                "checkpoint_cutoffs",
                checkpoint_cutoffs,
                default="1, 10")
            private$..subtype_statistics <- jmvcore::OptionBool$new(
                "subtype_statistics",
                subtype_statistics,
                default=TRUE)
            private$..subtype_visualization <- jmvcore::OptionBool$new(
                "subtype_visualization",
                subtype_visualization,
                default=TRUE)

            self$.addOption(private$..intensity_var)
            self$.addOption(private$..proportion_var)
            self$.addOption(private$..sample_id_var)
            self$.addOption(private$..group_var)
            self$.addOption(private$..scoring_method)
            self$.addOption(private$..binary_cutpoint)
            self$.addOption(private$..allred_cutpoint)
            self$.addOption(private$..intensity_scale)
            self$.addOption(private$..biomarker_type)
            self$.addOption(private$..show_plots)
            self$.addOption(private$..show_agreement_plots)
            self$.addOption(private$..include_statistics)
            self$.addOption(private$..include_digital_validation)
            self$.addOption(private$..agreement_analysis)
            self$.addOption(private$..quality_control)
            self$.addOption(private$..clinical_interpretation)
            self$.addOption(private$..export_results)
            self$.addOption(private$..multiple_cutoffs)
            self$.addOption(private$..cutoff_values)
            self$.addOption(private$..cps_analysis)
            self$.addOption(private$..immune_cells_var)
            self$.addOption(private$..tumor_cells_var)
            self$.addOption(private$..cutoff_comparison)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..bootstrap_n)
            self$.addOption(private$..automated_analysis)
            self$.addOption(private$..segmentation_method)
            self$.addOption(private$..color_deconvolution)
            self$.addOption(private$..dab_thresholds)
            self$.addOption(private$..minimum_nuclear_area)
            self$.addOption(private$..maximum_nuclear_area)
            self$.addOption(private$..batch_processing)
            self$.addOption(private$..image_format)
            self$.addOption(private$..validation_metrics)
            self$.addOption(private$..molecular_classification)
            self$.addOption(private$..classification_system)
            self$.addOption(private$..primary_marker1)
            self$.addOption(private$..primary_marker2)
            self$.addOption(private$..secondary_marker)
            self$.addOption(private$..classification_cutoffs)
            self$.addOption(private$..pd1_marker)
            self$.addOption(private$..pdl1_marker)
            self$.addOption(private$..checkpoint_cutoffs)
            self$.addOption(private$..subtype_statistics)
            self$.addOption(private$..subtype_visualization)
        }),
    active = list(
        intensity_var = function() private$..intensity_var$value,
        proportion_var = function() private$..proportion_var$value,
        sample_id_var = function() private$..sample_id_var$value,
        group_var = function() private$..group_var$value,
        scoring_method = function() private$..scoring_method$value,
        binary_cutpoint = function() private$..binary_cutpoint$value,
        allred_cutpoint = function() private$..allred_cutpoint$value,
        intensity_scale = function() private$..intensity_scale$value,
        biomarker_type = function() private$..biomarker_type$value,
        show_plots = function() private$..show_plots$value,
        show_agreement_plots = function() private$..show_agreement_plots$value,
        include_statistics = function() private$..include_statistics$value,
        include_digital_validation = function() private$..include_digital_validation$value,
        agreement_analysis = function() private$..agreement_analysis$value,
        quality_control = function() private$..quality_control$value,
        clinical_interpretation = function() private$..clinical_interpretation$value,
        export_results = function() private$..export_results$value,
        multiple_cutoffs = function() private$..multiple_cutoffs$value,
        cutoff_values = function() private$..cutoff_values$value,
        cps_analysis = function() private$..cps_analysis$value,
        immune_cells_var = function() private$..immune_cells_var$value,
        tumor_cells_var = function() private$..tumor_cells_var$value,
        cutoff_comparison = function() private$..cutoff_comparison$value,
        confidence_level = function() private$..confidence_level$value,
        bootstrap_n = function() private$..bootstrap_n$value,
        automated_analysis = function() private$..automated_analysis$value,
        segmentation_method = function() private$..segmentation_method$value,
        color_deconvolution = function() private$..color_deconvolution$value,
        dab_thresholds = function() private$..dab_thresholds$value,
        minimum_nuclear_area = function() private$..minimum_nuclear_area$value,
        maximum_nuclear_area = function() private$..maximum_nuclear_area$value,
        batch_processing = function() private$..batch_processing$value,
        image_format = function() private$..image_format$value,
        validation_metrics = function() private$..validation_metrics$value,
        molecular_classification = function() private$..molecular_classification$value,
        classification_system = function() private$..classification_system$value,
        primary_marker1 = function() private$..primary_marker1$value,
        primary_marker2 = function() private$..primary_marker2$value,
        secondary_marker = function() private$..secondary_marker$value,
        classification_cutoffs = function() private$..classification_cutoffs$value,
        pd1_marker = function() private$..pd1_marker$value,
        pdl1_marker = function() private$..pdl1_marker$value,
        checkpoint_cutoffs = function() private$..checkpoint_cutoffs$value,
        subtype_statistics = function() private$..subtype_statistics$value,
        subtype_visualization = function() private$..subtype_visualization$value),
    private = list(
        ..intensity_var = NA,
        ..proportion_var = NA,
        ..sample_id_var = NA,
        ..group_var = NA,
        ..scoring_method = NA,
        ..binary_cutpoint = NA,
        ..allred_cutpoint = NA,
        ..intensity_scale = NA,
        ..biomarker_type = NA,
        ..show_plots = NA,
        ..show_agreement_plots = NA,
        ..include_statistics = NA,
        ..include_digital_validation = NA,
        ..agreement_analysis = NA,
        ..quality_control = NA,
        ..clinical_interpretation = NA,
        ..export_results = NA,
        ..multiple_cutoffs = NA,
        ..cutoff_values = NA,
        ..cps_analysis = NA,
        ..immune_cells_var = NA,
        ..tumor_cells_var = NA,
        ..cutoff_comparison = NA,
        ..confidence_level = NA,
        ..bootstrap_n = NA,
        ..automated_analysis = NA,
        ..segmentation_method = NA,
        ..color_deconvolution = NA,
        ..dab_thresholds = NA,
        ..minimum_nuclear_area = NA,
        ..maximum_nuclear_area = NA,
        ..batch_processing = NA,
        ..image_format = NA,
        ..validation_metrics = NA,
        ..molecular_classification = NA,
        ..classification_system = NA,
        ..primary_marker1 = NA,
        ..primary_marker2 = NA,
        ..secondary_marker = NA,
        ..classification_cutoffs = NA,
        ..pd1_marker = NA,
        ..pdl1_marker = NA,
        ..checkpoint_cutoffs = NA,
        ..subtype_statistics = NA,
        ..subtype_visualization = NA)
)

ihcscoringResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ihcscoringResults",
    inherit = jmvcore::Group,
    active = list(
        interpretation = function() private$.items[["interpretation"]],
        scorestable = function() private$.items[["scorestable"]],
        statisticstable = function() private$.items[["statisticstable"]],
        agreementtable = function() private$.items[["agreementtable"]],
        qualitycontroltable = function() private$.items[["qualitycontroltable"]],
        distributionplot = function() private$.items[["distributionplot"]],
        correlationplot = function() private$.items[["correlationplot"]],
        agreementplot = function() private$.items[["agreementplot"]],
        cutpointplot = function() private$.items[["cutpointplot"]],
        biomarkerspecific = function() private$.items[["biomarkerspecific"]],
        digitalvalidation = function() private$.items[["digitalvalidation"]],
        automatedanalysis = function() private$.items[["automatedanalysis"]],
        advancedmetrics = function() private$.items[["advancedmetrics"]],
        multiplecutoffs = function() private$.items[["multiplecutoffs"]],
        cutoffplot = function() private$.items[["cutoffplot"]],
        cpsanalysis = function() private$.items[["cpsanalysis"]],
        cpsplot = function() private$.items[["cpsplot"]],
        molecularclassification = function() private$.items[["molecularclassification"]],
        subtypeplot = function() private$.items[["subtypeplot"]],
        checkpointplot = function() private$.items[["checkpointplot"]],
        exportdata = function() private$.items[["exportdata"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="IHC Scoring Standardization")
            self$add(jmvcore::Html$new(
                options=options,
                name="interpretation",
                title="Clinical Interpretation and Guidelines",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="scorestable",
                title="Individual Sample Scores",
                columns=list(
                    list(
                        `name`="sample_id", 
                        `title`="Sample ID", 
                        `type`="integer"),
                    list(
                        `name`="intensity", 
                        `title`="Intensity", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="proportion", 
                        `title`="Proportion (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="hscore", 
                        `title`="H-score", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="allred_intensity", 
                        `title`="Allred Intensity", 
                        `type`="integer"),
                    list(
                        `name`="allred_proportion", 
                        `title`="Allred Proportion", 
                        `type`="integer"),
                    list(
                        `name`="allred_total", 
                        `title`="Allred Total", 
                        `type`="integer"),
                    list(
                        `name`="binary_classification", 
                        `title`="Binary Result", 
                        `type`="text")),
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "scoring_method",
                    "binary_cutpoint",
                    "allred_cutpoint")))
            self$add(jmvcore::Table$new(
                options=options,
                name="statisticstable",
                title="Descriptive Statistics by Score Type",
                columns=list(
                    list(
                        `name`="score_type", 
                        `title`="Score Type", 
                        `type`="text"),
                    list(
                        `name`="mean", 
                        `title`="Mean", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="median", 
                        `title`="Median", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="sd", 
                        `title`="SD", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="min", 
                        `title`="Min", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="max", 
                        `title`="Max", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="q25", 
                        `title`="Q1", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="q75", 
                        `title`="Q3", 
                        `type`="number", 
                        `format`="zto")),
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "scoring_method",
                    "include_statistics")))
            self$add(jmvcore::Table$new(
                options=options,
                name="agreementtable",
                title="Method Agreement Analysis",
                columns=list(
                    list(
                        `name`="comparison", 
                        `title`="Comparison", 
                        `type`="text"),
                    list(
                        `name`="correlation", 
                        `title`="Correlation (r)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="p_value", 
                        `title`="P-value", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="icc", 
                        `title`="ICC", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="icc_ci_lower", 
                        `title`="ICC 95% CI Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="icc_ci_upper", 
                        `title`="ICC 95% CI Upper", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="interpretation", 
                        `title`="Agreement Level", 
                        `type`="text")),
                visible="(agreement_analysis)",
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "agreement_analysis",
                    "confidence_level")))
            self$add(jmvcore::Table$new(
                options=options,
                name="qualitycontroltable",
                title="Quality Control Metrics",
                columns=list(
                    list(
                        `name`="metric", 
                        `title`="QC Metric", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="threshold", 
                        `title`="Threshold", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="status", 
                        `title`="Status", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Recommendation", 
                        `type`="text")),
                visible="(quality_control)",
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "quality_control")))
            self$add(jmvcore::Image$new(
                options=options,
                name="distributionplot",
                title="Scoring Distribution Plot",
                width=600,
                height=400,
                visible="(show_plots)",
                requiresData=TRUE,
                renderFun=".distributionplot",
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "show_plots",
                    "scoring_method")))
            self$add(jmvcore::Image$new(
                options=options,
                name="correlationplot",
                title="Method Correlation Plot",
                width=500,
                height=400,
                visible="(show_plots)",
                requiresData=TRUE,
                renderFun=".correlationplot",
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "show_plots",
                    "agreement_analysis")))
            self$add(jmvcore::Image$new(
                options=options,
                name="agreementplot",
                title="Method Agreement Plot",
                width=500,
                height=400,
                visible="(show_agreement_plots)",
                requiresData=TRUE,
                renderFun=".agreementplot",
                clearWith=list(
                    "intensity_var",
                    "proportion_var",
                    "show_agreement_plots",
                    "agreement_analysis")))
            self$add(jmvcore::Image$new(
                options=options,
                name="cutpointplot",
                title="Cutpoint Optimization Plot",
                width=500,
                height=400,
                visible=FALSE,
                requiresData=TRUE,
                renderFun=".cutpointplot"))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    biomarkerresults = function() private$.items[["biomarkerresults"]],
                    clinicalcutpoints = function() private$.items[["clinicalcutpoints"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="biomarkerspecific",
                            title="Biomarker-Specific Analysis")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="biomarkerresults",
                            title="Biomarker-Specific Results",
                            columns=list(
                                list(
                                    `name`="parameter", 
                                    `title`="Parameter", 
                                    `type`="text"),
                                list(
                                    `name`="value", 
                                    `title`="Value", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="clinical_significance", 
                                    `title`="Clinical Significance", 
                                    `type`="text"),
                                list(
                                    `name`="reference_range", 
                                    `title`="Reference Range", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="clinicalcutpoints",
                            title="Clinical Cutpoint Analysis",
                            columns=list(
                                list(
                                    `name`="cutpoint_type", 
                                    `title`="Cutpoint Type", 
                                    `type`="text"),
                                list(
                                    `name`="threshold", 
                                    `title`="Threshold", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="sensitivity", 
                                    `title`="Sensitivity", 
                                    `type`="number", 
                                    `format`="pc"),
                                list(
                                    `name`="specificity", 
                                    `title`="Specificity", 
                                    `type`="number", 
                                    `format`="pc"),
                                list(
                                    `name`="clinical_context", 
                                    `title`="Clinical Context", 
                                    `type`="text"))))}))$new(options=options))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    algorithmcomparison = function() private$.items[["algorithmcomparison"]],
                    batcheffects = function() private$.items[["batcheffects"]],
                    validationplot = function() private$.items[["validationplot"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="digitalvalidation",
                            title="Digital Pathology Validation")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="algorithmcomparison",
                            title="Algorithm vs Manual Comparison",
                            columns=list(
                                list(
                                    `name`="metric", 
                                    `title`="Metric", 
                                    `type`="text"),
                                list(
                                    `name`="manual_mean", 
                                    `title`="Manual Mean", 
                                    `type`="number"),
                                list(
                                    `name`="automated_mean", 
                                    `title`="Automated Mean", 
                                    `type`="number"),
                                list(
                                    `name`="correlation", 
                                    `title`="Correlation", 
                                    `type`="number"),
                                list(
                                    `name`="bias", 
                                    `title`="Bias", 
                                    `type`="number"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="batcheffects",
                            title="Batch Effect Assessment",
                            columns=list(
                                list(
                                    `name`="batch_id", 
                                    `title`="Batch ID", 
                                    `type`="text"),
                                list(
                                    `name`="sample_count", 
                                    `title`="Sample Count", 
                                    `type`="integer"),
                                list(
                                    `name`="mean_hscore", 
                                    `title`="Mean H-Score", 
                                    `type`="number"),
                                list(
                                    `name`="batch_effect_size", 
                                    `title`="Effect Size", 
                                    `type`="number"),
                                list(
                                    `name`="p_value", 
                                    `title`="P-value", 
                                    `type`="number", 
                                    `format`="zto,pvalue"))))
                        self$add(jmvcore::Image$new(
                            options=options,
                            name="validationplot",
                            title="Digital Validation Plot",
                            width=500,
                            height=400,
                            renderFun=".validationplot"))}))$new(options=options))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    segmentationresults = function() private$.items[["segmentationresults"]],
                    intensityanalysis = function() private$.items[["intensityanalysis"]],
                    validationcomparison = function() private$.items[["validationcomparison"]],
                    segmentationplot = function() private$.items[["segmentationplot"]],
                    intensityplot = function() private$.items[["intensityplot"]],
                    validationplot = function() private$.items[["validationplot"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="automatedanalysis",
                            title="Automated Image Analysis Results")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="segmentationresults",
                            title="Nuclear Segmentation Results",
                            columns=list(
                                list(
                                    `name`="image_name", 
                                    `title`="Image Name", 
                                    `type`="text"),
                                list(
                                    `name`="total_nuclei", 
                                    `title`="Total Nuclei", 
                                    `type`="integer"),
                                list(
                                    `name`="positive_nuclei", 
                                    `title`="Positive Nuclei", 
                                    `type`="integer"),
                                list(
                                    `name`="percentage_positive", 
                                    `title`="Percentage Positive", 
                                    `type`="number", 
                                    `format`="pc"),
                                list(
                                    `name`="h_score", 
                                    `title`="Automated H-Score", 
                                    `type`="number"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="intensityanalysis",
                            title="DAB Intensity Analysis",
                            columns=list(
                                list(
                                    `name`="intensity_level", 
                                    `title`="Intensity Level", 
                                    `type`="text"),
                                list(
                                    `name`="nucleus_count", 
                                    `title`="Nucleus Count", 
                                    `type`="integer"),
                                list(
                                    `name`="percentage", 
                                    `title`="Percentage", 
                                    `type`="number", 
                                    `format`="pc"),
                                list(
                                    `name`="mean_optical_density", 
                                    `title`="Mean Optical Density", 
                                    `type`="number"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="validationcomparison",
                            title="Manual vs Automated Validation",
                            visible="(validation_metrics)",
                            columns=list(
                                list(
                                    `name`="metric", 
                                    `title`="Metric", 
                                    `type`="text"),
                                list(
                                    `name`="manual_score", 
                                    `title`="Manual Score", 
                                    `type`="number"),
                                list(
                                    `name`="automated_score", 
                                    `title`="Automated Score", 
                                    `type`="number"),
                                list(
                                    `name`="difference", 
                                    `title`="Difference", 
                                    `type`="number"),
                                list(
                                    `name`="correlation", 
                                    `title`="Correlation", 
                                    `type`="number"))))
                        self$add(jmvcore::Image$new(
                            options=options,
                            name="segmentationplot",
                            title="Nuclear Segmentation Visualization",
                            width=800,
                            height=600,
                            renderFun=".segmentationplot"))
                        self$add(jmvcore::Image$new(
                            options=options,
                            name="intensityplot",
                            title="DAB Intensity Distribution Plot",
                            width=600,
                            height=400,
                            renderFun=".intensityplot"))
                        self$add(jmvcore::Image$new(
                            options=options,
                            name="validationplot",
                            title="Manual vs Automated Correlation Plot",
                            visible="(validation_metrics)",
                            width=500,
                            height=400))}))$new(options=options))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    reliabilitymetrics = function() private$.items[["reliabilitymetrics"]],
                    distributionanalysis = function() private$.items[["distributionanalysis"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="advancedmetrics",
                            title="Advanced Statistical Metrics")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="reliabilitymetrics",
                            title="Reliability Assessment",
                            columns=list(
                                list(
                                    `name`="measure", 
                                    `title`="Reliability Measure", 
                                    `type`="text"),
                                list(
                                    `name`="value", 
                                    `title`="Value", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="ci_lower", 
                                    `title`="95% CI Lower", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="ci_upper", 
                                    `title`="95% CI Upper", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="interpretation", 
                                    `title`="Interpretation", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="distributionanalysis",
                            title="Distribution Analysis",
                            columns=list(
                                list(
                                    `name`="score_type", 
                                    `title`="Score Type", 
                                    `type`="text"),
                                list(
                                    `name`="normality_test", 
                                    `title`="Normality Test", 
                                    `type`="text"),
                                list(
                                    `name`="p_value", 
                                    `title`="P-value", 
                                    `type`="number", 
                                    `format`="zto,pvalue"),
                                list(
                                    `name`="skewness", 
                                    `title`="Skewness", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="kurtosis", 
                                    `title`="Kurtosis", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="distribution", 
                                    `title`="Distribution", 
                                    `type`="text"))))}))$new(options=options))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    cutoffresults = function() private$.items[["cutoffresults"]],
                    comparisonstats = function() private$.items[["comparisonstats"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="multiplecutoffs",
                            title="Multiple Cut-off Analysis")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="cutoffresults",
                            title="Cut-off Analysis Results",
                            columns=list(
                                list(
                                    `name`="cutoff_threshold", 
                                    `title`="Cut-off Threshold", 
                                    `type`="text"),
                                list(
                                    `name`="group_name", 
                                    `title`="Group", 
                                    `type`="text"),
                                list(
                                    `name`="positive_count", 
                                    `title`="Positive Cases", 
                                    `type`="integer"),
                                list(
                                    `name`="total_count", 
                                    `title`="Total Cases", 
                                    `type`="integer"),
                                list(
                                    `name`="positive_percentage", 
                                    `title`="Positive Rate (%)", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="confidence_interval", 
                                    `title`="95% CI", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="comparisonstats",
                            title="Statistical Comparisons Across Cut-offs",
                            visible="(cutoff_comparison)",
                            columns=list(
                                list(
                                    `name`="cutoff", 
                                    `title`="Cut-off", 
                                    `type`="text"),
                                list(
                                    `name`="test_statistic", 
                                    `title`="Test Statistic", 
                                    `type`="text"),
                                list(
                                    `name`="statistic_value", 
                                    `title`="Statistic Value", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="p_value", 
                                    `title`="P-value", 
                                    `type`="number", 
                                    `format`="zto,pvalue"),
                                list(
                                    `name`="interpretation", 
                                    `title`="Interpretation", 
                                    `type`="text"))))}))$new(options=options))
            self$add(jmvcore::Image$new(
                options=options,
                name="cutoffplot",
                title="Multiple Cut-off Analysis Plot",
                width=600,
                height=400,
                visible="(multiple_cutoffs && show_plots)",
                requiresData=TRUE,
                renderFun=".cutoffplot",
                clearWith=list(
                    "multiple_cutoffs",
                    "cutoff_values",
                    "show_plots",
                    "group_var")))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    cpsresults = function() private$.items[["cpsresults"]],
                    cpsstatistics = function() private$.items[["cpsstatistics"]],
                    cpscomparison = function() private$.items[["cpscomparison"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="cpsanalysis",
                            title="Combined Positive Score (CPS) Analysis")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="cpsresults",
                            title="CPS Analysis Results",
                            columns=list(
                                list(
                                    `name`="cps_cutoff", 
                                    `title`="CPS Cut-off", 
                                    `type`="text"),
                                list(
                                    `name`="positive_count", 
                                    `title`="Positive Cases", 
                                    `type`="integer"),
                                list(
                                    `name`="total_count", 
                                    `title`="Total Cases", 
                                    `type`="integer"),
                                list(
                                    `name`="positive_percentage", 
                                    `title`="Positive Rate (%)", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="confidence_interval", 
                                    `title`="95% CI", 
                                    `type`="text"),
                                list(
                                    `name`="clinical_significance", 
                                    `title`="Clinical Significance", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="cpsstatistics",
                            title="CPS Descriptive Statistics",
                            columns=list(
                                list(
                                    `name`="statistic", 
                                    `title`="Statistic", 
                                    `type`="text"),
                                list(
                                    `name`="value", 
                                    `title`="Value", 
                                    `type`="text"),
                                list(
                                    `name`="interpretation", 
                                    `title`="Interpretation", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="cpscomparison",
                            title="CPS Group Comparison",
                            visible="(group_var && cutoff_comparison)",
                            columns=list(
                                list(
                                    `name`="comparison_type", 
                                    `title`="Comparison Type", 
                                    `type`="text"),
                                list(
                                    `name`="test_statistic", 
                                    `title`="Test Statistic", 
                                    `type`="text"),
                                list(
                                    `name`="statistic_value", 
                                    `title`="Statistic Value", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="p_value", 
                                    `title`="P-value", 
                                    `type`="number", 
                                    `format`="zto,pvalue"),
                                list(
                                    `name`="interpretation", 
                                    `title`="Interpretation", 
                                    `type`="text"))))}))$new(options=options))
            self$add(jmvcore::Image$new(
                options=options,
                name="cpsplot",
                title="Combined Positive Score Distribution Plot",
                width=600,
                height=400,
                visible="(cps_analysis && show_plots)",
                requiresData=TRUE,
                renderFun=".cpsplot",
                clearWith=list(
                    "cps_analysis",
                    "tumor_cells_var",
                    "immune_cells_var",
                    "show_plots",
                    "group_var")))
            self$add(R6::R6Class(
                inherit = jmvcore::Group,
                active = list(
                    classificationtable = function() private$.items[["classificationtable"]],
                    subtypedistribution = function() private$.items[["subtypedistribution"]],
                    subtypestatistics = function() private$.items[["subtypestatistics"]],
                    checkpointanalysis = function() private$.items[["checkpointanalysis"]]),
                private = list(),
                public=list(
                    initialize=function(options) {
                        super$initialize(
                            options=options,
                            name="molecularclassification",
                            title="Molecular Subtyping Analysis")
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="classificationtable",
                            title="Molecular Classification Results",
                            columns=list(
                                list(
                                    `name`="sample_id", 
                                    `title`="Sample ID", 
                                    `type`="text"),
                                list(
                                    `name`="marker1_status", 
                                    `title`="Marker 1 Status", 
                                    `type`="text"),
                                list(
                                    `name`="marker2_status", 
                                    `title`="Marker 2 Status", 
                                    `type`="text"),
                                list(
                                    `name`="secondary_status", 
                                    `title`="Secondary Marker", 
                                    `type`="text"),
                                list(
                                    `name`="molecular_subtype", 
                                    `title`="Molecular Subtype", 
                                    `type`="text"),
                                list(
                                    `name`="confidence_score", 
                                    `title`="Confidence Score", 
                                    `type`="number", 
                                    `format`="zto"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="subtypedistribution",
                            title="Molecular Subtype Distribution",
                            columns=list(
                                list(
                                    `name`="subtype", 
                                    `title`="Molecular Subtype", 
                                    `type`="text"),
                                list(
                                    `name`="count", 
                                    `title`="Count", 
                                    `type`="integer"),
                                list(
                                    `name`="percentage", 
                                    `title`="Percentage (%)", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="confidence_interval", 
                                    `title`="95% CI", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="subtypestatistics",
                            title="Subtype Statistical Comparison",
                            visible="(subtype_statistics)",
                            columns=list(
                                list(
                                    `name`="comparison", 
                                    `title`="Comparison", 
                                    `type`="text"),
                                list(
                                    `name`="test_statistic", 
                                    `title`="Test Statistic", 
                                    `type`="text"),
                                list(
                                    `name`="statistic_value", 
                                    `title`="Statistic Value", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="p_value", 
                                    `title`="P-value", 
                                    `type`="number", 
                                    `format`="zto,pvalue"),
                                list(
                                    `name`="effect_size", 
                                    `title`="Effect Size", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="interpretation", 
                                    `title`="Interpretation", 
                                    `type`="text"))))
                        self$add(jmvcore::Table$new(
                            options=options,
                            name="checkpointanalysis",
                            title="Checkpoint Inhibitor Expression by Subtype",
                            visible="(pd1_marker || pdl1_marker)",
                            columns=list(
                                list(
                                    `name`="molecular_subtype", 
                                    `title`="Molecular Subtype", 
                                    `type`="text"),
                                list(
                                    `name`="marker", 
                                    `title`="Marker", 
                                    `type`="text"),
                                list(
                                    `name`="cutoff", 
                                    `title`="Cut-off (%)", 
                                    `type`="text"),
                                list(
                                    `name`="positive_count", 
                                    `title`="Positive Cases", 
                                    `type`="integer"),
                                list(
                                    `name`="total_count", 
                                    `title`="Total Cases", 
                                    `type`="integer"),
                                list(
                                    `name`="positive_rate", 
                                    `title`="Positive Rate (%)", 
                                    `type`="number", 
                                    `format`="zto"),
                                list(
                                    `name`="p_value", 
                                    `title`="P-value", 
                                    `type`="number", 
                                    `format`="zto,pvalue"))))}))$new(options=options))
            self$add(jmvcore::Image$new(
                options=options,
                name="subtypeplot",
                title="Molecular Subtype Distribution Plot",
                width=600,
                height=400,
                visible="(molecular_classification && subtype_visualization)",
                requiresData=TRUE,
                renderFun=".subtypeplot",
                clearWith=list(
                    "molecular_classification",
                    "classification_system",
                    "subtype_visualization")))
            self$add(jmvcore::Image$new(
                options=options,
                name="checkpointplot",
                title="Checkpoint Inhibitor Expression Plot",
                width=700,
                height=500,
                visible="(molecular_classification && (pd1_marker || pdl1_marker) && subtype_visualization)",
                requiresData=TRUE,
                renderFun=".checkpointplot",
                clearWith=list(
                    "molecular_classification",
                    "pd1_marker",
                    "pdl1_marker",
                    "checkpoint_cutoffs",
                    "subtype_visualization")))
            self$add(jmvcore::Table$new(
                options=options,
                name="exportdata",
                title="Export-Ready Data",
                visible="(export_results)",
                columns=list(
                    list(
                        `name`="sample_id", 
                        `title`="Sample ID", 
                        `type`="text"),
                    list(
                        `name`="intensity", 
                        `title`="Intensity", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="proportion", 
                        `title`="Proportion (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="hscore", 
                        `title`="H-score", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="allred_total", 
                        `title`="Allred Total", 
                        `type`="integer"),
                    list(
                        `name`="binary_result", 
                        `title`="Binary Result", 
                        `type`="text"),
                    list(
                        `name`="molecular_subtype", 
                        `title`="Molecular Subtype", 
                        `type`="text"),
                    list(
                        `name`="group", 
                        `title`="Group", 
                        `type`="text")),
                clearWith=list(
                    "export_results",
                    "intensity_var",
                    "proportion_var",
                    "molecular_classification")))}))

ihcscoringBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ihcscoringBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "ihcscoring",
                version = c(1,0,0),
                options = options,
                results = ihcscoringResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' IHC Scoring Standardization
#'
#' 
#' @param data the data as a data frame
#' @param intensity_var staining intensity scores (typically 0-3 scale)
#' @param proportion_var percentage of positive cells (0-100\%)
#' @param sample_id_var unique identifier for each sample
#' @param group_var grouping variable for comparative analysis
#' @param scoring_method primary scoring methodology to emphasize
#' @param binary_cutpoint H-score cutpoint for positive/negative
#'   classification
#' @param allred_cutpoint Allred score cutpoint for positive/negative
#'   classification
#' @param intensity_scale intensity scoring scale used
#' @param biomarker_type specific biomarker being analyzed
#' @param show_plots display scoring distribution and correlation plots
#' @param show_agreement_plots display method agreement analysis plots
#' @param include_statistics calculate comprehensive descriptive statistics
#' @param include_digital_validation include digital pathology validation
#'   metrics
#' @param agreement_analysis calculate inter-method agreement statistics
#' @param quality_control perform quality control checks and outlier detection
#' @param clinical_interpretation provide clinical context and interpretation
#' @param export_results format results for external analysis or reporting
#' @param multiple_cutoffs analyze biomarker positivity across multiple
#'   pre-specified cut-off values
#' @param cutoff_values comma-separated list of percentage cut-off values for
#'   positivity analysis
#' @param cps_analysis calculate Combined Positive Score for PD-L1 assessment
#' @param immune_cells_var percentage of PD-L1+ immune cells (required for CPS
#'   calculation)
#' @param tumor_cells_var percentage of PD-L1+ tumor cells (required for CPS
#'   calculation)
#' @param cutoff_comparison perform statistical comparisons across different
#'   cut-off values
#' @param confidence_level confidence level for statistical intervals
#' @param bootstrap_n number of bootstrap replicates for confidence intervals
#' @param automated_analysis enable automated quantification from histological
#'   images
#' @param segmentation_method method for nuclear segmentation in automated
#'   analysis
#' @param color_deconvolution separate DAB and hematoxylin channels for better
#'   quantification
#' @param dab_thresholds optical density thresholds for weak, moderate, and
#'   strong DAB staining (comma-separated)
#' @param minimum_nuclear_area minimum area threshold for nuclear detection
#' @param maximum_nuclear_area maximum area threshold for nuclear detection
#' @param batch_processing process multiple image files in batch mode
#' @param image_format format of input histological images
#' @param validation_metrics compare automated results with manual scoring
#'   when available
#' @param molecular_classification perform molecular subtype classification
#'   based on marker combinations
#' @param classification_system molecular classification system to apply
#' @param primary_marker1 first primary marker for molecular classification
#' @param primary_marker2 second primary marker for molecular classification
#' @param secondary_marker secondary marker for subtype refinement
#' @param classification_cutoffs comma-separated cut-off values for each
#'   marker in order (primary1, primary2, secondary)
#' @param pd1_marker PD-1 expression scores for immune checkpoint analysis
#' @param pdl1_marker PD-L1 expression scores for immune checkpoint analysis
#' @param checkpoint_cutoffs comma-separated cut-off values for PD-1/PD-L1
#'   positivity analysis
#' @param subtype_statistics calculate statistical comparisons between
#'   molecular subtypes
#' @param subtype_visualization create plots showing molecular subtype
#'   distributions and associations
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$interpretation} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$scorestable} \tab \tab \tab \tab \tab Calculated H-scores, Allred scores, and binary classifications \cr
#'   \code{results$statisticstable} \tab \tab \tab \tab \tab Comprehensive statistical summary for each scoring method \cr
#'   \code{results$agreementtable} \tab \tab \tab \tab \tab Correlation and agreement statistics between scoring methods \cr
#'   \code{results$qualitycontroltable} \tab \tab \tab \tab \tab Outlier detection and data quality assessment \cr
#'   \code{results$distributionplot} \tab \tab \tab \tab \tab Visual distribution of H-scores and Allred scores \cr
#'   \code{results$correlationplot} \tab \tab \tab \tab \tab Correlation between H-score and Allred score methods \cr
#'   \code{results$agreementplot} \tab \tab \tab \tab \tab Bland-Altman style agreement analysis between methods \cr
#'   \code{results$cutpointplot} \tab \tab \tab \tab \tab ROC analysis for optimal cutpoint determination \cr
#'   \code{results$biomarkerspecific$biomarkerresults} \tab \tab \tab \tab \tab Analysis tailored to specific biomarker characteristics \cr
#'   \code{results$biomarkerspecific$clinicalcutpoints} \tab \tab \tab \tab \tab Established clinical thresholds and their performance \cr
#'   \code{results$digitalvalidation$algorithmcomparison} \tab \tab \tab \tab \tab Statistical comparison of digital vs manual scoring \cr
#'   \code{results$digitalvalidation$batcheffects} \tab \tab \tab \tab \tab Analysis of systematic differences across batches \cr
#'   \code{results$digitalvalidation$validationplot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$automatedanalysis$segmentationresults} \tab \tab \tab \tab \tab Summary of automated nuclear segmentation \cr
#'   \code{results$automatedanalysis$intensityanalysis} \tab \tab \tab \tab \tab Distribution of DAB intensity levels \cr
#'   \code{results$automatedanalysis$validationcomparison} \tab \tab \tab \tab \tab Comparison between manual and automated scores \cr
#'   \code{results$automatedanalysis$segmentationplot} \tab \tab \tab \tab \tab Overlay of detected nuclei on original image \cr
#'   \code{results$automatedanalysis$intensityplot} \tab \tab \tab \tab \tab Histogram of DAB optical density values \cr
#'   \code{results$automatedanalysis$validationplot} \tab \tab \tab \tab \tab Scatter plot of manual vs automated scores \cr
#'   \code{results$advancedmetrics$reliabilitymetrics} \tab \tab \tab \tab \tab Comprehensive reliability and consistency metrics \cr
#'   \code{results$advancedmetrics$distributionanalysis} \tab \tab \tab \tab \tab Normality testing and distribution characteristics \cr
#'   \code{results$multiplecutoffs$cutoffresults} \tab \tab \tab \tab \tab Positivity rates across different cut-off thresholds \cr
#'   \code{results$multiplecutoffs$comparisonstats} \tab \tab \tab \tab \tab Statistical tests comparing groups across different cut-off values \cr
#'   \code{results$cutoffplot} \tab \tab \tab \tab \tab Biomarker positivity rates across different thresholds \cr
#'   \code{results$cpsanalysis$cpsresults} \tab \tab \tab \tab \tab Combined Positive Score analysis for PD-L1 assessment \cr
#'   \code{results$cpsanalysis$cpsstatistics} \tab \tab \tab \tab \tab Summary statistics for Combined Positive Score \cr
#'   \code{results$cpsanalysis$cpscomparison} \tab \tab \tab \tab \tab Statistical comparison of CPS between groups \cr
#'   \code{results$cpsplot} \tab \tab \tab \tab \tab Distribution of CPS scores with clinical thresholds \cr
#'   \code{results$molecularclassification$classificationtable} \tab \tab \tab \tab \tab Molecular subtype classification based on marker combinations \cr
#'   \code{results$molecularclassification$subtypedistribution} \tab \tab \tab \tab \tab Frequency and percentage of each molecular subtype \cr
#'   \code{results$molecularclassification$subtypestatistics} \tab \tab \tab \tab \tab Statistical comparisons between molecular subtypes \cr
#'   \code{results$molecularclassification$checkpointanalysis} \tab \tab \tab \tab \tab PD-1/PD-L1 expression patterns across molecular subtypes \cr
#'   \code{results$subtypeplot} \tab \tab \tab \tab \tab Visual representation of molecular subtype frequencies \cr
#'   \code{results$checkpointplot} \tab \tab \tab \tab \tab PD-1/PD-L1 expression patterns across molecular subtypes \cr
#'   \code{results$exportdata} \tab \tab \tab \tab \tab Formatted results for external analysis or reporting \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$scorestable$asDF}
#'
#' \code{as.data.frame(results$scorestable)}
#'
#' @export
ihcscoring <- function(
    data,
    intensity_var,
    proportion_var,
    sample_id_var,
    group_var,
    scoring_method = "both",
    binary_cutpoint = 100,
    allred_cutpoint = 3,
    intensity_scale = "standard",
    biomarker_type = "other",
    show_plots = TRUE,
    show_agreement_plots = TRUE,
    include_statistics = TRUE,
    include_digital_validation = FALSE,
    agreement_analysis = TRUE,
    quality_control = TRUE,
    clinical_interpretation = TRUE,
    export_results = FALSE,
    multiple_cutoffs = FALSE,
    cutoff_values = "1, 5, 10, 25, 50",
    cps_analysis = FALSE,
    immune_cells_var,
    tumor_cells_var,
    cutoff_comparison = TRUE,
    confidence_level = 0.95,
    bootstrap_n = 1000,
    automated_analysis = FALSE,
    segmentation_method = "manual",
    color_deconvolution = TRUE,
    dab_thresholds = "0.1, 0.3, 0.6",
    minimum_nuclear_area = 50,
    maximum_nuclear_area = 2000,
    batch_processing = FALSE,
    image_format = "tiff",
    validation_metrics = TRUE,
    molecular_classification = FALSE,
    classification_system = "bladder_mibc",
    primary_marker1,
    primary_marker2,
    secondary_marker,
    classification_cutoffs = "20, 20, 70",
    pd1_marker,
    pdl1_marker,
    checkpoint_cutoffs = "1, 10",
    subtype_statistics = TRUE,
    subtype_visualization = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("ihcscoring requires jmvcore to be installed (restart may be required)")

    if ( ! missing(intensity_var)) intensity_var <- jmvcore::resolveQuo(jmvcore::enquo(intensity_var))
    if ( ! missing(proportion_var)) proportion_var <- jmvcore::resolveQuo(jmvcore::enquo(proportion_var))
    if ( ! missing(sample_id_var)) sample_id_var <- jmvcore::resolveQuo(jmvcore::enquo(sample_id_var))
    if ( ! missing(group_var)) group_var <- jmvcore::resolveQuo(jmvcore::enquo(group_var))
    if ( ! missing(immune_cells_var)) immune_cells_var <- jmvcore::resolveQuo(jmvcore::enquo(immune_cells_var))
    if ( ! missing(tumor_cells_var)) tumor_cells_var <- jmvcore::resolveQuo(jmvcore::enquo(tumor_cells_var))
    if ( ! missing(primary_marker1)) primary_marker1 <- jmvcore::resolveQuo(jmvcore::enquo(primary_marker1))
    if ( ! missing(primary_marker2)) primary_marker2 <- jmvcore::resolveQuo(jmvcore::enquo(primary_marker2))
    if ( ! missing(secondary_marker)) secondary_marker <- jmvcore::resolveQuo(jmvcore::enquo(secondary_marker))
    if ( ! missing(pd1_marker)) pd1_marker <- jmvcore::resolveQuo(jmvcore::enquo(pd1_marker))
    if ( ! missing(pdl1_marker)) pdl1_marker <- jmvcore::resolveQuo(jmvcore::enquo(pdl1_marker))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(intensity_var), intensity_var, NULL),
            `if`( ! missing(proportion_var), proportion_var, NULL),
            `if`( ! missing(sample_id_var), sample_id_var, NULL),
            `if`( ! missing(group_var), group_var, NULL),
            `if`( ! missing(immune_cells_var), immune_cells_var, NULL),
            `if`( ! missing(tumor_cells_var), tumor_cells_var, NULL),
            `if`( ! missing(primary_marker1), primary_marker1, NULL),
            `if`( ! missing(primary_marker2), primary_marker2, NULL),
            `if`( ! missing(secondary_marker), secondary_marker, NULL),
            `if`( ! missing(pd1_marker), pd1_marker, NULL),
            `if`( ! missing(pdl1_marker), pdl1_marker, NULL))

    for (v in group_var) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- ihcscoringOptions$new(
        intensity_var = intensity_var,
        proportion_var = proportion_var,
        sample_id_var = sample_id_var,
        group_var = group_var,
        scoring_method = scoring_method,
        binary_cutpoint = binary_cutpoint,
        allred_cutpoint = allred_cutpoint,
        intensity_scale = intensity_scale,
        biomarker_type = biomarker_type,
        show_plots = show_plots,
        show_agreement_plots = show_agreement_plots,
        include_statistics = include_statistics,
        include_digital_validation = include_digital_validation,
        agreement_analysis = agreement_analysis,
        quality_control = quality_control,
        clinical_interpretation = clinical_interpretation,
        export_results = export_results,
        multiple_cutoffs = multiple_cutoffs,
        cutoff_values = cutoff_values,
        cps_analysis = cps_analysis,
        immune_cells_var = immune_cells_var,
        tumor_cells_var = tumor_cells_var,
        cutoff_comparison = cutoff_comparison,
        confidence_level = confidence_level,
        bootstrap_n = bootstrap_n,
        automated_analysis = automated_analysis,
        segmentation_method = segmentation_method,
        color_deconvolution = color_deconvolution,
        dab_thresholds = dab_thresholds,
        minimum_nuclear_area = minimum_nuclear_area,
        maximum_nuclear_area = maximum_nuclear_area,
        batch_processing = batch_processing,
        image_format = image_format,
        validation_metrics = validation_metrics,
        molecular_classification = molecular_classification,
        classification_system = classification_system,
        primary_marker1 = primary_marker1,
        primary_marker2 = primary_marker2,
        secondary_marker = secondary_marker,
        classification_cutoffs = classification_cutoffs,
        pd1_marker = pd1_marker,
        pdl1_marker = pdl1_marker,
        checkpoint_cutoffs = checkpoint_cutoffs,
        subtype_statistics = subtype_statistics,
        subtype_visualization = subtype_visualization)

    analysis <- ihcscoringClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

