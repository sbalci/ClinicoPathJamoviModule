
# This file is automatically generated, you probably don't want to edit this

pagetrendtestOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "pagetrendtestOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dependent = NULL,
            subject = NULL,
            within = NULL,
            trend_direction = "increasing",
            custom_order = NULL,
            method = "asymptotic",
            alpha = 0.05,
            effect_size = TRUE,
            confidence_level = 0.95,
            show_ranks = TRUE,
            show_descriptives = TRUE,
            friedman_comparison = TRUE,
            show_assumptions = TRUE,
            clinical_interpretation = TRUE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="pagetrendtest",
                requiresData=TRUE,
                ...)

            private$..dependent <- jmvcore::OptionVariable$new(
                "dependent",
                dependent,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..subject <- jmvcore::OptionVariable$new(
                "subject",
                subject,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..within <- jmvcore::OptionVariable$new(
                "within",
                within,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..trend_direction <- jmvcore::OptionList$new(
                "trend_direction",
                trend_direction,
                options=list(
                    "increasing",
                    "decreasing",
                    "custom"),
                default="increasing")
            private$..custom_order <- jmvcore::OptionString$new(
                "custom_order",
                custom_order)
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "asymptotic",
                    "exact",
                    "permutation"),
                default="asymptotic")
            private$..alpha <- jmvcore::OptionNumber$new(
                "alpha",
                alpha,
                min=0.01,
                max=0.1,
                default=0.05)
            private$..effect_size <- jmvcore::OptionBool$new(
                "effect_size",
                effect_size,
                default=TRUE)
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.8,
                max=0.99,
                default=0.95)
            private$..show_ranks <- jmvcore::OptionBool$new(
                "show_ranks",
                show_ranks,
                default=TRUE)
            private$..show_descriptives <- jmvcore::OptionBool$new(
                "show_descriptives",
                show_descriptives,
                default=TRUE)
            private$..friedman_comparison <- jmvcore::OptionBool$new(
                "friedman_comparison",
                friedman_comparison,
                default=TRUE)
            private$..show_assumptions <- jmvcore::OptionBool$new(
                "show_assumptions",
                show_assumptions,
                default=TRUE)
            private$..clinical_interpretation <- jmvcore::OptionBool$new(
                "clinical_interpretation",
                clinical_interpretation,
                default=TRUE)

            self$.addOption(private$..dependent)
            self$.addOption(private$..subject)
            self$.addOption(private$..within)
            self$.addOption(private$..trend_direction)
            self$.addOption(private$..custom_order)
            self$.addOption(private$..method)
            self$.addOption(private$..alpha)
            self$.addOption(private$..effect_size)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..show_ranks)
            self$.addOption(private$..show_descriptives)
            self$.addOption(private$..friedman_comparison)
            self$.addOption(private$..show_assumptions)
            self$.addOption(private$..clinical_interpretation)
        }),
    active = list(
        dependent = function() private$..dependent$value,
        subject = function() private$..subject$value,
        within = function() private$..within$value,
        trend_direction = function() private$..trend_direction$value,
        custom_order = function() private$..custom_order$value,
        method = function() private$..method$value,
        alpha = function() private$..alpha$value,
        effect_size = function() private$..effect_size$value,
        confidence_level = function() private$..confidence_level$value,
        show_ranks = function() private$..show_ranks$value,
        show_descriptives = function() private$..show_descriptives$value,
        friedman_comparison = function() private$..friedman_comparison$value,
        show_assumptions = function() private$..show_assumptions$value,
        clinical_interpretation = function() private$..clinical_interpretation$value),
    private = list(
        ..dependent = NA,
        ..subject = NA,
        ..within = NA,
        ..trend_direction = NA,
        ..custom_order = NA,
        ..method = NA,
        ..alpha = NA,
        ..effect_size = NA,
        ..confidence_level = NA,
        ..show_ranks = NA,
        ..show_descriptives = NA,
        ..friedman_comparison = NA,
        ..show_assumptions = NA,
        ..clinical_interpretation = NA)
)

pagetrendtestResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "pagetrendtestResults",
    inherit = jmvcore::Group,
    active = list(
        instructions = function() private$.items[["instructions"]],
        dataInfo = function() private$.items[["dataInfo"]],
        trendOrdering = function() private$.items[["trendOrdering"]],
        descriptiveStats = function() private$.items[["descriptiveStats"]],
        pageTrendTest = function() private$.items[["pageTrendTest"]],
        effectSize = function() private$.items[["effectSize"]],
        rankAnalysis = function() private$.items[["rankAnalysis"]],
        friedmanComparison = function() private$.items[["friedmanComparison"]],
        assumptionAssessment = function() private$.items[["assumptionAssessment"]],
        clinicalInterpretation = function() private$.items[["clinicalInterpretation"]],
        trendPlot = function() private$.items[["trendPlot"]],
        rankTrendPlot = function() private$.items[["rankTrendPlot"]],
        comparisonPlot = function() private$.items[["comparisonPlot"]],
        profilePlot = function() private$.items[["profilePlot"]],
        methodExplanation = function() private$.items[["methodExplanation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Page's Trend Test for Ordered Alternatives",
                refs=list(
                    "ClinicoPathJamoviModule"))
            self$add(jmvcore::Html$new(
                options=options,
                name="instructions",
                title="Analysis Instructions",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="dataInfo",
                title="Data Summary",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "dependent",
                    "subject",
                    "within"),
                columns=list(
                    list(
                        `name`="characteristic", 
                        `title`="Characteristic", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="trendOrdering",
                title="Trend Ordering",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "within",
                    "trend_direction",
                    "custom_order"),
                columns=list(
                    list(
                        `name`="position", 
                        `title`="Position", 
                        `type`="integer"),
                    list(
                        `name`="condition", 
                        `title`="Condition", 
                        `type`="text"),
                    list(
                        `name`="expected_rank", 
                        `title`="Expected Rank", 
                        `type`="number", 
                        `format`="zto:1"),
                    list(
                        `name`="trend_weight", 
                        `title`="Trend Weight", 
                        `type`="number", 
                        `format`="zto:2"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="descriptiveStats",
                title="Descriptive Statistics by Condition",
                visible="(show_descriptives)",
                rows=1,
                clearWith=list(
                    "show_descriptives",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="condition", 
                        `title`="Condition", 
                        `type`="text"),
                    list(
                        `name`="n", 
                        `title`="N", 
                        `type`="integer"),
                    list(
                        `name`="median", 
                        `title`="Median", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="mean", 
                        `title`="Mean", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="sd", 
                        `title`="SD", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="iqr", 
                        `title`="IQR", 
                        `type`="number", 
                        `format`="zto:3"),
                    list(
                        `name`="mean_rank", 
                        `title`="Mean Rank", 
                        `type`="number", 
                        `format`="zto:2"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="pageTrendTest",
                title="Page's Trend Test Results",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "dependent",
                    "subject",
                    "within",
                    "trend_direction",
                    "method"),
                columns=list(
                    list(
                        `name`="statistic", 
                        `title`="L Statistic", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="expected_value", 
                        `title`="Expected L", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="variance", 
                        `title`="Variance", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="standardized", 
                        `title`="Standardized L", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="significance", 
                        `title`="Significance", 
                        `type`="text"),
                    list(
                        `name`="conclusion", 
                        `title`="Conclusion", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="effectSize",
                title="Effect Size and Trend Strength",
                visible="(effect_size)",
                rows=1,
                clearWith=list(
                    "effect_size",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="measure", 
                        `title`="Effect Size", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="confidence_interval", 
                        `title`="Confidence Interval", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="rankAnalysis",
                title="Rank Analysis and Trend Components",
                visible="(show_ranks)",
                rows=1,
                clearWith=list(
                    "show_ranks",
                    "dependent",
                    "within",
                    "trend_direction"),
                columns=list(
                    list(
                        `name`="condition", 
                        `title`="Condition", 
                        `type`="text"),
                    list(
                        `name`="mean_rank", 
                        `title`="Observed Mean Rank", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="expected_rank", 
                        `title`="Expected Rank", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="rank_contribution", 
                        `title`="Rank Contribution to L", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="trend_alignment", 
                        `title`="Trend Alignment", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="friedmanComparison",
                title="Comparison with Friedman Test",
                visible="(friedman_comparison)",
                rows=1,
                clearWith=list(
                    "friedman_comparison",
                    "dependent",
                    "within"),
                columns=list(
                    list(
                        `name`="test", 
                        `title`="Test", 
                        `type`="text"),
                    list(
                        `name`="statistic", 
                        `title`="Test Statistic", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="df", 
                        `title`="df", 
                        `type`="integer"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="power_advantage", 
                        `title`="Power Advantage", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="assumptionAssessment",
                title="Assumption Assessment",
                visible="(show_assumptions)",
                rows=1,
                clearWith=list(
                    "show_assumptions",
                    "dependent",
                    "subject",
                    "within"),
                columns=list(
                    list(
                        `name`="assumption", 
                        `title`="Assumption", 
                        `type`="text"),
                    list(
                        `name`="status", 
                        `title`="Status", 
                        `type`="text"),
                    list(
                        `name`="details", 
                        `title`="Details", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Recommendation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="clinicalInterpretation",
                title="Clinical Interpretation Guide",
                visible="(clinical_interpretation)",
                rows=1,
                clearWith=list(
                    "clinical_interpretation",
                    "dependent",
                    "within",
                    "trend_direction"),
                columns=list(
                    list(
                        `name`="aspect", 
                        `title`="Clinical Aspect", 
                        `type`="text"),
                    list(
                        `name`="finding", 
                        `title`="Statistical Finding", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Clinical Interpretation", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Clinical Recommendation", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="trendPlot",
                title="Trend Visualization",
                visible=TRUE,
                requiresData=TRUE,
                width=800,
                height=600,
                renderFun=".plotTrend",
                clearWith=list(
                    "dependent",
                    "within",
                    "trend_direction")))
            self$add(jmvcore::Image$new(
                options=options,
                name="rankTrendPlot",
                title="Rank Trend Analysis",
                visible="(show_ranks)",
                requiresData=TRUE,
                width=700,
                height=500,
                renderFun=".plotRankTrend",
                clearWith=list(
                    "show_ranks",
                    "dependent",
                    "within",
                    "trend_direction")))
            self$add(jmvcore::Image$new(
                options=options,
                name="comparisonPlot",
                title="Page vs Friedman Comparison",
                visible="(friedman_comparison)",
                requiresData=TRUE,
                width=600,
                height=400,
                renderFun=".plotComparison",
                clearWith=list(
                    "friedman_comparison",
                    "dependent",
                    "within")))
            self$add(jmvcore::Image$new(
                options=options,
                name="profilePlot",
                title="Individual Profile Plot with Trend",
                visible=TRUE,
                requiresData=TRUE,
                width=800,
                height=600,
                renderFun=".plotProfilesWithTrend",
                clearWith=list(
                    "dependent",
                    "subject",
                    "within",
                    "trend_direction")))
            self$add(jmvcore::Html$new(
                options=options,
                name="methodExplanation",
                title="Method and Clinical Context",
                visible=TRUE))}))

pagetrendtestBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "pagetrendtestBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "pagetrendtest",
                version = c(1,0,0),
                options = options,
                results = pagetrendtestResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Page's Trend Test for Ordered Alternatives
#'
#' Page's trend test for detecting ordered alternatives in repeated measures 
#' data. This test is used when there is a specific predicted ordering of the 
#' treatment  effects or time points, making it more powerful than the general 
#' Friedman test when the alternative hypothesis specifies a trend or ordered 
#' pattern.
#' 
#'
#' @examples
#' data \%>\%
#' pagetrendtest(
#'     dependent = "measurement_value",
#'     subject = "patient_id",
#'     within = "time_point",
#'     trend_direction = "increasing"
#' )
#'
#' @param data the data as a data frame
#' @param dependent Continuous or ordinal dependent variable measured
#'   repeatedly
#' @param subject Variable identifying subjects/cases for repeated
#'   measurements
#' @param within Factor variable indicating the repeated measure
#'   conditions/time points (must be orderable)
#' @param trend_direction Expected direction of trend across conditions
#' @param custom_order Custom ordering of factor levels when trend_direction
#'   is 'custom' (e.g., 'baseline,week1,month1,month6')
#' @param method Method for calculating p-values
#' @param alpha Alpha level for hypothesis testing
#' @param effect_size Include effect size measures (Page's L coefficient and
#'   trend strength)
#' @param confidence_level Confidence level for confidence intervals
#' @param show_ranks Show detailed rank analysis and trend components
#' @param show_descriptives Show descriptive statistics for each condition
#' @param friedman_comparison Include comparison with standard Friedman test
#' @param show_assumptions Assess assumptions and provide recommendations
#' @param clinical_interpretation Provide clinical interpretation guidance for
#'   results
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$instructions} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$dataInfo} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$trendOrdering} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$descriptiveStats} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$pageTrendTest} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$effectSize} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$rankAnalysis} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$friedmanComparison} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$assumptionAssessment} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$clinicalInterpretation} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$trendPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$rankTrendPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$comparisonPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$profilePlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$methodExplanation} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$dataInfo$asDF}
#'
#' \code{as.data.frame(results$dataInfo)}
#'
#' @export
pagetrendtest <- function(
    data,
    dependent,
    subject,
    within,
    trend_direction = "increasing",
    custom_order,
    method = "asymptotic",
    alpha = 0.05,
    effect_size = TRUE,
    confidence_level = 0.95,
    show_ranks = TRUE,
    show_descriptives = TRUE,
    friedman_comparison = TRUE,
    show_assumptions = TRUE,
    clinical_interpretation = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("pagetrendtest requires jmvcore to be installed (restart may be required)")

    if ( ! missing(dependent)) dependent <- jmvcore::resolveQuo(jmvcore::enquo(dependent))
    if ( ! missing(subject)) subject <- jmvcore::resolveQuo(jmvcore::enquo(subject))
    if ( ! missing(within)) within <- jmvcore::resolveQuo(jmvcore::enquo(within))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dependent), dependent, NULL),
            `if`( ! missing(subject), subject, NULL),
            `if`( ! missing(within), within, NULL))

    for (v in within) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- pagetrendtestOptions$new(
        dependent = dependent,
        subject = subject,
        within = within,
        trend_direction = trend_direction,
        custom_order = custom_order,
        method = method,
        alpha = alpha,
        effect_size = effect_size,
        confidence_level = confidence_level,
        show_ranks = show_ranks,
        show_descriptives = show_descriptives,
        friedman_comparison = friedman_comparison,
        show_assumptions = show_assumptions,
        clinical_interpretation = clinical_interpretation)

    analysis <- pagetrendtestClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

