
# This file is automatically generated, you probably don't want to edit this

agepyramid2Options <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "agepyramid2Options",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            age = NULL,
            gender = NULL,
            female = NULL,
            bin_width = 5,
            plot_title = "Age Pyramid",
            color1 = "#1F77B4",
            color2 = "#FF7F0E", ...) {

            super$initialize(
                package="ClinicoPath",
                name="agepyramid2",
                requiresData=TRUE,
                ...)

            private$..age <- jmvcore::OptionVariable$new(
                "age",
                age,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..gender <- jmvcore::OptionVariable$new(
                "gender",
                gender,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..female <- jmvcore::OptionLevel$new(
                "female",
                female,
                variable="(gender)")
            private$..bin_width <- jmvcore::OptionNumber$new(
                "bin_width",
                bin_width,
                default=5)
            private$..plot_title <- jmvcore::OptionString$new(
                "plot_title",
                plot_title,
                default="Age Pyramid")
            private$..color1 <- jmvcore::OptionString$new(
                "color1",
                color1,
                default="#1F77B4")
            private$..color2 <- jmvcore::OptionString$new(
                "color2",
                color2,
                default="#FF7F0E")

            self$.addOption(private$..age)
            self$.addOption(private$..gender)
            self$.addOption(private$..female)
            self$.addOption(private$..bin_width)
            self$.addOption(private$..plot_title)
            self$.addOption(private$..color1)
            self$.addOption(private$..color2)
        }),
    active = list(
        age = function() private$..age$value,
        gender = function() private$..gender$value,
        female = function() private$..female$value,
        bin_width = function() private$..bin_width$value,
        plot_title = function() private$..plot_title$value,
        color1 = function() private$..color1$value,
        color2 = function() private$..color2$value),
    private = list(
        ..age = NA,
        ..gender = NA,
        ..female = NA,
        ..bin_width = NA,
        ..plot_title = NA,
        ..color1 = NA,
        ..color2 = NA)
)

agepyramid2Results <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "agepyramid2Results",
    inherit = jmvcore::Group,
    active = list(
        pyramidTable = function() private$.items[["pyramidTable"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Age Pyramid",
                refs=list(
                    "ClinicoPathJamoviModule"))
            self$add(jmvcore::Table$new(
                options=options,
                name="pyramidTable",
                title="Population Data",
                rows=0,
                columns=list(
                    list(
                        `name`="Pop", 
                        `title`="Population", 
                        `type`="text"),
                    list(
                        `name`="Female", 
                        `title`="Female", 
                        `type`="number"),
                    list(
                        `name`="Male", 
                        `title`="Male", 
                        `type`="number")),
                clearWith=list(
                    "age",
                    "gender",
                    "female")))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Age Pyramid",
                width=600,
                height=450,
                renderFun=".plot",
                requiresData=TRUE,
                clearWith=list(
                    "age",
                    "gender",
                    "female")))}))

agepyramid2Base <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "agepyramid2Base",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "agepyramid2",
                version = c(0,0,3),
                options = options,
                results = agepyramid2Results$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Age Pyramid
#'
#' 
#' @param data The data as a data frame.
#' @param age a string naming the variable from \code{data} that contains the
#'   continuous values used for the report
#' @param gender a string naming the variable from \code{data} that contains
#'   the categorical values used for the report
#' @param female a string naming the level from \code{gender} that contains
#'   the level female
#' @param bin_width The width of the age bins in years. Adjust this to change
#'   the granularity of the age groups.
#' @param plot_title The title displayed on the age pyramid plot.
#' @param color1 First color for the pyramid chart bars.
#' @param color2 Second color for the pyramid chart bars.
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$pyramidTable} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$pyramidTable$asDF}
#'
#' \code{as.data.frame(results$pyramidTable)}
#'
#' @export
agepyramid2 <- function(
    data,
    age,
    gender,
    female,
    bin_width = 5,
    plot_title = "Age Pyramid",
    color1 = "#1F77B4",
    color2 = "#FF7F0E") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("agepyramid2 requires jmvcore to be installed (restart may be required)")

    if ( ! missing(age)) age <- jmvcore::resolveQuo(jmvcore::enquo(age))
    if ( ! missing(gender)) gender <- jmvcore::resolveQuo(jmvcore::enquo(gender))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(age), age, NULL),
            `if`( ! missing(gender), gender, NULL))

    for (v in gender) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- agepyramid2Options$new(
        age = age,
        gender = gender,
        female = female,
        bin_width = bin_width,
        plot_title = plot_title,
        color1 = color1,
        color2 = color2)

    analysis <- agepyramid2Class$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

