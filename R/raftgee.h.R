
# This file is automatically generated, you probably don't want to edit this

raftgeeOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "raftgeeOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            elapsedtime = NULL,
            outcome = NULL,
            covariates = NULL,
            outcomeLevel = "1",
            rank_method = "logrank",
            correlation_structure = "independence",
            cluster_variable = NULL,
            confidence_level = 0.95,
            max_iterations = 100,
            convergence_tolerance = 0.000001,
            weights_variable = NULL,
            robust_variance = TRUE,
            bootstrap_samples = 0,
            show_model_summary = TRUE,
            show_coefficients = TRUE,
            show_diagnostics = TRUE,
            show_residual_plots = TRUE,
            show_qq_plots = TRUE,
            show_survival_plots = TRUE,
            show_acceleration_plots = TRUE,
            show_comparison_plots = TRUE,
            showSummaries = FALSE,
            showExplanations = FALSE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="raftgee",
                requiresData=TRUE,
                ...)

            private$..elapsedtime <- jmvcore::OptionVariable$new(
                "elapsedtime",
                elapsedtime,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..outcome <- jmvcore::OptionVariable$new(
                "outcome",
                outcome,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..covariates <- jmvcore::OptionVariables$new(
                "covariates",
                covariates,
                suggested=list(
                    "continuous",
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..outcomeLevel <- jmvcore::OptionString$new(
                "outcomeLevel",
                outcomeLevel,
                default="1")
            private$..rank_method <- jmvcore::OptionList$new(
                "rank_method",
                rank_method,
                options=list(
                    "logrank",
                    "gehan",
                    "normal",
                    "wilcoxon"),
                default="logrank")
            private$..correlation_structure <- jmvcore::OptionList$new(
                "correlation_structure",
                correlation_structure,
                options=list(
                    "independence",
                    "exchangeable",
                    "ar1",
                    "unstructured"),
                default="independence")
            private$..cluster_variable <- jmvcore::OptionVariable$new(
                "cluster_variable",
                cluster_variable,
                permitted=list(
                    "factor",
                    "numeric"))
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.5,
                max=0.99,
                default=0.95)
            private$..max_iterations <- jmvcore::OptionNumber$new(
                "max_iterations",
                max_iterations,
                min=10,
                max=1000,
                default=100)
            private$..convergence_tolerance <- jmvcore::OptionNumber$new(
                "convergence_tolerance",
                convergence_tolerance,
                min=1e-8,
                max=0.01,
                default=0.000001)
            private$..weights_variable <- jmvcore::OptionVariable$new(
                "weights_variable",
                weights_variable,
                permitted=list(
                    "numeric"))
            private$..robust_variance <- jmvcore::OptionBool$new(
                "robust_variance",
                robust_variance,
                default=TRUE)
            private$..bootstrap_samples <- jmvcore::OptionNumber$new(
                "bootstrap_samples",
                bootstrap_samples,
                min=0,
                max=2000,
                default=0)
            private$..show_model_summary <- jmvcore::OptionBool$new(
                "show_model_summary",
                show_model_summary,
                default=TRUE)
            private$..show_coefficients <- jmvcore::OptionBool$new(
                "show_coefficients",
                show_coefficients,
                default=TRUE)
            private$..show_diagnostics <- jmvcore::OptionBool$new(
                "show_diagnostics",
                show_diagnostics,
                default=TRUE)
            private$..show_residual_plots <- jmvcore::OptionBool$new(
                "show_residual_plots",
                show_residual_plots,
                default=TRUE)
            private$..show_qq_plots <- jmvcore::OptionBool$new(
                "show_qq_plots",
                show_qq_plots,
                default=TRUE)
            private$..show_survival_plots <- jmvcore::OptionBool$new(
                "show_survival_plots",
                show_survival_plots,
                default=TRUE)
            private$..show_acceleration_plots <- jmvcore::OptionBool$new(
                "show_acceleration_plots",
                show_acceleration_plots,
                default=TRUE)
            private$..show_comparison_plots <- jmvcore::OptionBool$new(
                "show_comparison_plots",
                show_comparison_plots,
                default=TRUE)
            private$..showSummaries <- jmvcore::OptionBool$new(
                "showSummaries",
                showSummaries,
                default=FALSE)
            private$..showExplanations <- jmvcore::OptionBool$new(
                "showExplanations",
                showExplanations,
                default=FALSE)

            self$.addOption(private$..elapsedtime)
            self$.addOption(private$..outcome)
            self$.addOption(private$..covariates)
            self$.addOption(private$..outcomeLevel)
            self$.addOption(private$..rank_method)
            self$.addOption(private$..correlation_structure)
            self$.addOption(private$..cluster_variable)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..max_iterations)
            self$.addOption(private$..convergence_tolerance)
            self$.addOption(private$..weights_variable)
            self$.addOption(private$..robust_variance)
            self$.addOption(private$..bootstrap_samples)
            self$.addOption(private$..show_model_summary)
            self$.addOption(private$..show_coefficients)
            self$.addOption(private$..show_diagnostics)
            self$.addOption(private$..show_residual_plots)
            self$.addOption(private$..show_qq_plots)
            self$.addOption(private$..show_survival_plots)
            self$.addOption(private$..show_acceleration_plots)
            self$.addOption(private$..show_comparison_plots)
            self$.addOption(private$..showSummaries)
            self$.addOption(private$..showExplanations)
        }),
    active = list(
        elapsedtime = function() private$..elapsedtime$value,
        outcome = function() private$..outcome$value,
        covariates = function() private$..covariates$value,
        outcomeLevel = function() private$..outcomeLevel$value,
        rank_method = function() private$..rank_method$value,
        correlation_structure = function() private$..correlation_structure$value,
        cluster_variable = function() private$..cluster_variable$value,
        confidence_level = function() private$..confidence_level$value,
        max_iterations = function() private$..max_iterations$value,
        convergence_tolerance = function() private$..convergence_tolerance$value,
        weights_variable = function() private$..weights_variable$value,
        robust_variance = function() private$..robust_variance$value,
        bootstrap_samples = function() private$..bootstrap_samples$value,
        show_model_summary = function() private$..show_model_summary$value,
        show_coefficients = function() private$..show_coefficients$value,
        show_diagnostics = function() private$..show_diagnostics$value,
        show_residual_plots = function() private$..show_residual_plots$value,
        show_qq_plots = function() private$..show_qq_plots$value,
        show_survival_plots = function() private$..show_survival_plots$value,
        show_acceleration_plots = function() private$..show_acceleration_plots$value,
        show_comparison_plots = function() private$..show_comparison_plots$value,
        showSummaries = function() private$..showSummaries$value,
        showExplanations = function() private$..showExplanations$value),
    private = list(
        ..elapsedtime = NA,
        ..outcome = NA,
        ..covariates = NA,
        ..outcomeLevel = NA,
        ..rank_method = NA,
        ..correlation_structure = NA,
        ..cluster_variable = NA,
        ..confidence_level = NA,
        ..max_iterations = NA,
        ..convergence_tolerance = NA,
        ..weights_variable = NA,
        ..robust_variance = NA,
        ..bootstrap_samples = NA,
        ..show_model_summary = NA,
        ..show_coefficients = NA,
        ..show_diagnostics = NA,
        ..show_residual_plots = NA,
        ..show_qq_plots = NA,
        ..show_survival_plots = NA,
        ..show_acceleration_plots = NA,
        ..show_comparison_plots = NA,
        ..showSummaries = NA,
        ..showExplanations = NA)
)

raftgeeResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "raftgeeResults",
    inherit = jmvcore::Group,
    active = list(
        modelSummary = function() private$.items[["modelSummary"]],
        coefficients = function() private$.items[["coefficients"]],
        diagnostics = function() private$.items[["diagnostics"]],
        geeDiagnostics = function() private$.items[["geeDiagnostics"]],
        convergenceInfo = function() private$.items[["convergenceInfo"]],
        modelComparison = function() private$.items[["modelComparison"]],
        residualPlots = function() private$.items[["residualPlots"]],
        qqPlots = function() private$.items[["qqPlots"]],
        survivalPlots = function() private$.items[["survivalPlots"]],
        accelerationPlots = function() private$.items[["accelerationPlots"]],
        comparisonPlots = function() private$.items[["comparisonPlots"]],
        correlationPlots = function() private$.items[["correlationPlots"]],
        summaryTable = function() private$.items[["summaryTable"]],
        methodExplanation = function() private$.items[["methodExplanation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Rank-based AFT Estimation")
            self$add(jmvcore::Table$new(
                options=options,
                name="modelSummary",
                title="Model Summary",
                visible="(show_model_summary)",
                columns=list(
                    list(
                        `name`="term", 
                        `title`="", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="coefficients",
                title="AFT Model Coefficients",
                visible="(show_coefficients)",
                columns=list(
                    list(
                        `name`="term", 
                        `title`="Term", 
                        `type`="text"),
                    list(
                        `name`="estimate", 
                        `title`="Estimate", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="se", 
                        `title`="SE", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="z", 
                        `title`="z", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="pvalue", 
                        `title`="p", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="ciLower", 
                        `title`="Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="ciUpper", 
                        `title`="Upper", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="acceleration", 
                        `title`="Accel. Factor", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="accel_lower", 
                        `title`="AF Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="accel_upper", 
                        `title`="AF Upper", 
                        `type`="number", 
                        `format`="zto"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="diagnostics",
                title="Model Diagnostics",
                visible="(show_diagnostics)",
                columns=list(
                    list(
                        `name`="statistic", 
                        `title`="Statistic", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="geeDiagnostics",
                title="GEE Diagnostics",
                visible="(show_diagnostics)",
                columns=list(
                    list(
                        `name`="parameter", 
                        `title`="Parameter", 
                        `type`="text"),
                    list(
                        `name`="estimate", 
                        `title`="Estimate", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="se", 
                        `title`="SE", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="convergenceInfo",
                title="Convergence Information",
                visible="(show_model_summary)",
                columns=list(
                    list(
                        `name`="criterion", 
                        `title`="Criterion", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="modelComparison",
                title="Model Comparison",
                visible="(show_comparison_plots)",
                columns=list(
                    list(
                        `name`="model", 
                        `title`="Model", 
                        `type`="text"),
                    list(
                        `name`="aic", 
                        `title`="AIC", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="loglik", 
                        `title`="Log-Likelihood", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="concordance", 
                        `title`="C-Index", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="residualPlots",
                title="Residual Diagnostic Plots",
                visible="(show_residual_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="qqPlots",
                title="Q-Q Plots",
                visible="(show_qq_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="survivalPlots",
                title="AFT-based Survival Curves",
                visible="(show_survival_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="accelerationPlots",
                title="Time Acceleration Factor Plots",
                visible="(show_acceleration_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="comparisonPlots",
                title="AFT vs Cox Model Comparison",
                visible="(show_comparison_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="correlationPlots",
                title="Working Correlation Structure",
                visible="(show_diagnostics)",
                width=600,
                height=600))
            self$add(jmvcore::Html$new(
                options=options,
                name="summaryTable",
                title="Analysis Summary",
                visible="(showSummaries)"))
            self$add(jmvcore::Html$new(
                options=options,
                name="methodExplanation",
                title="Method Explanation",
                visible="(showExplanations)"))}))

raftgeeBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "raftgeeBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "raftgee",
                version = c(1,0,0),
                options = options,
                results = raftgeeResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Rank-based AFT Estimation
#'
#' Implements rank-based accelerated failure time (AFT) models using  
#' generalized estimating equations (GEE) for survival data analysis. This 
#' approach provides robust estimation when proportional hazards assumptions 
#' are violated, offering distribution-free methods for modeling survival 
#' times with emphasis on median survival relationships and time acceleration 
#' effects in clinical research applications.
#'
#' @examples
#' # Example 1: Basic rank-based AFT with GEE
#' library(aftgee)
#' library(survival)
#'
#' raftgee(
#'     data = lung_data,
#'     elapsedtime = "time",
#'     outcome = "status",
#'     outcomeLevel = "2",
#'     covariates = c("age", "sex", "ph.ecog"),
#'     rank_method = "logrank",
#'     correlation_structure = "independence"
#' )
#'
#' @param data the data as a data frame
#' @param elapsedtime Survival time or follow-up duration variable
#' @param outcome Event indicator variable (0/1, FALSE/TRUE, or factor)
#' @param covariates Covariate variables for AFT modeling
#' @param outcomeLevel Level of outcome variable indicating event occurrence
#' @param rank_method Rank-based test method for AFT estimation
#' @param correlation_structure Working correlation structure for GEE
#' @param cluster_variable Variable defining clusters for correlated
#'   observations
#' @param confidence_level Confidence level for parameter estimates
#' @param max_iterations Maximum number of iterations for GEE algorithm
#' @param convergence_tolerance Convergence tolerance for parameter estimation
#' @param weights_variable Optional weights variable for observations
#' @param robust_variance Use robust sandwich variance estimator
#' @param bootstrap_samples Number of bootstrap samples for variance
#'   estimation (0 = no bootstrap)
#' @param show_model_summary Display comprehensive AFT model summary
#' @param show_coefficients Display AFT model coefficients table
#' @param show_diagnostics Display model diagnostic statistics
#' @param show_residual_plots Display residual diagnostic plots
#' @param show_qq_plots Display quantile-quantile plots
#' @param show_survival_plots Display AFT-based survival curves
#' @param show_acceleration_plots Display time acceleration factor plots
#' @param show_comparison_plots Display comparison with Cox models
#' @param showSummaries Generate natural language summaries of the analysis
#'   results
#' @param showExplanations Show detailed explanations of the methodology and
#'   interpretation
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$modelSummary} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$coefficients} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$diagnostics} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$geeDiagnostics} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$convergenceInfo} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$modelComparison} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$residualPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$qqPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$survivalPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$accelerationPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$comparisonPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$correlationPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$summaryTable} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$methodExplanation} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$modelSummary$asDF}
#'
#' \code{as.data.frame(results$modelSummary)}
#'
#' @export
raftgee <- function(
    data,
    elapsedtime,
    outcome,
    covariates,
    outcomeLevel = "1",
    rank_method = "logrank",
    correlation_structure = "independence",
    cluster_variable,
    confidence_level = 0.95,
    max_iterations = 100,
    convergence_tolerance = 0.000001,
    weights_variable,
    robust_variance = TRUE,
    bootstrap_samples = 0,
    show_model_summary = TRUE,
    show_coefficients = TRUE,
    show_diagnostics = TRUE,
    show_residual_plots = TRUE,
    show_qq_plots = TRUE,
    show_survival_plots = TRUE,
    show_acceleration_plots = TRUE,
    show_comparison_plots = TRUE,
    showSummaries = FALSE,
    showExplanations = FALSE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("raftgee requires jmvcore to be installed (restart may be required)")

    if ( ! missing(elapsedtime)) elapsedtime <- jmvcore::resolveQuo(jmvcore::enquo(elapsedtime))
    if ( ! missing(outcome)) outcome <- jmvcore::resolveQuo(jmvcore::enquo(outcome))
    if ( ! missing(covariates)) covariates <- jmvcore::resolveQuo(jmvcore::enquo(covariates))
    if ( ! missing(cluster_variable)) cluster_variable <- jmvcore::resolveQuo(jmvcore::enquo(cluster_variable))
    if ( ! missing(weights_variable)) weights_variable <- jmvcore::resolveQuo(jmvcore::enquo(weights_variable))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(elapsedtime), elapsedtime, NULL),
            `if`( ! missing(outcome), outcome, NULL),
            `if`( ! missing(covariates), covariates, NULL),
            `if`( ! missing(cluster_variable), cluster_variable, NULL),
            `if`( ! missing(weights_variable), weights_variable, NULL))


    options <- raftgeeOptions$new(
        elapsedtime = elapsedtime,
        outcome = outcome,
        covariates = covariates,
        outcomeLevel = outcomeLevel,
        rank_method = rank_method,
        correlation_structure = correlation_structure,
        cluster_variable = cluster_variable,
        confidence_level = confidence_level,
        max_iterations = max_iterations,
        convergence_tolerance = convergence_tolerance,
        weights_variable = weights_variable,
        robust_variance = robust_variance,
        bootstrap_samples = bootstrap_samples,
        show_model_summary = show_model_summary,
        show_coefficients = show_coefficients,
        show_diagnostics = show_diagnostics,
        show_residual_plots = show_residual_plots,
        show_qq_plots = show_qq_plots,
        show_survival_plots = show_survival_plots,
        show_acceleration_plots = show_acceleration_plots,
        show_comparison_plots = show_comparison_plots,
        showSummaries = showSummaries,
        showExplanations = showExplanations)

    analysis <- raftgeeClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

