
# This file is automatically generated, you probably don't want to edit this

directregressionOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "directregressionOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            elapsedtime = NULL,
            outcome = NULL,
            explanatory = NULL,
            outcomeLevel = "1",
            time_points = "12,24,60",
            regression_type = "linear",
            link_function = "identity",
            confidence_level = 0.95,
            bootstrap_se = FALSE,
            bootstrap_reps = 500,
            pseudo_method = "jackknife",
            show_pseudo_values = FALSE,
            show_residuals = FALSE,
            model_comparison = TRUE,
            survival_plot = TRUE,
            residual_plot = FALSE,
            prediction_plot = TRUE,
            showSummaries = TRUE,
            showExplanations = TRUE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="directregression",
                requiresData=TRUE,
                ...)

            private$..elapsedtime <- jmvcore::OptionVariable$new(
                "elapsedtime",
                elapsedtime,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..outcome <- jmvcore::OptionVariable$new(
                "outcome",
                outcome,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..explanatory <- jmvcore::OptionVariables$new(
                "explanatory",
                explanatory,
                suggested=list(
                    "continuous",
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..outcomeLevel <- jmvcore::OptionString$new(
                "outcomeLevel",
                outcomeLevel,
                default="1")
            private$..time_points <- jmvcore::OptionString$new(
                "time_points",
                time_points,
                default="12,24,60")
            private$..regression_type <- jmvcore::OptionList$new(
                "regression_type",
                regression_type,
                options=list(
                    "linear",
                    "logistic",
                    "complementary_log_log"),
                default="linear")
            private$..link_function <- jmvcore::OptionList$new(
                "link_function",
                link_function,
                options=list(
                    "identity",
                    "logit",
                    "cloglog",
                    "probit"),
                default="identity")
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                default=0.95,
                min=0.5,
                max=0.99)
            private$..bootstrap_se <- jmvcore::OptionBool$new(
                "bootstrap_se",
                bootstrap_se,
                default=FALSE)
            private$..bootstrap_reps <- jmvcore::OptionNumber$new(
                "bootstrap_reps",
                bootstrap_reps,
                default=500,
                min=100,
                max=2000)
            private$..pseudo_method <- jmvcore::OptionList$new(
                "pseudo_method",
                pseudo_method,
                options=list(
                    "jackknife",
                    "bootstrap",
                    "analytical"),
                default="jackknife")
            private$..show_pseudo_values <- jmvcore::OptionBool$new(
                "show_pseudo_values",
                show_pseudo_values,
                default=FALSE)
            private$..show_residuals <- jmvcore::OptionBool$new(
                "show_residuals",
                show_residuals,
                default=FALSE)
            private$..model_comparison <- jmvcore::OptionBool$new(
                "model_comparison",
                model_comparison,
                default=TRUE)
            private$..survival_plot <- jmvcore::OptionBool$new(
                "survival_plot",
                survival_plot,
                default=TRUE)
            private$..residual_plot <- jmvcore::OptionBool$new(
                "residual_plot",
                residual_plot,
                default=FALSE)
            private$..prediction_plot <- jmvcore::OptionBool$new(
                "prediction_plot",
                prediction_plot,
                default=TRUE)
            private$..showSummaries <- jmvcore::OptionBool$new(
                "showSummaries",
                showSummaries,
                default=TRUE)
            private$..showExplanations <- jmvcore::OptionBool$new(
                "showExplanations",
                showExplanations,
                default=TRUE)

            self$.addOption(private$..elapsedtime)
            self$.addOption(private$..outcome)
            self$.addOption(private$..explanatory)
            self$.addOption(private$..outcomeLevel)
            self$.addOption(private$..time_points)
            self$.addOption(private$..regression_type)
            self$.addOption(private$..link_function)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..bootstrap_se)
            self$.addOption(private$..bootstrap_reps)
            self$.addOption(private$..pseudo_method)
            self$.addOption(private$..show_pseudo_values)
            self$.addOption(private$..show_residuals)
            self$.addOption(private$..model_comparison)
            self$.addOption(private$..survival_plot)
            self$.addOption(private$..residual_plot)
            self$.addOption(private$..prediction_plot)
            self$.addOption(private$..showSummaries)
            self$.addOption(private$..showExplanations)
        }),
    active = list(
        elapsedtime = function() private$..elapsedtime$value,
        outcome = function() private$..outcome$value,
        explanatory = function() private$..explanatory$value,
        outcomeLevel = function() private$..outcomeLevel$value,
        time_points = function() private$..time_points$value,
        regression_type = function() private$..regression_type$value,
        link_function = function() private$..link_function$value,
        confidence_level = function() private$..confidence_level$value,
        bootstrap_se = function() private$..bootstrap_se$value,
        bootstrap_reps = function() private$..bootstrap_reps$value,
        pseudo_method = function() private$..pseudo_method$value,
        show_pseudo_values = function() private$..show_pseudo_values$value,
        show_residuals = function() private$..show_residuals$value,
        model_comparison = function() private$..model_comparison$value,
        survival_plot = function() private$..survival_plot$value,
        residual_plot = function() private$..residual_plot$value,
        prediction_plot = function() private$..prediction_plot$value,
        showSummaries = function() private$..showSummaries$value,
        showExplanations = function() private$..showExplanations$value),
    private = list(
        ..elapsedtime = NA,
        ..outcome = NA,
        ..explanatory = NA,
        ..outcomeLevel = NA,
        ..time_points = NA,
        ..regression_type = NA,
        ..link_function = NA,
        ..confidence_level = NA,
        ..bootstrap_se = NA,
        ..bootstrap_reps = NA,
        ..pseudo_method = NA,
        ..show_pseudo_values = NA,
        ..show_residuals = NA,
        ..model_comparison = NA,
        ..survival_plot = NA,
        ..residual_plot = NA,
        ..prediction_plot = NA,
        ..showSummaries = NA,
        ..showExplanations = NA)
)

directregressionResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "directregressionResults",
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        modelSummary = function() private$.items[["modelSummary"]],
        pseudoValues = function() private$.items[["pseudoValues"]],
        modelComparison = function() private$.items[["modelComparison"]],
        residualAnalysis = function() private$.items[["residualAnalysis"]],
        methodologyExplanation = function() private$.items[["methodologyExplanation"]],
        analysisSummary = function() private$.items[["analysisSummary"]],
        survivalPlot = function() private$.items[["survivalPlot"]],
        residualPlot = function() private$.items[["residualPlot"]],
        predictionPlot = function() private$.items[["predictionPlot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Direct Regression on Survival Function")
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="Analysis Complete",
                visible=FALSE))
            self$add(jmvcore::Table$new(
                options=options,
                name="modelSummary",
                title="Direct Regression Model Summary",
                columns=list(
                    list(
                        `name`="time_point", 
                        `title`="Time Point", 
                        `type`="number"),
                    list(
                        `name`="term", 
                        `title`="Term", 
                        `type`="text"),
                    list(
                        `name`="estimate", 
                        `title`="Estimate", 
                        `type`="number"),
                    list(
                        `name`="se", 
                        `title`="SE", 
                        `type`="number"),
                    list(
                        `name`="statistic", 
                        `title`="t/z", 
                        `type`="number"),
                    list(
                        `name`="p", 
                        `title`="p", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="ci_lower", 
                        `title`="Lower CI", 
                        `type`="number"),
                    list(
                        `name`="ci_upper", 
                        `title`="Upper CI", 
                        `type`="number"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="pseudoValues",
                title="Pseudo-Observation Values",
                visible="(show_pseudo_values)",
                columns=list(
                    list(
                        `name`="observation", 
                        `title`="Observation", 
                        `type`="integer"),
                    list(
                        `name`="time_point", 
                        `title`="Time Point", 
                        `type`="number"),
                    list(
                        `name`="pseudo_value", 
                        `title`="Pseudo-Value", 
                        `type`="number"),
                    list(
                        `name`="survival_prob", 
                        `title`="Survival Probability", 
                        `type`="number"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="modelComparison",
                title="Model Comparison Across Time Points",
                visible="(model_comparison)",
                columns=list(
                    list(
                        `name`="time_point", 
                        `title`="Time Point", 
                        `type`="number"),
                    list(
                        `name`="r_squared", 
                        `title`="R\u00B2", 
                        `type`="number"),
                    list(
                        `name`="adj_r_squared", 
                        `title`="Adj. R\u00B2", 
                        `type`="number"),
                    list(
                        `name`="aic", 
                        `title`="AIC", 
                        `type`="number"),
                    list(
                        `name`="bic", 
                        `title`="BIC", 
                        `type`="number"),
                    list(
                        `name`="rmse", 
                        `title`="RMSE", 
                        `type`="number"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="residualAnalysis",
                title="Residual Analysis",
                visible="(show_residuals)",
                columns=list(
                    list(
                        `name`="time_point", 
                        `title`="Time Point", 
                        `type`="number"),
                    list(
                        `name`="residual_type", 
                        `title`="Residual Type", 
                        `type`="text"),
                    list(
                        `name`="mean_residual", 
                        `title`="Mean Residual", 
                        `type`="number"),
                    list(
                        `name`="sd_residual", 
                        `title`="SD Residual", 
                        `type`="number"),
                    list(
                        `name`="normality_p", 
                        `title`="Normality Test p", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Html$new(
                options=options,
                name="methodologyExplanation",
                title="Methodology Explanation",
                visible="(showExplanations)"))
            self$add(jmvcore::Html$new(
                options=options,
                name="analysisSummary",
                title="Analysis Summary",
                visible="(showSummaries)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="survivalPlot",
                title="Survival Function with Regression Predictions",
                width=600,
                height=400,
                requiresData=TRUE,
                visible="(survival_plot)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="residualPlot",
                title="Residual Diagnostic Plots",
                width=600,
                height=400,
                requiresData=TRUE,
                visible="(residual_plot)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="predictionPlot",
                title="Model Predictions at Time Points",
                width=600,
                height=400,
                requiresData=TRUE,
                visible="(prediction_plot)"))}))

directregressionBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "directregressionBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "directregression",
                version = c(1,0,0),
                options = options,
                results = directregressionResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Direct Regression on Survival Function
#'
#' Direct regression on the survival function using pseudo-observations. This 
#' method allows for direct modeling of survival probabilities at specific 
#' time points using standard regression techniques, providing an alternative 
#' to hazard-based models. Particularly useful when interest lies in survival 
#' probabilities rather than hazard ratios.
#' 
#'
#' @examples
#' data \%>\%
#' directregression(
#'     elapsedtime = "time_to_event",
#'     outcome = "event_status",
#'     explanatory = c("age", "treatment", "stage"),
#'     time_points = c(12, 24, 60),
#'     regression_type = "linear"
#' )
#'
#' @param data the data as a data frame
#' @param elapsedtime Time to event or censoring
#' @param outcome Event indicator (1 = event, 0 = censored)
#' @param explanatory Explanatory variables for direct regression modeling
#' @param outcomeLevel Level indicating event occurrence
#' @param time_points Comma-separated list of time points at which to perform
#'   direct regression
#' @param regression_type Type of regression model for pseudo-observations
#' @param link_function Link function for the regression model
#' @param confidence_level Confidence level for confidence intervals
#' @param bootstrap_se Use bootstrap method for standard error estimation
#' @param bootstrap_reps Number of bootstrap replications for standard errors
#' @param pseudo_method Method for calculating pseudo-observations
#' @param show_pseudo_values Display pseudo-observation values in results
#' @param show_residuals Display residual analysis
#' @param model_comparison Compare models across different time points
#' @param survival_plot Display survival function with regression predictions
#' @param residual_plot Display residual diagnostic plots
#' @param prediction_plot Display model predictions at specified time points
#' @param showSummaries Show comprehensive analysis summaries
#' @param showExplanations Show detailed methodology explanations
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$modelSummary} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$pseudoValues} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$modelComparison} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$residualAnalysis} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$methodologyExplanation} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$analysisSummary} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$survivalPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$residualPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$predictionPlot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$modelSummary$asDF}
#'
#' \code{as.data.frame(results$modelSummary)}
#'
#' @export
directregression <- function(
    data,
    elapsedtime,
    outcome,
    explanatory,
    outcomeLevel = "1",
    time_points = "12,24,60",
    regression_type = "linear",
    link_function = "identity",
    confidence_level = 0.95,
    bootstrap_se = FALSE,
    bootstrap_reps = 500,
    pseudo_method = "jackknife",
    show_pseudo_values = FALSE,
    show_residuals = FALSE,
    model_comparison = TRUE,
    survival_plot = TRUE,
    residual_plot = FALSE,
    prediction_plot = TRUE,
    showSummaries = TRUE,
    showExplanations = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("directregression requires jmvcore to be installed (restart may be required)")

    if ( ! missing(elapsedtime)) elapsedtime <- jmvcore::resolveQuo(jmvcore::enquo(elapsedtime))
    if ( ! missing(outcome)) outcome <- jmvcore::resolveQuo(jmvcore::enquo(outcome))
    if ( ! missing(explanatory)) explanatory <- jmvcore::resolveQuo(jmvcore::enquo(explanatory))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(elapsedtime), elapsedtime, NULL),
            `if`( ! missing(outcome), outcome, NULL),
            `if`( ! missing(explanatory), explanatory, NULL))


    options <- directregressionOptions$new(
        elapsedtime = elapsedtime,
        outcome = outcome,
        explanatory = explanatory,
        outcomeLevel = outcomeLevel,
        time_points = time_points,
        regression_type = regression_type,
        link_function = link_function,
        confidence_level = confidence_level,
        bootstrap_se = bootstrap_se,
        bootstrap_reps = bootstrap_reps,
        pseudo_method = pseudo_method,
        show_pseudo_values = show_pseudo_values,
        show_residuals = show_residuals,
        model_comparison = model_comparison,
        survival_plot = survival_plot,
        residual_plot = residual_plot,
        prediction_plot = prediction_plot,
        showSummaries = showSummaries,
        showExplanations = showExplanations)

    analysis <- directregressionClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

