
# This file is automatically generated, you probably don't want to edit this

toxicityprofileOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "toxicityprofileOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            patientID = NULL,
            adverseEvent = NULL,
            grade = NULL,
            treatment = NULL,
            systemOrganClass = NULL,
            timeToEvent = NULL,
            plotType = "stacked_bar",
            sortBy = "frequency",
            showHighGradeOnly = FALSE,
            minIncidence = 5,
            gradeColors = "ctcae",
            showPercentages = TRUE,
            showConfidenceIntervals = FALSE,
            groupComparison = FALSE,
            cumulativeIncidence = FALSE,
            confidenceLevel = "0.95", ...) {

            super$initialize(
                package="ClinicoPath",
                name="toxicityprofile",
                requiresData=TRUE,
                ...)

            private$..patientID <- jmvcore::OptionVariable$new(
                "patientID",
                patientID,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "numeric",
                    "factor",
                    "id"))
            private$..adverseEvent <- jmvcore::OptionVariable$new(
                "adverseEvent",
                adverseEvent,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..grade <- jmvcore::OptionVariable$new(
                "grade",
                grade,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..treatment <- jmvcore::OptionVariable$new(
                "treatment",
                treatment,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..systemOrganClass <- jmvcore::OptionVariable$new(
                "systemOrganClass",
                systemOrganClass,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..timeToEvent <- jmvcore::OptionVariable$new(
                "timeToEvent",
                timeToEvent,
                suggested=list(
                    "continuous"))
            private$..plotType <- jmvcore::OptionList$new(
                "plotType",
                plotType,
                options=list(
                    "stacked_bar",
                    "dot_plot",
                    "heatmap",
                    "time_to_event"),
                default="stacked_bar")
            private$..sortBy <- jmvcore::OptionList$new(
                "sortBy",
                sortBy,
                options=list(
                    "frequency",
                    "high_grade",
                    "alphabetical",
                    "soc"),
                default="frequency")
            private$..showHighGradeOnly <- jmvcore::OptionBool$new(
                "showHighGradeOnly",
                showHighGradeOnly,
                default=FALSE)
            private$..minIncidence <- jmvcore::OptionNumber$new(
                "minIncidence",
                minIncidence,
                default=5,
                min=0,
                max=100)
            private$..gradeColors <- jmvcore::OptionList$new(
                "gradeColors",
                gradeColors,
                options=list(
                    "ctcae",
                    "traffic",
                    "viridis",
                    "custom"),
                default="ctcae")
            private$..showPercentages <- jmvcore::OptionBool$new(
                "showPercentages",
                showPercentages,
                default=TRUE)
            private$..showConfidenceIntervals <- jmvcore::OptionBool$new(
                "showConfidenceIntervals",
                showConfidenceIntervals,
                default=FALSE)
            private$..groupComparison <- jmvcore::OptionBool$new(
                "groupComparison",
                groupComparison,
                default=FALSE)
            private$..cumulativeIncidence <- jmvcore::OptionBool$new(
                "cumulativeIncidence",
                cumulativeIncidence,
                default=FALSE)
            private$..confidenceLevel <- jmvcore::OptionList$new(
                "confidenceLevel",
                confidenceLevel,
                options=list(
                    "0.95",
                    "0.90",
                    "0.99"),
                default="0.95")

            self$.addOption(private$..patientID)
            self$.addOption(private$..adverseEvent)
            self$.addOption(private$..grade)
            self$.addOption(private$..treatment)
            self$.addOption(private$..systemOrganClass)
            self$.addOption(private$..timeToEvent)
            self$.addOption(private$..plotType)
            self$.addOption(private$..sortBy)
            self$.addOption(private$..showHighGradeOnly)
            self$.addOption(private$..minIncidence)
            self$.addOption(private$..gradeColors)
            self$.addOption(private$..showPercentages)
            self$.addOption(private$..showConfidenceIntervals)
            self$.addOption(private$..groupComparison)
            self$.addOption(private$..cumulativeIncidence)
            self$.addOption(private$..confidenceLevel)
        }),
    active = list(
        patientID = function() private$..patientID$value,
        adverseEvent = function() private$..adverseEvent$value,
        grade = function() private$..grade$value,
        treatment = function() private$..treatment$value,
        systemOrganClass = function() private$..systemOrganClass$value,
        timeToEvent = function() private$..timeToEvent$value,
        plotType = function() private$..plotType$value,
        sortBy = function() private$..sortBy$value,
        showHighGradeOnly = function() private$..showHighGradeOnly$value,
        minIncidence = function() private$..minIncidence$value,
        gradeColors = function() private$..gradeColors$value,
        showPercentages = function() private$..showPercentages$value,
        showConfidenceIntervals = function() private$..showConfidenceIntervals$value,
        groupComparison = function() private$..groupComparison$value,
        cumulativeIncidence = function() private$..cumulativeIncidence$value,
        confidenceLevel = function() private$..confidenceLevel$value),
    private = list(
        ..patientID = NA,
        ..adverseEvent = NA,
        ..grade = NA,
        ..treatment = NA,
        ..systemOrganClass = NA,
        ..timeToEvent = NA,
        ..plotType = NA,
        ..sortBy = NA,
        ..showHighGradeOnly = NA,
        ..minIncidence = NA,
        ..gradeColors = NA,
        ..showPercentages = NA,
        ..showConfidenceIntervals = NA,
        ..groupComparison = NA,
        ..cumulativeIncidence = NA,
        ..confidenceLevel = NA)
)

toxicityprofileResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "toxicityprofileResults",
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        plot = function() private$.items[["plot"]],
        summary = function() private$.items[["summary"]],
        gradeDistribution = function() private$.items[["gradeDistribution"]],
        groupComparison = function() private$.items[["groupComparison"]],
        socSummary = function() private$.items[["socSummary"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Treatment Toxicity Profile",
                refs=list(
                    "ggplot2",
                    "dplyr",
                    "survival",
                    "RColorBrewer",
                    "ClinicoPathJamoviModule",
                    "viridis",
                    "scales",
                    "binom"))
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="Instructions"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Toxicity Profile Plot",
                width=700,
                height=500,
                requiresData=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="summary",
                title="Adverse Event Summary",
                columns=list(
                    list(
                        `name`="adverse_event", 
                        `title`="Adverse Event", 
                        `type`="text"),
                    list(
                        `name`="n_patients", 
                        `title`="N Patients", 
                        `type`="integer"),
                    list(
                        `name`="n_events", 
                        `title`="N Events", 
                        `type`="integer"),
                    list(
                        `name`="incidence", 
                        `title`="Incidence (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="grade_1_2", 
                        `title`="Grade 1-2 (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="grade_3_plus", 
                        `title`="Grade 3+ (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="median_time", 
                        `title`="Median Time (days)", 
                        `type`="number", 
                        `format`="zto"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="gradeDistribution",
                title="Grade Distribution",
                columns=list(
                    list(
                        `name`="adverse_event", 
                        `title`="Adverse Event", 
                        `type`="text"),
                    list(
                        `name`="grade_1", 
                        `title`="Grade 1", 
                        `type`="integer"),
                    list(
                        `name`="grade_2", 
                        `title`="Grade 2", 
                        `type`="integer"),
                    list(
                        `name`="grade_3", 
                        `title`="Grade 3", 
                        `type`="integer"),
                    list(
                        `name`="grade_4", 
                        `title`="Grade 4", 
                        `type`="integer"),
                    list(
                        `name`="grade_5", 
                        `title`="Grade 5", 
                        `type`="integer"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="groupComparison",
                title="Treatment Group Comparison",
                columns=list(
                    list(
                        `name`="adverse_event", 
                        `title`="Adverse Event", 
                        `type`="text"),
                    list(
                        `name`="group1_incidence", 
                        `title`="Group 1 (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="group2_incidence", 
                        `title`="Group 2 (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="risk_ratio", 
                        `title`="Risk Ratio", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="ci_lower", 
                        `title`="95% CI Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="ci_upper", 
                        `title`="95% CI Upper", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="pvalue", 
                        `title`="P-value", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="socSummary",
                title="System Organ Class Summary",
                columns=list(
                    list(
                        `name`="soc", 
                        `title`="System Organ Class", 
                        `type`="text"),
                    list(
                        `name`="n_events", 
                        `title`="N Events", 
                        `type`="integer"),
                    list(
                        `name`="n_patients", 
                        `title`="N Patients", 
                        `type`="integer"),
                    list(
                        `name`="incidence", 
                        `title`="Incidence (%)", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="high_grade_incidence", 
                        `title`="High Grade (%)", 
                        `type`="number", 
                        `format`="zto"))))}))

toxicityprofileBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "toxicityprofileBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "toxicityprofile",
                version = c(0,0,3),
                options = options,
                results = toxicityprofileResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Treatment Toxicity Profile
#'
#' 
#' @param data The data as a data frame.
#' @param patientID Variable containing patient identifiers.
#' @param adverseEvent Variable containing adverse event names or categories.
#' @param grade CTCAE grade or severity level (1-5 or equivalent scale).
#' @param treatment Treatment or study arm for comparison (optional).
#' @param systemOrganClass System organ class (SOC) for grouping adverse
#'   events.
#' @param timeToEvent Time from treatment start to adverse event occurrence.
#' @param plotType Primary visualization method for toxicity profile.
#' @param sortBy Method for ordering adverse events in the plot.
#' @param showHighGradeOnly Display only grade 3+ adverse events.
#' @param minIncidence Minimum incidence percentage for events to be
#'   displayed.
#' @param gradeColors Color scheme for toxicity grades.
#' @param showPercentages Display percentages on the plot.
#' @param showConfidenceIntervals Display confidence intervals for incidence
#'   rates.
#' @param groupComparison Perform statistical comparison between treatment
#'   groups.
#' @param cumulativeIncidence Display cumulative incidence over time (requires
#'   time variable).
#' @param confidenceLevel Confidence level for intervals and tests.
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$summary} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$gradeDistribution} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$groupComparison} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$socSummary} \tab \tab \tab \tab \tab a table \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$summary$asDF}
#'
#' \code{as.data.frame(results$summary)}
#'
#' @export
toxicityprofile <- function(
    data,
    patientID,
    adverseEvent,
    grade,
    treatment,
    systemOrganClass,
    timeToEvent,
    plotType = "stacked_bar",
    sortBy = "frequency",
    showHighGradeOnly = FALSE,
    minIncidence = 5,
    gradeColors = "ctcae",
    showPercentages = TRUE,
    showConfidenceIntervals = FALSE,
    groupComparison = FALSE,
    cumulativeIncidence = FALSE,
    confidenceLevel = "0.95") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("toxicityprofile requires jmvcore to be installed (restart may be required)")

    if ( ! missing(patientID)) patientID <- jmvcore::resolveQuo(jmvcore::enquo(patientID))
    if ( ! missing(adverseEvent)) adverseEvent <- jmvcore::resolveQuo(jmvcore::enquo(adverseEvent))
    if ( ! missing(grade)) grade <- jmvcore::resolveQuo(jmvcore::enquo(grade))
    if ( ! missing(treatment)) treatment <- jmvcore::resolveQuo(jmvcore::enquo(treatment))
    if ( ! missing(systemOrganClass)) systemOrganClass <- jmvcore::resolveQuo(jmvcore::enquo(systemOrganClass))
    if ( ! missing(timeToEvent)) timeToEvent <- jmvcore::resolveQuo(jmvcore::enquo(timeToEvent))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(patientID), patientID, NULL),
            `if`( ! missing(adverseEvent), adverseEvent, NULL),
            `if`( ! missing(grade), grade, NULL),
            `if`( ! missing(treatment), treatment, NULL),
            `if`( ! missing(systemOrganClass), systemOrganClass, NULL),
            `if`( ! missing(timeToEvent), timeToEvent, NULL))

    for (v in adverseEvent) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])
    for (v in treatment) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])
    for (v in systemOrganClass) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- toxicityprofileOptions$new(
        patientID = patientID,
        adverseEvent = adverseEvent,
        grade = grade,
        treatment = treatment,
        systemOrganClass = systemOrganClass,
        timeToEvent = timeToEvent,
        plotType = plotType,
        sortBy = sortBy,
        showHighGradeOnly = showHighGradeOnly,
        minIncidence = minIncidence,
        gradeColors = gradeColors,
        showPercentages = showPercentages,
        showConfidenceIntervals = showConfidenceIntervals,
        groupComparison = groupComparison,
        cumulativeIncidence = cumulativeIncidence,
        confidenceLevel = confidenceLevel)

    analysis <- toxicityprofileClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

