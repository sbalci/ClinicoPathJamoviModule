
# This file is automatically generated, you probably don't want to edit this

transformationmodelsOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "transformationmodelsOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            elapsedtime = NULL,
            outcome = NULL,
            covariates = NULL,
            outcomeLevel = "1",
            transformation = "boxcox",
            distribution = "normal",
            method = "ml",
            support = "automatic",
            lambda_search = TRUE,
            lambda_range_min = -2,
            lambda_range_max = 2,
            stratify_variable = NULL,
            weights_variable = NULL,
            confidence_level = 0.95,
            max_iterations = 100,
            convergence_tolerance = 0.000001,
            transformation_validation = TRUE,
            model_selection = FALSE,
            bootstrap_ci = FALSE,
            bootstrap_samples = 500,
            show_model_summary = TRUE,
            show_coefficients = TRUE,
            show_transformation = TRUE,
            show_diagnostics = TRUE,
            show_comparison = TRUE,
            show_transformation_plots = TRUE,
            show_residual_plots = TRUE,
            show_survival_plots = TRUE,
            show_qq_plots = TRUE,
            showSummaries = FALSE,
            showExplanations = FALSE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="transformationmodels",
                requiresData=TRUE,
                ...)

            private$..elapsedtime <- jmvcore::OptionVariable$new(
                "elapsedtime",
                elapsedtime,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..outcome <- jmvcore::OptionVariable$new(
                "outcome",
                outcome,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..covariates <- jmvcore::OptionVariables$new(
                "covariates",
                covariates,
                suggested=list(
                    "continuous",
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..outcomeLevel <- jmvcore::OptionString$new(
                "outcomeLevel",
                outcomeLevel,
                default="1")
            private$..transformation <- jmvcore::OptionList$new(
                "transformation",
                transformation,
                options=list(
                    "linear",
                    "boxcox",
                    "loglog",
                    "probit",
                    "logit",
                    "cloglog",
                    "nonparametric"),
                default="boxcox")
            private$..distribution <- jmvcore::OptionList$new(
                "distribution",
                distribution,
                options=list(
                    "normal",
                    "logistic",
                    "extreme",
                    "exponential",
                    "weibull"),
                default="normal")
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "ml",
                    "score",
                    "gradient",
                    "iterative"),
                default="ml")
            private$..support <- jmvcore::OptionList$new(
                "support",
                support,
                options=list(
                    "automatic",
                    "bounded",
                    "unbounded",
                    "custom"),
                default="automatic")
            private$..lambda_search <- jmvcore::OptionBool$new(
                "lambda_search",
                lambda_search,
                default=TRUE)
            private$..lambda_range_min <- jmvcore::OptionNumber$new(
                "lambda_range_min",
                lambda_range_min,
                min=-3,
                max=0,
                default=-2)
            private$..lambda_range_max <- jmvcore::OptionNumber$new(
                "lambda_range_max",
                lambda_range_max,
                min=0,
                max=3,
                default=2)
            private$..stratify_variable <- jmvcore::OptionVariable$new(
                "stratify_variable",
                stratify_variable,
                permitted=list(
                    "factor",
                    "numeric"))
            private$..weights_variable <- jmvcore::OptionVariable$new(
                "weights_variable",
                weights_variable,
                permitted=list(
                    "numeric"))
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.5,
                max=0.99,
                default=0.95)
            private$..max_iterations <- jmvcore::OptionNumber$new(
                "max_iterations",
                max_iterations,
                min=10,
                max=1000,
                default=100)
            private$..convergence_tolerance <- jmvcore::OptionNumber$new(
                "convergence_tolerance",
                convergence_tolerance,
                min=1e-8,
                max=0.01,
                default=0.000001)
            private$..transformation_validation <- jmvcore::OptionBool$new(
                "transformation_validation",
                transformation_validation,
                default=TRUE)
            private$..model_selection <- jmvcore::OptionBool$new(
                "model_selection",
                model_selection,
                default=FALSE)
            private$..bootstrap_ci <- jmvcore::OptionBool$new(
                "bootstrap_ci",
                bootstrap_ci,
                default=FALSE)
            private$..bootstrap_samples <- jmvcore::OptionNumber$new(
                "bootstrap_samples",
                bootstrap_samples,
                min=100,
                max=2000,
                default=500)
            private$..show_model_summary <- jmvcore::OptionBool$new(
                "show_model_summary",
                show_model_summary,
                default=TRUE)
            private$..show_coefficients <- jmvcore::OptionBool$new(
                "show_coefficients",
                show_coefficients,
                default=TRUE)
            private$..show_transformation <- jmvcore::OptionBool$new(
                "show_transformation",
                show_transformation,
                default=TRUE)
            private$..show_diagnostics <- jmvcore::OptionBool$new(
                "show_diagnostics",
                show_diagnostics,
                default=TRUE)
            private$..show_comparison <- jmvcore::OptionBool$new(
                "show_comparison",
                show_comparison,
                default=TRUE)
            private$..show_transformation_plots <- jmvcore::OptionBool$new(
                "show_transformation_plots",
                show_transformation_plots,
                default=TRUE)
            private$..show_residual_plots <- jmvcore::OptionBool$new(
                "show_residual_plots",
                show_residual_plots,
                default=TRUE)
            private$..show_survival_plots <- jmvcore::OptionBool$new(
                "show_survival_plots",
                show_survival_plots,
                default=TRUE)
            private$..show_qq_plots <- jmvcore::OptionBool$new(
                "show_qq_plots",
                show_qq_plots,
                default=TRUE)
            private$..showSummaries <- jmvcore::OptionBool$new(
                "showSummaries",
                showSummaries,
                default=FALSE)
            private$..showExplanations <- jmvcore::OptionBool$new(
                "showExplanations",
                showExplanations,
                default=FALSE)

            self$.addOption(private$..elapsedtime)
            self$.addOption(private$..outcome)
            self$.addOption(private$..covariates)
            self$.addOption(private$..outcomeLevel)
            self$.addOption(private$..transformation)
            self$.addOption(private$..distribution)
            self$.addOption(private$..method)
            self$.addOption(private$..support)
            self$.addOption(private$..lambda_search)
            self$.addOption(private$..lambda_range_min)
            self$.addOption(private$..lambda_range_max)
            self$.addOption(private$..stratify_variable)
            self$.addOption(private$..weights_variable)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..max_iterations)
            self$.addOption(private$..convergence_tolerance)
            self$.addOption(private$..transformation_validation)
            self$.addOption(private$..model_selection)
            self$.addOption(private$..bootstrap_ci)
            self$.addOption(private$..bootstrap_samples)
            self$.addOption(private$..show_model_summary)
            self$.addOption(private$..show_coefficients)
            self$.addOption(private$..show_transformation)
            self$.addOption(private$..show_diagnostics)
            self$.addOption(private$..show_comparison)
            self$.addOption(private$..show_transformation_plots)
            self$.addOption(private$..show_residual_plots)
            self$.addOption(private$..show_survival_plots)
            self$.addOption(private$..show_qq_plots)
            self$.addOption(private$..showSummaries)
            self$.addOption(private$..showExplanations)
        }),
    active = list(
        elapsedtime = function() private$..elapsedtime$value,
        outcome = function() private$..outcome$value,
        covariates = function() private$..covariates$value,
        outcomeLevel = function() private$..outcomeLevel$value,
        transformation = function() private$..transformation$value,
        distribution = function() private$..distribution$value,
        method = function() private$..method$value,
        support = function() private$..support$value,
        lambda_search = function() private$..lambda_search$value,
        lambda_range_min = function() private$..lambda_range_min$value,
        lambda_range_max = function() private$..lambda_range_max$value,
        stratify_variable = function() private$..stratify_variable$value,
        weights_variable = function() private$..weights_variable$value,
        confidence_level = function() private$..confidence_level$value,
        max_iterations = function() private$..max_iterations$value,
        convergence_tolerance = function() private$..convergence_tolerance$value,
        transformation_validation = function() private$..transformation_validation$value,
        model_selection = function() private$..model_selection$value,
        bootstrap_ci = function() private$..bootstrap_ci$value,
        bootstrap_samples = function() private$..bootstrap_samples$value,
        show_model_summary = function() private$..show_model_summary$value,
        show_coefficients = function() private$..show_coefficients$value,
        show_transformation = function() private$..show_transformation$value,
        show_diagnostics = function() private$..show_diagnostics$value,
        show_comparison = function() private$..show_comparison$value,
        show_transformation_plots = function() private$..show_transformation_plots$value,
        show_residual_plots = function() private$..show_residual_plots$value,
        show_survival_plots = function() private$..show_survival_plots$value,
        show_qq_plots = function() private$..show_qq_plots$value,
        showSummaries = function() private$..showSummaries$value,
        showExplanations = function() private$..showExplanations$value),
    private = list(
        ..elapsedtime = NA,
        ..outcome = NA,
        ..covariates = NA,
        ..outcomeLevel = NA,
        ..transformation = NA,
        ..distribution = NA,
        ..method = NA,
        ..support = NA,
        ..lambda_search = NA,
        ..lambda_range_min = NA,
        ..lambda_range_max = NA,
        ..stratify_variable = NA,
        ..weights_variable = NA,
        ..confidence_level = NA,
        ..max_iterations = NA,
        ..convergence_tolerance = NA,
        ..transformation_validation = NA,
        ..model_selection = NA,
        ..bootstrap_ci = NA,
        ..bootstrap_samples = NA,
        ..show_model_summary = NA,
        ..show_coefficients = NA,
        ..show_transformation = NA,
        ..show_diagnostics = NA,
        ..show_comparison = NA,
        ..show_transformation_plots = NA,
        ..show_residual_plots = NA,
        ..show_survival_plots = NA,
        ..show_qq_plots = NA,
        ..showSummaries = NA,
        ..showExplanations = NA)
)

transformationmodelsResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "transformationmodelsResults",
    inherit = jmvcore::Group,
    active = list(
        modelSummary = function() private$.items[["modelSummary"]],
        coefficients = function() private$.items[["coefficients"]],
        transformationAnalysis = function() private$.items[["transformationAnalysis"]],
        diagnostics = function() private$.items[["diagnostics"]],
        modelComparison = function() private$.items[["modelComparison"]],
        transformationInfo = function() private$.items[["transformationInfo"]],
        lambdaSearch = function() private$.items[["lambdaSearch"]],
        validationResults = function() private$.items[["validationResults"]],
        transformationPlots = function() private$.items[["transformationPlots"]],
        residualPlots = function() private$.items[["residualPlots"]],
        survivalPlots = function() private$.items[["survivalPlots"]],
        qqPlots = function() private$.items[["qqPlots"]],
        summaryTable = function() private$.items[["summaryTable"]],
        methodExplanation = function() private$.items[["methodExplanation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Transformation Models",
                refs=list(
                    "ClinicoPathJamoviModule"))
            self$add(jmvcore::Table$new(
                options=options,
                name="modelSummary",
                title="Model Summary",
                visible="(show_model_summary)",
                columns=list(
                    list(
                        `name`="term", 
                        `title`="", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="coefficients",
                title="Transformation Model Coefficients",
                visible="(show_coefficients)",
                columns=list(
                    list(
                        `name`="term", 
                        `title`="Term", 
                        `type`="text"),
                    list(
                        `name`="estimate", 
                        `title`="Estimate", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="se", 
                        `title`="SE", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="z", 
                        `title`="z", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="pvalue", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="ciLower", 
                        `title`="Lower CI", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="ciUpper", 
                        `title`="Upper CI", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="effect_ratio", 
                        `title`="Effect Ratio", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="er_lower", 
                        `title`="ER Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="er_upper", 
                        `title`="ER Upper", 
                        `type`="number", 
                        `format`="zto"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="transformationAnalysis",
                title="Transformation Function Analysis",
                visible="(show_transformation)",
                columns=list(
                    list(
                        `name`="parameter", 
                        `title`="Parameter", 
                        `type`="text"),
                    list(
                        `name`="estimate", 
                        `title`="Estimate", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="se", 
                        `title`="SE", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="diagnostics",
                title="Model Diagnostics",
                visible="(show_diagnostics)",
                columns=list(
                    list(
                        `name`="measure", 
                        `title`="Measure", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="modelComparison",
                title="Transformation Model Comparison",
                visible="(show_comparison)",
                columns=list(
                    list(
                        `name`="transformation", 
                        `title`="Transformation", 
                        `type`="text"),
                    list(
                        `name`="loglik", 
                        `title`="Log-Likelihood", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="aic", 
                        `title`="AIC", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="bic", 
                        `title`="BIC", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="convergence", 
                        `title`="Converged", 
                        `type`="text"),
                    list(
                        `name`="selected", 
                        `title`="Selected", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="transformationInfo",
                title="Transformation Information",
                visible="(show_transformation)",
                columns=list(
                    list(
                        `name`="criterion", 
                        `title`="Criterion", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="lambdaSearch",
                title="Lambda Parameter Search",
                visible="(show_transformation && lambda_search)",
                columns=list(
                    list(
                        `name`="lambda", 
                        `title`="Lambda", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="loglik", 
                        `title`="Log-Likelihood", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="selected", 
                        `title`="Selected", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="validationResults",
                title="Transformation Validation",
                visible="(show_diagnostics && transformation_validation)",
                columns=list(
                    list(
                        `name`="test", 
                        `title`="Test", 
                        `type`="text"),
                    list(
                        `name`="statistic", 
                        `title`="Statistic", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="pvalue", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="conclusion", 
                        `title`="Conclusion", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="transformationPlots",
                title="Transformation Function Plots",
                visible="(show_transformation_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="residualPlots",
                title="Residual Diagnostic Plots",
                visible="(show_residual_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="survivalPlots",
                title="Survival Curves",
                visible="(show_survival_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Image$new(
                options=options,
                name="qqPlots",
                title="Q-Q Plots",
                visible="(show_qq_plots)",
                width=800,
                height=600))
            self$add(jmvcore::Html$new(
                options=options,
                name="summaryTable",
                title="Analysis Summary",
                visible="(showSummaries)"))
            self$add(jmvcore::Html$new(
                options=options,
                name="methodExplanation",
                title="Method Explanation",
                visible="(showExplanations)"))}))

transformationmodelsBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "transformationmodelsBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "transformationmodels",
                version = c(1,0,0),
                options = options,
                results = transformationmodelsResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Transformation Models
#'
#' Implements transformation models providing a unified framework for  various 
#' survival analysis approaches through transformation functions. Includes 
#' linear transformation models, Box-Cox transformations, and non-parametric 
#' transformations, enabling flexible modeling of survival data with automatic 
#' transformation selection and validation.
#'
#' @examples
#' # Example: Transformation model with Box-Cox transformation
#' library(survival)
#' library(tram)
#'
#' transformationmodels(
#'     data = survival_data,
#'     elapsedtime = "time",
#'     outcome = "status",
#'     outcomeLevel = "1",
#'     covariates = c("age", "treatment", "biomarker"),
#'     transformation = "boxcox",
#'     distribution = "normal",
#'     method = "ml"
#' )
#'
#' @param data the data as a data frame
#' @param elapsedtime Survival time or follow-up duration variable
#' @param outcome Event indicator variable (0/1, FALSE/TRUE, or factor)
#' @param covariates Predictor variables for the transformation model
#' @param outcomeLevel Level of outcome variable indicating event occurrence
#' @param transformation Type of transformation function to apply
#' @param distribution Error distribution assumption
#' @param method Parameter estimation method
#' @param support Specification of transformation support
#' @param lambda_search Automatic search for optimal Box-Cox lambda parameter
#' @param lambda_range_min Minimum value for lambda parameter search
#' @param lambda_range_max Maximum value for lambda parameter search
#' @param stratify_variable Variable for stratified analysis
#' @param weights_variable Observation weights variable
#' @param confidence_level Confidence level for intervals
#' @param max_iterations Maximum number of iterations
#' @param convergence_tolerance Convergence tolerance for estimation
#' @param transformation_validation Validate transformation assumptions
#' @param model_selection Automatic selection of best transformation
#' @param bootstrap_ci Compute bootstrap confidence intervals
#' @param bootstrap_samples Number of bootstrap samples
#' @param show_model_summary Display comprehensive model summary
#' @param show_coefficients Display coefficient estimates table
#' @param show_transformation Display transformation function analysis
#' @param show_diagnostics Display model diagnostics
#' @param show_comparison Compare different transformation models
#' @param show_transformation_plots Display transformation function plots
#' @param show_residual_plots Display residual diagnostic plots
#' @param show_survival_plots Display survival curves
#' @param show_qq_plots Display quantile-quantile plots
#' @param showSummaries Generate natural language summaries
#' @param showExplanations Show detailed methodology explanations
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$modelSummary} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$coefficients} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$transformationAnalysis} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$diagnostics} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$modelComparison} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$transformationInfo} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$lambdaSearch} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$validationResults} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$transformationPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$residualPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$survivalPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$qqPlots} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$summaryTable} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$methodExplanation} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$modelSummary$asDF}
#'
#' \code{as.data.frame(results$modelSummary)}
#'
#' @export
transformationmodels <- function(
    data,
    elapsedtime,
    outcome,
    covariates,
    outcomeLevel = "1",
    transformation = "boxcox",
    distribution = "normal",
    method = "ml",
    support = "automatic",
    lambda_search = TRUE,
    lambda_range_min = -2,
    lambda_range_max = 2,
    stratify_variable,
    weights_variable,
    confidence_level = 0.95,
    max_iterations = 100,
    convergence_tolerance = 0.000001,
    transformation_validation = TRUE,
    model_selection = FALSE,
    bootstrap_ci = FALSE,
    bootstrap_samples = 500,
    show_model_summary = TRUE,
    show_coefficients = TRUE,
    show_transformation = TRUE,
    show_diagnostics = TRUE,
    show_comparison = TRUE,
    show_transformation_plots = TRUE,
    show_residual_plots = TRUE,
    show_survival_plots = TRUE,
    show_qq_plots = TRUE,
    showSummaries = FALSE,
    showExplanations = FALSE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("transformationmodels requires jmvcore to be installed (restart may be required)")

    if ( ! missing(elapsedtime)) elapsedtime <- jmvcore::resolveQuo(jmvcore::enquo(elapsedtime))
    if ( ! missing(outcome)) outcome <- jmvcore::resolveQuo(jmvcore::enquo(outcome))
    if ( ! missing(covariates)) covariates <- jmvcore::resolveQuo(jmvcore::enquo(covariates))
    if ( ! missing(stratify_variable)) stratify_variable <- jmvcore::resolveQuo(jmvcore::enquo(stratify_variable))
    if ( ! missing(weights_variable)) weights_variable <- jmvcore::resolveQuo(jmvcore::enquo(weights_variable))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(elapsedtime), elapsedtime, NULL),
            `if`( ! missing(outcome), outcome, NULL),
            `if`( ! missing(covariates), covariates, NULL),
            `if`( ! missing(stratify_variable), stratify_variable, NULL),
            `if`( ! missing(weights_variable), weights_variable, NULL))


    options <- transformationmodelsOptions$new(
        elapsedtime = elapsedtime,
        outcome = outcome,
        covariates = covariates,
        outcomeLevel = outcomeLevel,
        transformation = transformation,
        distribution = distribution,
        method = method,
        support = support,
        lambda_search = lambda_search,
        lambda_range_min = lambda_range_min,
        lambda_range_max = lambda_range_max,
        stratify_variable = stratify_variable,
        weights_variable = weights_variable,
        confidence_level = confidence_level,
        max_iterations = max_iterations,
        convergence_tolerance = convergence_tolerance,
        transformation_validation = transformation_validation,
        model_selection = model_selection,
        bootstrap_ci = bootstrap_ci,
        bootstrap_samples = bootstrap_samples,
        show_model_summary = show_model_summary,
        show_coefficients = show_coefficients,
        show_transformation = show_transformation,
        show_diagnostics = show_diagnostics,
        show_comparison = show_comparison,
        show_transformation_plots = show_transformation_plots,
        show_residual_plots = show_residual_plots,
        show_survival_plots = show_survival_plots,
        show_qq_plots = show_qq_plots,
        showSummaries = showSummaries,
        showExplanations = showExplanations)

    analysis <- transformationmodelsClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

