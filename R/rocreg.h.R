
# This file is automatically generated, you probably don't want to edit this

rocregOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "rocregOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            predictor = NULL,
            outcome = NULL,
            positive_level = NULL,
            covariates = NULL,
            covariate_effects = "both",
            method = "binormal",
            adjusted_auc = TRUE,
            covariate_specific_auc = TRUE,
            covariate_profiles = "",
            compare_unadjusted = TRUE,
            test_covariate_effects = TRUE,
            variable_selection = FALSE,
            selection_criterion = "aic",
            test_interactions = FALSE,
            confidence_intervals = TRUE,
            ci_method = "bootstrap",
            bootstrap_samples = 500,
            confidence_level = 0.95,
            plot_covariate_specific_rocs = TRUE,
            plot_auc_by_covariate = TRUE,
            plot_effect_estimates = TRUE,
            plot_residual_diagnostics = FALSE,
            adjustment_purpose = "confounder",
            clinical_application = "general",
            show_interpretation = TRUE,
            standardize_covariates = TRUE,
            missing_handling = "complete",
            random_seed = 123, ...) {

            super$initialize(
                package="ClinicoPath",
                name="rocreg",
                requiresData=TRUE,
                ...)

            private$..predictor <- jmvcore::OptionVariable$new(
                "predictor",
                predictor,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..outcome <- jmvcore::OptionVariable$new(
                "outcome",
                outcome,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor"))
            private$..positive_level <- jmvcore::OptionLevel$new(
                "positive_level",
                positive_level,
                variable="(outcome)")
            private$..covariates <- jmvcore::OptionVariables$new(
                "covariates",
                covariates,
                suggested=list(
                    "continuous",
                    "nominal"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..covariate_effects <- jmvcore::OptionList$new(
                "covariate_effects",
                covariate_effects,
                options=list(
                    "both",
                    "sensitivity",
                    "specificity"),
                default="both")
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "binormal",
                    "nonparametric",
                    "semiparametric",
                    "bayesian"),
                default="binormal")
            private$..adjusted_auc <- jmvcore::OptionBool$new(
                "adjusted_auc",
                adjusted_auc,
                default=TRUE)
            private$..covariate_specific_auc <- jmvcore::OptionBool$new(
                "covariate_specific_auc",
                covariate_specific_auc,
                default=TRUE)
            private$..covariate_profiles <- jmvcore::OptionString$new(
                "covariate_profiles",
                covariate_profiles,
                default="")
            private$..compare_unadjusted <- jmvcore::OptionBool$new(
                "compare_unadjusted",
                compare_unadjusted,
                default=TRUE)
            private$..test_covariate_effects <- jmvcore::OptionBool$new(
                "test_covariate_effects",
                test_covariate_effects,
                default=TRUE)
            private$..variable_selection <- jmvcore::OptionBool$new(
                "variable_selection",
                variable_selection,
                default=FALSE)
            private$..selection_criterion <- jmvcore::OptionList$new(
                "selection_criterion",
                selection_criterion,
                options=list(
                    "aic",
                    "bic",
                    "forward",
                    "backward"),
                default="aic")
            private$..test_interactions <- jmvcore::OptionBool$new(
                "test_interactions",
                test_interactions,
                default=FALSE)
            private$..confidence_intervals <- jmvcore::OptionBool$new(
                "confidence_intervals",
                confidence_intervals,
                default=TRUE)
            private$..ci_method <- jmvcore::OptionList$new(
                "ci_method",
                ci_method,
                options=list(
                    "bootstrap",
                    "asymptotic"),
                default="bootstrap")
            private$..bootstrap_samples <- jmvcore::OptionInteger$new(
                "bootstrap_samples",
                bootstrap_samples,
                default=500,
                min=100,
                max=5000)
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                default=0.95,
                min=0.8,
                max=0.99)
            private$..plot_covariate_specific_rocs <- jmvcore::OptionBool$new(
                "plot_covariate_specific_rocs",
                plot_covariate_specific_rocs,
                default=TRUE)
            private$..plot_auc_by_covariate <- jmvcore::OptionBool$new(
                "plot_auc_by_covariate",
                plot_auc_by_covariate,
                default=TRUE)
            private$..plot_effect_estimates <- jmvcore::OptionBool$new(
                "plot_effect_estimates",
                plot_effect_estimates,
                default=TRUE)
            private$..plot_residual_diagnostics <- jmvcore::OptionBool$new(
                "plot_residual_diagnostics",
                plot_residual_diagnostics,
                default=FALSE)
            private$..adjustment_purpose <- jmvcore::OptionList$new(
                "adjustment_purpose",
                adjustment_purpose,
                options=list(
                    "confounder",
                    "batch",
                    "subgroup",
                    "multicenter"),
                default="confounder")
            private$..clinical_application <- jmvcore::OptionList$new(
                "clinical_application",
                clinical_application,
                options=list(
                    "general",
                    "digital_path",
                    "biomarker",
                    "imaging",
                    "multicenter_trial"),
                default="general")
            private$..show_interpretation <- jmvcore::OptionBool$new(
                "show_interpretation",
                show_interpretation,
                default=TRUE)
            private$..standardize_covariates <- jmvcore::OptionBool$new(
                "standardize_covariates",
                standardize_covariates,
                default=TRUE)
            private$..missing_handling <- jmvcore::OptionList$new(
                "missing_handling",
                missing_handling,
                options=list(
                    "complete",
                    "imputation"),
                default="complete")
            private$..random_seed <- jmvcore::OptionInteger$new(
                "random_seed",
                random_seed,
                default=123,
                min=1,
                max=999999)

            self$.addOption(private$..predictor)
            self$.addOption(private$..outcome)
            self$.addOption(private$..positive_level)
            self$.addOption(private$..covariates)
            self$.addOption(private$..covariate_effects)
            self$.addOption(private$..method)
            self$.addOption(private$..adjusted_auc)
            self$.addOption(private$..covariate_specific_auc)
            self$.addOption(private$..covariate_profiles)
            self$.addOption(private$..compare_unadjusted)
            self$.addOption(private$..test_covariate_effects)
            self$.addOption(private$..variable_selection)
            self$.addOption(private$..selection_criterion)
            self$.addOption(private$..test_interactions)
            self$.addOption(private$..confidence_intervals)
            self$.addOption(private$..ci_method)
            self$.addOption(private$..bootstrap_samples)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..plot_covariate_specific_rocs)
            self$.addOption(private$..plot_auc_by_covariate)
            self$.addOption(private$..plot_effect_estimates)
            self$.addOption(private$..plot_residual_diagnostics)
            self$.addOption(private$..adjustment_purpose)
            self$.addOption(private$..clinical_application)
            self$.addOption(private$..show_interpretation)
            self$.addOption(private$..standardize_covariates)
            self$.addOption(private$..missing_handling)
            self$.addOption(private$..random_seed)
        }),
    active = list(
        predictor = function() private$..predictor$value,
        outcome = function() private$..outcome$value,
        positive_level = function() private$..positive_level$value,
        covariates = function() private$..covariates$value,
        covariate_effects = function() private$..covariate_effects$value,
        method = function() private$..method$value,
        adjusted_auc = function() private$..adjusted_auc$value,
        covariate_specific_auc = function() private$..covariate_specific_auc$value,
        covariate_profiles = function() private$..covariate_profiles$value,
        compare_unadjusted = function() private$..compare_unadjusted$value,
        test_covariate_effects = function() private$..test_covariate_effects$value,
        variable_selection = function() private$..variable_selection$value,
        selection_criterion = function() private$..selection_criterion$value,
        test_interactions = function() private$..test_interactions$value,
        confidence_intervals = function() private$..confidence_intervals$value,
        ci_method = function() private$..ci_method$value,
        bootstrap_samples = function() private$..bootstrap_samples$value,
        confidence_level = function() private$..confidence_level$value,
        plot_covariate_specific_rocs = function() private$..plot_covariate_specific_rocs$value,
        plot_auc_by_covariate = function() private$..plot_auc_by_covariate$value,
        plot_effect_estimates = function() private$..plot_effect_estimates$value,
        plot_residual_diagnostics = function() private$..plot_residual_diagnostics$value,
        adjustment_purpose = function() private$..adjustment_purpose$value,
        clinical_application = function() private$..clinical_application$value,
        show_interpretation = function() private$..show_interpretation$value,
        standardize_covariates = function() private$..standardize_covariates$value,
        missing_handling = function() private$..missing_handling$value,
        random_seed = function() private$..random_seed$value),
    private = list(
        ..predictor = NA,
        ..outcome = NA,
        ..positive_level = NA,
        ..covariates = NA,
        ..covariate_effects = NA,
        ..method = NA,
        ..adjusted_auc = NA,
        ..covariate_specific_auc = NA,
        ..covariate_profiles = NA,
        ..compare_unadjusted = NA,
        ..test_covariate_effects = NA,
        ..variable_selection = NA,
        ..selection_criterion = NA,
        ..test_interactions = NA,
        ..confidence_intervals = NA,
        ..ci_method = NA,
        ..bootstrap_samples = NA,
        ..confidence_level = NA,
        ..plot_covariate_specific_rocs = NA,
        ..plot_auc_by_covariate = NA,
        ..plot_effect_estimates = NA,
        ..plot_residual_diagnostics = NA,
        ..adjustment_purpose = NA,
        ..clinical_application = NA,
        ..show_interpretation = NA,
        ..standardize_covariates = NA,
        ..missing_handling = NA,
        ..random_seed = NA)
)

rocregResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "rocregResults",
    inherit = jmvcore::Group,
    active = list(
        instructions = function() private$.items[["instructions"]],
        adjustedAUC = function() private$.items[["adjustedAUC"]],
        covariateEffects = function() private$.items[["covariateEffects"]],
        covariateSpecificAUC = function() private$.items[["covariateSpecificAUC"]],
        covariateROCPlot = function() private$.items[["covariateROCPlot"]],
        aucByCovPlot = function() private$.items[["aucByCovPlot"]],
        effectEstimatesPlot = function() private$.items[["effectEstimatesPlot"]],
        stratifiedSummary = function() private$.items[["stratifiedSummary"]],
        groupSpecificAUC = function() private$.items[["groupSpecificAUC"]],
        interpretation = function() private$.items[["interpretation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="ROC Regression (Covariate-Adjusted ROC)")
            self$add(jmvcore::Html$new(
                options=options,
                name="instructions",
                title="Instructions",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="adjustedAUC",
                title="Covariate-Adjusted AUC",
                visible="(adjusted_auc)",
                rows=1,
                columns=list(
                    list(
                        `name`="auc_adjusted", 
                        `title`="Adjusted AUC", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="auc_unadjusted", 
                        `title`="Unadjusted AUC", 
                        `type`="number", 
                        `format`="zto", 
                        `visible`="(compare_unadjusted)"),
                    list(
                        `name`="auc_se", 
                        `title`="SE", 
                        `type`="number"),
                    list(
                        `name`="auc_ci_lower", 
                        `title`="95% CI Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="auc_ci_upper", 
                        `title`="95% CI Upper", 
                        `type`="number", 
                        `format`="zto"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="covariateEffects",
                title="Covariate Effects on ROC",
                visible="(test_covariate_effects)",
                columns=list(
                    list(
                        `name`="covariate", 
                        `title`="Covariate", 
                        `type`="text"),
                    list(
                        `name`="effect_location", 
                        `title`="Effect on Location", 
                        `type`="number"),
                    list(
                        `name`="effect_scale", 
                        `title`="Effect on Scale", 
                        `type`="number"),
                    list(
                        `name`="chi_square", 
                        `title`="Chi-Square", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="P-value", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="covariateSpecificAUC",
                title="Covariate-Specific AUC",
                visible="(covariate_specific_auc)",
                columns=list(
                    list(
                        `name`="profile", 
                        `title`="Covariate Profile", 
                        `type`="text"),
                    list(
                        `name`="auc", 
                        `title`="AUC", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="auc_ci_lower", 
                        `title`="95% CI Lower", 
                        `type`="number", 
                        `format`="zto"),
                    list(
                        `name`="auc_ci_upper", 
                        `title`="95% CI Upper", 
                        `type`="number", 
                        `format`="zto"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="covariateROCPlot",
                title="Covariate-Specific ROC Curves",
                width=600,
                height=500,
                renderFun=".plotCovariateROCs",
                visible="(plot_covariate_specific_rocs)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="aucByCovPlot",
                title="AUC vs Covariate",
                width=600,
                height=500,
                renderFun=".plotAUCByCovariate",
                visible="(plot_auc_by_covariate)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="effectEstimatesPlot",
                title="Covariate Effect Estimates",
                width=600,
                height=500,
                renderFun=".plotEffectEstimates",
                visible="(plot_effect_estimates)"))
            self$add(jmvcore::Table$new(
                options=options,
                name="stratifiedSummary",
                title="Stratified ROC Summary",
                visible=TRUE,
                columns=list(
                    list(
                        `name`="covariate", 
                        `title`="Covariate", 
                        `type`="text"),
                    list(
                        `name`="n_groups", 
                        `title`="N Groups", 
                        `type`="integer"),
                    list(
                        `name`="auc_range_min", 
                        `title`="Min AUC", 
                        `type`="number"),
                    list(
                        `name`="auc_range_max", 
                        `title`="Max AUC", 
                        `type`="number"),
                    list(
                        `name`="pooled_auc", 
                        `title`="Pooled AUC", 
                        `type`="number"),
                    list(
                        `name`="q_statistic", 
                        `title`="Q Statistic", 
                        `type`="number"),
                    list(
                        `name`="df", 
                        `title`="df", 
                        `type`="integer"),
                    list(
                        `name`="p_homogeneity", 
                        `title`="P(Homogeneity)", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="heterogeneous", 
                        `title`="Heterogeneous", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="groupSpecificAUC",
                title="Group-Specific AUC",
                visible=TRUE,
                columns=list(
                    list(
                        `name`="covariate", 
                        `title`="Covariate", 
                        `type`="text", 
                        `combineBelow`=TRUE),
                    list(
                        `name`="group", 
                        `title`="Group", 
                        `type`="text"),
                    list(
                        `name`="n_diseased", 
                        `title`="N Diseased", 
                        `type`="integer"),
                    list(
                        `name`="n_healthy", 
                        `title`="N Healthy", 
                        `type`="integer"),
                    list(
                        `name`="auc", 
                        `title`="AUC", 
                        `type`="number"),
                    list(
                        `name`="auc_se", 
                        `title`="SE", 
                        `type`="number"),
                    list(
                        `name`="ci_lower", 
                        `title`="Lower CI", 
                        `type`="number"),
                    list(
                        `name`="ci_upper", 
                        `title`="Upper CI", 
                        `type`="number"))))
            self$add(jmvcore::Html$new(
                options=options,
                name="interpretation",
                title="Clinical Interpretation",
                visible="(show_interpretation)"))}))

rocregBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "rocregBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "rocreg",
                version = c(0,0,32),
                options = options,
                results = rocregResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' ROC Regression (Covariate-Adjusted ROC)
#'
#' ROC regression for covariate-adjusted diagnostic accuracy assessment. 
#' Traditional ROC analysis assumes diagnostic test performance is constant 
#' across all patients, but accuracy often varies by patient characteristics 
#' (age, disease stage, scanner type, staining protocol). ROC regression 
#' models how sensitivity and specificity change as functions of covariates, 
#' providing covariate-specific ROC curves and adjusted summary measures 
#' (covariate-adjusted AUC). Essential for comparing diagnostic tests across 
#' heterogeneous populations, adjusting for confounders in test comparison 
#' studies, and identifying patient subgroups where tests perform differently. 
#' Particularly valuable in digital pathology for accounting for batch effects 
#' (different scanners, institutions, staining protocols) and in biomarker 
#' studies for age/stage-specific performance assessment. Uses binormal, 
#' non-parametric, or Bayesian approaches to model covariate effects on ROC 
#' curves. Provides both population-averaged and covariate-specific diagnostic 
#' accuracy estimates.
#' 
#'
#' @examples
#' result <- rocreg(
#'     data = multi_center_data,
#'     predictor = "biomarker_level",
#'     outcome = "disease_status",
#'     covariates = c("age", "scanner_type", "center")
#' )
#'
#' @param data The data as a data frame.
#' @param predictor Continuous diagnostic test variable (biomarker level,
#'   imaging score, AI probability). The test whose accuracy varies by
#'   covariates.
#' @param outcome Binary outcome variable (diseased vs non-diseased, positive
#'   vs negative).
#' @param positive_level Which level represents the diseased/positive state.
#' @param covariates Variables that may affect diagnostic accuracy. Examples:
#'   age, sex, disease stage, scanner type, institution, staining batch. Both
#'   continuous and categorical covariates supported.
#' @param covariate_effects Where to model covariate effects. "Both" allows
#'   different effects in diseased and non-diseased groups, "sensitivity" models
#'   effects only in diseased, "specificity" only in non-diseased.
#' @param method Method for ROC regression. Binormal assumes normal
#'   distributions with covariate effects on location/scale, non-parametric uses
#'   kernel smoothing, semi-parametric combines both, Bayesian provides
#'   posterior distributions.
#' @param adjusted_auc Calculate population-averaged AUC adjusted for
#'   covariates. This is the overall AUC accounting for covariate distribution
#'   in the population.
#' @param covariate_specific_auc Calculate AUC at specific covariate values to
#'   show how diagnostic accuracy varies across patient characteristics.
#' @param covariate_profiles Specify covariate values for covariate-specific
#'   ROC curves. Example for age: "30, 50, 70" to get curves for
#'   young/middle/elderly patients. Leave empty for automatic selection
#'   (quartiles for continuous, all levels for categorical).
#' @param compare_unadjusted Show comparison between covariate-adjusted and
#'   unadjusted (pooled) ROC analysis. Demonstrates impact of covariate
#'   adjustment.
#' @param test_covariate_effects Perform statistical tests to determine if
#'   covariates significantly affect diagnostic accuracy. Likelihood ratio tests
#'   or Wald tests depending on method.
#' @param variable_selection Use stepwise or information criteria to select
#'   which covariates to include in final model. Helpful with many potential
#'   confounders.
#' @param selection_criterion Criterion for automatic variable selection.
#' @param test_interactions Test for interactions between covariates (e.g.,
#'   does age effect differ by sex? does scanner effect vary by institution?).
#' @param confidence_intervals Calculate confidence intervals for
#'   covariate-adjusted AUC and ROC curves.
#' @param ci_method Method for confidence interval estimation. Bootstrap
#'   recommended for small samples or non-parametric methods.
#' @param bootstrap_samples Number of bootstrap samples for CI estimation.
#' @param confidence_level Confidence level for intervals.
#' @param plot_covariate_specific_rocs Display multiple ROC curves for
#'   different covariate profiles on same plot. Shows how ROC curve changes with
#'   patient characteristics.
#' @param plot_auc_by_covariate For continuous covariates, plot how AUC
#'   changes as covariate varies. Shows non-linear relationships between
#'   covariates and diagnostic accuracy.
#' @param plot_effect_estimates Forest plot showing estimated covariate
#'   effects on diagnostic accuracy with confidence intervals.
#' @param plot_residual_diagnostics Display residual plots and goodness-of-fit
#'   diagnostics for ROC regression model.
#' @param adjustment_purpose Purpose of covariate adjustment for
#'   interpretation guidance.
#' @param clinical_application Clinical application context for
#'   interpretation.
#' @param show_interpretation Provide interpretation with clinical context and
#'   recommendations.
#' @param standardize_covariates Standardize continuous covariates (mean 0, SD
#'   1) for better numerical stability and interpretability of effect sizes.
#' @param missing_handling Method for handling missing covariate data.
#' @param random_seed Random seed for bootstrap and stochastic procedures.
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$instructions} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$adjustedAUC} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$covariateEffects} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$covariateSpecificAUC} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$covariateROCPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$aucByCovPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$effectEstimatesPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$stratifiedSummary} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$groupSpecificAUC} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$interpretation} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$adjustedAUC$asDF}
#'
#' \code{as.data.frame(results$adjustedAUC)}
#'
#' @export
rocreg <- function(
    data,
    predictor,
    outcome,
    positive_level,
    covariates,
    covariate_effects = "both",
    method = "binormal",
    adjusted_auc = TRUE,
    covariate_specific_auc = TRUE,
    covariate_profiles = "",
    compare_unadjusted = TRUE,
    test_covariate_effects = TRUE,
    variable_selection = FALSE,
    selection_criterion = "aic",
    test_interactions = FALSE,
    confidence_intervals = TRUE,
    ci_method = "bootstrap",
    bootstrap_samples = 500,
    confidence_level = 0.95,
    plot_covariate_specific_rocs = TRUE,
    plot_auc_by_covariate = TRUE,
    plot_effect_estimates = TRUE,
    plot_residual_diagnostics = FALSE,
    adjustment_purpose = "confounder",
    clinical_application = "general",
    show_interpretation = TRUE,
    standardize_covariates = TRUE,
    missing_handling = "complete",
    random_seed = 123) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("rocreg requires jmvcore to be installed (restart may be required)")

    if ( ! missing(predictor)) predictor <- jmvcore::resolveQuo(jmvcore::enquo(predictor))
    if ( ! missing(outcome)) outcome <- jmvcore::resolveQuo(jmvcore::enquo(outcome))
    if ( ! missing(covariates)) covariates <- jmvcore::resolveQuo(jmvcore::enquo(covariates))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(predictor), predictor, NULL),
            `if`( ! missing(outcome), outcome, NULL),
            `if`( ! missing(covariates), covariates, NULL))

    for (v in outcome) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- rocregOptions$new(
        predictor = predictor,
        outcome = outcome,
        positive_level = positive_level,
        covariates = covariates,
        covariate_effects = covariate_effects,
        method = method,
        adjusted_auc = adjusted_auc,
        covariate_specific_auc = covariate_specific_auc,
        covariate_profiles = covariate_profiles,
        compare_unadjusted = compare_unadjusted,
        test_covariate_effects = test_covariate_effects,
        variable_selection = variable_selection,
        selection_criterion = selection_criterion,
        test_interactions = test_interactions,
        confidence_intervals = confidence_intervals,
        ci_method = ci_method,
        bootstrap_samples = bootstrap_samples,
        confidence_level = confidence_level,
        plot_covariate_specific_rocs = plot_covariate_specific_rocs,
        plot_auc_by_covariate = plot_auc_by_covariate,
        plot_effect_estimates = plot_effect_estimates,
        plot_residual_diagnostics = plot_residual_diagnostics,
        adjustment_purpose = adjustment_purpose,
        clinical_application = clinical_application,
        show_interpretation = show_interpretation,
        standardize_covariates = standardize_covariates,
        missing_handling = missing_handling,
        random_seed = random_seed)

    analysis <- rocregClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

