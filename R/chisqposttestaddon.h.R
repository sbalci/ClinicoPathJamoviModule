
# This file is automatically generated, you probably don't want to edit this

chisqposttestaddonOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "chisqposttestaddonOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function( ...) {

            super$initialize(
                package="ClinicoPath",
                name="chisqposttestaddon",
                requiresData=FALSE,
                ...)


        }),
    active = list(),
    private = list()
)

chisqposttestaddonResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "chisqposttestaddonResults",
    inherit = jmvcore::Group,
    active = list(),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Chi-Square Post-Hoc Analysis")}))

chisqposttestaddonBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "chisqposttestaddonBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "chisqposttestaddon",
                version = c(0,0,31),
                options = options,
                results = chisqposttestaddonResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'na')
        }))

