
# This file is automatically generated, you probably don't want to edit this

crosstablepivotOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "crosstablepivotOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            vars = NULL,
            group = NULL,
            statistics = TRUE,
            show_totals = TRUE,
            format_style = "clinical",
            export_excel = FALSE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="crosstablepivot",
                requiresData=TRUE,
                ...)

            private$..vars <- jmvcore::OptionVariables$new(
                "vars",
                vars,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor",
                    "numeric",
                    "id"))
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor",
                    "numeric",
                    "id"))
            private$..statistics <- jmvcore::OptionBool$new(
                "statistics",
                statistics,
                default=TRUE)
            private$..show_totals <- jmvcore::OptionBool$new(
                "show_totals",
                show_totals,
                default=TRUE)
            private$..format_style <- jmvcore::OptionList$new(
                "format_style",
                format_style,
                options=list(
                    "standard",
                    "clinical",
                    "publication"),
                default="clinical")
            private$..export_excel <- jmvcore::OptionBool$new(
                "export_excel",
                export_excel,
                default=FALSE)

            self$.addOption(private$..vars)
            self$.addOption(private$..group)
            self$.addOption(private$..statistics)
            self$.addOption(private$..show_totals)
            self$.addOption(private$..format_style)
            self$.addOption(private$..export_excel)
        }),
    active = list(
        vars = function() private$..vars$value,
        group = function() private$..group$value,
        statistics = function() private$..statistics$value,
        show_totals = function() private$..show_totals$value,
        format_style = function() private$..format_style$value,
        export_excel = function() private$..export_excel$value),
    private = list(
        ..vars = NA,
        ..group = NA,
        ..statistics = NA,
        ..show_totals = NA,
        ..format_style = NA,
        ..export_excel = NA)
)

crosstablepivotResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "crosstablepivotResults",
    inherit = jmvcore::Group,
    active = list(
        instructions = function() private$.items[["instructions"]],
        pivot_table = function() private$.items[["pivot_table"]],
        summary_stats = function() private$.items[["summary_stats"]],
        export_info = function() private$.items[["export_info"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Enhanced Cross Tables (Pivot)")
            self$add(jmvcore::Html$new(
                options=options,
                name="instructions",
                title="Instructions"))
            self$add(jmvcore::Table$new(
                options=options,
                name="pivot_table",
                title="Pivot Cross Table",
                clearWith=list(
                    "vars",
                    "group",
                    "statistics",
                    "show_totals",
                    "format_style"),
                columns=list(
                    list(
                        `name`="variable", 
                        `title`="Variable", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="summary_stats",
                title="Summary Statistics",
                visible="(statistics)",
                clearWith=list(
                    "vars",
                    "group",
                    "statistics"),
                columns=list(
                    list(
                        `name`="variable", 
                        `title`="Variable", 
                        `type`="text"),
                    list(
                        `name`="statistic", 
                        `title`="Statistic", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number"))))
            self$add(jmvcore::Html$new(
                options=options,
                name="export_info",
                title="Export Information",
                visible="(export_excel)"))}))

crosstablepivotBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "crosstablepivotBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "crosstablepivot",
                version = c(0,0,3),
                options = options,
                results = crosstablepivotResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Enhanced Cross Tables (Pivot)
#'
#' Enhanced cross tables using pivottabler for flexible clinical data 
#' analysis. Provides pivot-style layouts with advanced statistics and 
#' professional formatting. Non-breaking addition to existing crosstable 
#' functionality.
#' 
#' @param data The data as a data frame.
#' @param vars The variables to be placed in table rows.
#' @param group The variable used for grouping (table columns).
#' @param statistics Include count and percentage statistics.
#' @param show_totals Include row and column totals.
#' @param format_style Table formatting style.
#' @param export_excel Enable Excel export functionality.
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$instructions} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$pivot_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$summary_stats} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$export_info} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$pivot_table$asDF}
#'
#' \code{as.data.frame(results$pivot_table)}
#'
#' @export
crosstablepivot <- function(
    data,
    vars,
    group,
    statistics = TRUE,
    show_totals = TRUE,
    format_style = "clinical",
    export_excel = FALSE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("crosstablepivot requires jmvcore to be installed (restart may be required)")

    if ( ! missing(vars)) vars <- jmvcore::resolveQuo(jmvcore::enquo(vars))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(vars), vars, NULL),
            `if`( ! missing(group), group, NULL))


    options <- crosstablepivotOptions$new(
        vars = vars,
        group = group,
        statistics = statistics,
        show_totals = show_totals,
        format_style = format_style,
        export_excel = export_excel)

    analysis <- crosstablepivotClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

