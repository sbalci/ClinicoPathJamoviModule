
# This file is automatically generated, you probably don't want to edit this

jcorrelationOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jcorrelationOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            vars = NULL,
            group = NULL,
            method = "pearson",
            alternative = "two.sided",
            ci = TRUE,
            ciWidth = 95,
            flag = TRUE,
            flagAlpha = 0.05,
            plots = TRUE,
            plotType = "matrix",
            report = TRUE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="jcorrelation",
                requiresData=TRUE,
                ...)

            private$..vars <- jmvcore::OptionVariables$new(
                "vars",
                vars,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group,
                suggested=list(
                    "nominal",
                    "ordinal"),
                permitted=list(
                    "factor"),
                default=NULL)
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "pearson",
                    "spearman",
                    "kendall"),
                default="pearson")
            private$..alternative <- jmvcore::OptionList$new(
                "alternative",
                alternative,
                options=list(
                    "two.sided",
                    "greater",
                    "less"),
                default="two.sided")
            private$..ci <- jmvcore::OptionBool$new(
                "ci",
                ci,
                default=TRUE)
            private$..ciWidth <- jmvcore::OptionNumber$new(
                "ciWidth",
                ciWidth,
                min=50,
                max=99.9,
                default=95)
            private$..flag <- jmvcore::OptionBool$new(
                "flag",
                flag,
                default=TRUE)
            private$..flagAlpha <- jmvcore::OptionNumber$new(
                "flagAlpha",
                flagAlpha,
                min=0.001,
                max=0.1,
                default=0.05)
            private$..plots <- jmvcore::OptionBool$new(
                "plots",
                plots,
                default=TRUE)
            private$..plotType <- jmvcore::OptionList$new(
                "plotType",
                plotType,
                options=list(
                    "matrix",
                    "pairs",
                    "network"),
                default="matrix")
            private$..report <- jmvcore::OptionBool$new(
                "report",
                report,
                default=TRUE)

            self$.addOption(private$..vars)
            self$.addOption(private$..group)
            self$.addOption(private$..method)
            self$.addOption(private$..alternative)
            self$.addOption(private$..ci)
            self$.addOption(private$..ciWidth)
            self$.addOption(private$..flag)
            self$.addOption(private$..flagAlpha)
            self$.addOption(private$..plots)
            self$.addOption(private$..plotType)
            self$.addOption(private$..report)
        }),
    active = list(
        vars = function() private$..vars$value,
        group = function() private$..group$value,
        method = function() private$..method$value,
        alternative = function() private$..alternative$value,
        ci = function() private$..ci$value,
        ciWidth = function() private$..ciWidth$value,
        flag = function() private$..flag$value,
        flagAlpha = function() private$..flagAlpha$value,
        plots = function() private$..plots$value,
        plotType = function() private$..plotType$value,
        report = function() private$..report$value),
    private = list(
        ..vars = NA,
        ..group = NA,
        ..method = NA,
        ..alternative = NA,
        ..ci = NA,
        ..ciWidth = NA,
        ..flag = NA,
        ..flagAlpha = NA,
        ..plots = NA,
        ..plotType = NA,
        ..report = NA)
)

jcorrelationResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jcorrelationResults",
    inherit = jmvcore::Group,
    active = list(
        matrix = function() private$.items[["matrix"]],
        tests = function() private$.items[["tests"]],
        summary = function() private$.items[["summary"]],
        report = function() private$.items[["report"]],
        plot = function() private$.items[["plot"]],
        plotMatrix = function() private$.items[["plotMatrix"]],
        plotPairs = function() private$.items[["plotPairs"]],
        plotNetwork = function() private$.items[["plotNetwork"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Correlation Analysis",
                refs=list(
                    "correlation",
                    "report",
                    "corrplot",
                    "GGally",
                    "qgraph",
                    "igraph",
                    "ggraph",
                    "ClinicoPathJamoviModule"))
            self$add(jmvcore::Table$new(
                options=options,
                name="matrix",
                title="Correlation Matrix",
                columns=list(),
                clearWith=list(
                    "vars",
                    "group",
                    "method",
                    "alternative",
                    "ci",
                    "ciWidth")))
            self$add(jmvcore::Table$new(
                options=options,
                name="tests",
                title="Pairwise Correlations",
                columns=list(),
                clearWith=list(
                    "vars",
                    "group",
                    "method",
                    "alternative",
                    "ci",
                    "ciWidth")))
            self$add(jmvcore::Table$new(
                options=options,
                name="summary",
                title="Summary Statistics",
                visible=TRUE,
                columns=list(
                    list(
                        `name`="stat", 
                        `title`="Statistic", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto"))))
            self$add(jmvcore::Html$new(
                options=options,
                name="report",
                title="Natural Language Report",
                visible="(report)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Correlation Plot",
                width=600,
                height=450,
                renderFun=".plot",
                visible="(plots)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plotMatrix",
                title="Correlation Matrix Plot",
                width=600,
                height=450,
                renderFun=".plotMatrix",
                visible="(plots && plotType:matrix)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plotPairs",
                title="Pairs Plot",
                width=800,
                height=600,
                renderFun=".plotPairs",
                visible="(plots && plotType:pairs)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plotNetwork",
                title="Network Plot",
                width=600,
                height=600,
                renderFun=".plotNetwork",
                visible="(plots && plotType:network)"))}))

jcorrelationBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jcorrelationBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "jcorrelation",
                version = c(0,0,3),
                options = options,
                results = jcorrelationResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = TRUE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Correlation Analysis
#'
#' Comprehensive correlation analysis including Pearson, Spearman, and Kendall  
#' correlations with significance tests, confidence intervals, and natural 
#' language  reporting. Suitable for exploring relationships between 
#' continuous variables.
#' 
#'
#' @examples
#' # Basic correlation analysis
#' jcorrelation(
#'     data = histopathology,
#'     vars = c("Age", "OverallTime", "MeasurementA", "MeasurementB")
#' )
#'
#' # With grouping variable
#' jcorrelation(
#'     data = histopathology,
#'     vars = c("Age", "OverallTime", "MeasurementA"),
#'     group = "Sex"
#' )
#'
#' @param data The data as a data frame.
#' @param vars A vector of strings naming the variables to correlate.
#' @param group Variable to split the analysis by.
#' @param method The correlation method to use: 'pearson' (default),
#'   'spearman', or 'kendall'.
#' @param alternative The alternative hypothesis: 'two.sided' (default),
#'   'greater', or 'less'.
#' @param ci TRUE (default) or FALSE, provide confidence intervals.
#' @param ciWidth Confidence interval level (default: 95\%).
#' @param flag TRUE (default) or FALSE, flag significant correlations.
#' @param flagAlpha Alpha level for flagging significant correlations
#'   (default: 0.05).
#' @param plots TRUE (default) or FALSE, provide correlation plots.
#' @param plotType Type of correlation plot: 'matrix' (default), 'pairs', or
#'   'network'.
#' @param report TRUE (default) or FALSE, provide natural language
#'   interpretation.
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$matrix} \tab \tab \tab \tab \tab correlation matrix with significance tests \cr
#'   \code{results$tests} \tab \tab \tab \tab \tab detailed correlation tests for each pair of variables \cr
#'   \code{results$summary} \tab \tab \tab \tab \tab summary of correlation analysis \cr
#'   \code{results$report} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plotMatrix} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plotPairs} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plotNetwork} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$matrix$asDF}
#'
#' \code{as.data.frame(results$matrix)}
#'
#' @export
jcorrelation <- function(
    data,
    vars,
    group = NULL,
    method = "pearson",
    alternative = "two.sided",
    ci = TRUE,
    ciWidth = 95,
    flag = TRUE,
    flagAlpha = 0.05,
    plots = TRUE,
    plotType = "matrix",
    report = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("jcorrelation requires jmvcore to be installed (restart may be required)")

    if ( ! missing(vars)) vars <- jmvcore::resolveQuo(jmvcore::enquo(vars))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(vars), vars, NULL),
            `if`( ! missing(group), group, NULL))

    for (v in group) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- jcorrelationOptions$new(
        vars = vars,
        group = group,
        method = method,
        alternative = alternative,
        ci = ci,
        ciWidth = ciWidth,
        flag = flag,
        flagAlpha = flagAlpha,
        plots = plots,
        plotType = plotType,
        report = report)

    analysis <- jcorrelationClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

