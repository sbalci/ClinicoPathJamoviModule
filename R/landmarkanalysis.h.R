
# This file is automatically generated, you probably don't want to edit this

landmarkanalysisOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "landmarkanalysisOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            time = NULL,
            status = NULL,
            predictors = NULL,
            landmark_times = "6, 12, 24",
            prediction_window = 12,
            min_events = 10,
            include_baseline = TRUE,
            dynamic_prediction = TRUE,
            calibration_plot = TRUE,
            discrimination_plot = TRUE,
            supermodel = FALSE,
            bootstrap_validation = FALSE,
            n_bootstrap = 200, ...) {

            super$initialize(
                package="ClinicoPath",
                name="landmarkanalysis",
                requiresData=TRUE,
                ...)

            private$..time <- jmvcore::OptionVariable$new(
                "time",
                time,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..status <- jmvcore::OptionVariable$new(
                "status",
                status,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..predictors <- jmvcore::OptionVariables$new(
                "predictors",
                predictors,
                suggested=list(
                    "continuous",
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "numeric",
                    "factor"))
            private$..landmark_times <- jmvcore::OptionString$new(
                "landmark_times",
                landmark_times,
                default="6, 12, 24")
            private$..prediction_window <- jmvcore::OptionNumber$new(
                "prediction_window",
                prediction_window,
                default=12,
                min=1)
            private$..min_events <- jmvcore::OptionInteger$new(
                "min_events",
                min_events,
                default=10,
                min=5,
                max=50)
            private$..include_baseline <- jmvcore::OptionBool$new(
                "include_baseline",
                include_baseline,
                default=TRUE)
            private$..dynamic_prediction <- jmvcore::OptionBool$new(
                "dynamic_prediction",
                dynamic_prediction,
                default=TRUE)
            private$..calibration_plot <- jmvcore::OptionBool$new(
                "calibration_plot",
                calibration_plot,
                default=TRUE)
            private$..discrimination_plot <- jmvcore::OptionBool$new(
                "discrimination_plot",
                discrimination_plot,
                default=TRUE)
            private$..supermodel <- jmvcore::OptionBool$new(
                "supermodel",
                supermodel,
                default=FALSE)
            private$..bootstrap_validation <- jmvcore::OptionBool$new(
                "bootstrap_validation",
                bootstrap_validation,
                default=FALSE)
            private$..n_bootstrap <- jmvcore::OptionInteger$new(
                "n_bootstrap",
                n_bootstrap,
                default=200,
                min=50,
                max=1000)

            self$.addOption(private$..time)
            self$.addOption(private$..status)
            self$.addOption(private$..predictors)
            self$.addOption(private$..landmark_times)
            self$.addOption(private$..prediction_window)
            self$.addOption(private$..min_events)
            self$.addOption(private$..include_baseline)
            self$.addOption(private$..dynamic_prediction)
            self$.addOption(private$..calibration_plot)
            self$.addOption(private$..discrimination_plot)
            self$.addOption(private$..supermodel)
            self$.addOption(private$..bootstrap_validation)
            self$.addOption(private$..n_bootstrap)
        }),
    active = list(
        time = function() private$..time$value,
        status = function() private$..status$value,
        predictors = function() private$..predictors$value,
        landmark_times = function() private$..landmark_times$value,
        prediction_window = function() private$..prediction_window$value,
        min_events = function() private$..min_events$value,
        include_baseline = function() private$..include_baseline$value,
        dynamic_prediction = function() private$..dynamic_prediction$value,
        calibration_plot = function() private$..calibration_plot$value,
        discrimination_plot = function() private$..discrimination_plot$value,
        supermodel = function() private$..supermodel$value,
        bootstrap_validation = function() private$..bootstrap_validation$value,
        n_bootstrap = function() private$..n_bootstrap$value),
    private = list(
        ..time = NA,
        ..status = NA,
        ..predictors = NA,
        ..landmark_times = NA,
        ..prediction_window = NA,
        ..min_events = NA,
        ..include_baseline = NA,
        ..dynamic_prediction = NA,
        ..calibration_plot = NA,
        ..discrimination_plot = NA,
        ..supermodel = NA,
        ..bootstrap_validation = NA,
        ..n_bootstrap = NA)
)

landmarkanalysisResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "landmarkanalysisResults",
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        summary = function() private$.items[["summary"]],
        baselineModel = function() private$.items[["baselineModel"]],
        landmarkResults = function() private$.items[["landmarkResults"]],
        modelComparison = function() private$.items[["modelComparison"]],
        dynamicPredictionPlot = function() private$.items[["dynamicPredictionPlot"]],
        calibrationPlot = function() private$.items[["calibrationPlot"]],
        discriminationPlot = function() private$.items[["discriminationPlot"]],
        superModelResults = function() private$.items[["superModelResults"]],
        validationResults = function() private$.items[["validationResults"]],
        clinicalGuidance = function() private$.items[["clinicalGuidance"]],
        technicalNotes = function() private$.items[["technicalNotes"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Landmark Analysis for Time-varying Predictors")
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="Instructions",
                clearWith=list(
                    "time",
                    "status",
                    "predictors")))
            self$add(jmvcore::Html$new(
                options=options,
                name="summary",
                title="Analysis Summary",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="baselineModel",
                title="Baseline Cox Model",
                visible="(include_baseline)",
                rows=1,
                columns=list(
                    list(
                        `name`="parameter", 
                        `title`="Parameter", 
                        `type`="text"),
                    list(
                        `name`="hr", 
                        `title`="Hazard Ratio", 
                        `type`="number"),
                    list(
                        `name`="hr_lower", 
                        `title`="HR Lower", 
                        `type`="number"),
                    list(
                        `name`="hr_upper", 
                        `title`="HR Upper", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="landmarkResults",
                title="Landmark Model Results",
                visible=TRUE,
                rows=1,
                columns=list(
                    list(
                        `name`="landmark_time", 
                        `title`="Landmark Time", 
                        `type`="text"),
                    list(
                        `name`="n_subjects", 
                        `title`="N Subjects", 
                        `type`="integer"),
                    list(
                        `name`="n_events", 
                        `title`="N Events", 
                        `type`="integer"),
                    list(
                        `name`="parameter", 
                        `title`="Parameter", 
                        `type`="text"),
                    list(
                        `name`="hr", 
                        `title`="Hazard Ratio", 
                        `type`="number"),
                    list(
                        `name`="hr_lower", 
                        `title`="HR Lower", 
                        `type`="number"),
                    list(
                        `name`="hr_upper", 
                        `title`="HR Upper", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="modelComparison",
                title="Model Performance Comparison",
                visible=TRUE,
                rows=1,
                columns=list(
                    list(
                        `name`="model", 
                        `title`="Model", 
                        `type`="text"),
                    list(
                        `name`="landmark_time", 
                        `title`="Landmark", 
                        `type`="text"),
                    list(
                        `name`="c_index", 
                        `title`="C-Index", 
                        `type`="number"),
                    list(
                        `name`="c_index_se", 
                        `title`="C-Index SE", 
                        `type`="number"),
                    list(
                        `name`="auc", 
                        `title`="AUC", 
                        `type`="number"),
                    list(
                        `name`="brier_score", 
                        `title`="Brier Score", 
                        `type`="number"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="dynamicPredictionPlot",
                title="Dynamic Risk Prediction",
                width=700,
                height=500,
                renderFun=".plotDynamicRisk",
                visible="(dynamic_prediction)",
                clearWith=list(
                    "time",
                    "status",
                    "predictors",
                    "landmark_times")))
            self$add(jmvcore::Image$new(
                options=options,
                name="calibrationPlot",
                title="Calibration Assessment",
                width=700,
                height=500,
                renderFun=".plotCalibration",
                visible="(calibration_plot)",
                clearWith=list(
                    "time",
                    "status",
                    "predictors",
                    "landmark_times")))
            self$add(jmvcore::Image$new(
                options=options,
                name="discriminationPlot",
                title="Discrimination Over Time",
                width=700,
                height=500,
                renderFun=".plotDiscrimination",
                visible="(discrimination_plot)",
                clearWith=list(
                    "time",
                    "status",
                    "predictors",
                    "landmark_times")))
            self$add(jmvcore::Table$new(
                options=options,
                name="superModelResults",
                title="Super Model Results",
                visible="(supermodel)",
                rows=1,
                columns=list(
                    list(
                        `name`="parameter", 
                        `title`="Parameter", 
                        `type`="text"),
                    list(
                        `name`="hr", 
                        `title`="Hazard Ratio", 
                        `type`="number"),
                    list(
                        `name`="hr_lower", 
                        `title`="HR Lower", 
                        `type`="number"),
                    list(
                        `name`="hr_upper", 
                        `title`="HR Upper", 
                        `type`="number"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Html$new(
                options=options,
                name="validationResults",
                title="Bootstrap Validation",
                visible="(bootstrap_validation)"))
            self$add(jmvcore::Html$new(
                options=options,
                name="clinicalGuidance",
                title="Clinical Interpretation",
                visible=TRUE))
            self$add(jmvcore::Html$new(
                options=options,
                name="technicalNotes",
                title="Technical Notes",
                visible=TRUE))}))

landmarkanalysisBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "landmarkanalysisBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "landmarkanalysis",
                version = c(0,0,3),
                options = options,
                results = landmarkanalysisResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Landmark Analysis for Time-varying Predictors
#'
#' Landmark analysis for survival data with time-varying predictors.
#' Provides unbiased estimates by analyzing survival from fixed landmark 
#' times.
#' 
#'
#' @examples
#' \donttest{
#' # Example usage
#' landmarkanalysis(
#'     data = survival_data,
#'     time = followup_time,
#'     status = event_status,
#'     predictors = c(biomarker, treatment),
#'     landmark_times = c(6, 12, 24)
#' )
#'}
#' @param data The data as a data frame.
#' @param time Follow-up time variable
#' @param status Event status variable
#' @param predictors Predictor variables for landmark analysis
#' @param landmark_times Vector of landmark time points
#' @param prediction_window Length of prediction window from each landmark
#' @param min_events Minimum events threshold for landmark analysis
#' @param include_baseline Whether to include baseline Cox model
#' @param dynamic_prediction Calculate time-dependent risk predictions
#' @param calibration_plot Generate calibration assessment plots
#' @param discrimination_plot Generate discrimination assessment plots
#' @param supermodel Combine landmark models using super model
#' @param bootstrap_validation Use bootstrap for model validation
#' @param n_bootstrap Number of bootstrap replications
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$summary} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$baselineModel} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$landmarkResults} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$modelComparison} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$dynamicPredictionPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$calibrationPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$discriminationPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$superModelResults} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$validationResults} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$clinicalGuidance} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$technicalNotes} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$baselineModel$asDF}
#'
#' \code{as.data.frame(results$baselineModel)}
#'
#' @export
landmarkanalysis <- function(
    data,
    time,
    status,
    predictors,
    landmark_times = "6, 12, 24",
    prediction_window = 12,
    min_events = 10,
    include_baseline = TRUE,
    dynamic_prediction = TRUE,
    calibration_plot = TRUE,
    discrimination_plot = TRUE,
    supermodel = FALSE,
    bootstrap_validation = FALSE,
    n_bootstrap = 200) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("landmarkanalysis requires jmvcore to be installed (restart may be required)")

    if ( ! missing(time)) time <- jmvcore::resolveQuo(jmvcore::enquo(time))
    if ( ! missing(status)) status <- jmvcore::resolveQuo(jmvcore::enquo(status))
    if ( ! missing(predictors)) predictors <- jmvcore::resolveQuo(jmvcore::enquo(predictors))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(time), time, NULL),
            `if`( ! missing(status), status, NULL),
            `if`( ! missing(predictors), predictors, NULL))


    options <- landmarkanalysisOptions$new(
        time = time,
        status = status,
        predictors = predictors,
        landmark_times = landmark_times,
        prediction_window = prediction_window,
        min_events = min_events,
        include_baseline = include_baseline,
        dynamic_prediction = dynamic_prediction,
        calibration_plot = calibration_plot,
        discrimination_plot = discrimination_plot,
        supermodel = supermodel,
        bootstrap_validation = bootstrap_validation,
        n_bootstrap = n_bootstrap)

    analysis <- landmarkanalysisClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

