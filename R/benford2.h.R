
# This file is automatically generated, you probably don't want to edit this

benford2Options <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "benford2Options",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            var = NULL, ...) {

            super$initialize(
                package="ClinicoPath",
                name="benford2",
                requiresData=TRUE,
                ...)

            private$..var <- jmvcore::OptionVariable$new(
                "var",
                var,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))

            self$.addOption(private$..var)
        }),
    active = list(
        var = function() private$..var$value),
    private = list(
        ..var = NA)
)

benford2Results <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "benford2Results",
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        text = function() private$.items[["text"]],
        text2 = function() private$.items[["text2"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Benford Analysis",
                refs=list(
                    "benford.analysis",
                    "ClinicoPathJamoviModule",
                    "glue"))
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="To Do"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Benford Analysis"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text2",
                title="Suspects"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Benford Analysis",
                width=600,
                height=450,
                renderFun=".plot",
                requiresData=TRUE))}))

benford2Base <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "benford2Base",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "benford2",
                version = c(0,0,3),
                options = options,
                results = benford2Results$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Benford Analysis 2
#'
#' 
#' @param data The data as a data frame.
#' @param var a string naming the variable from \code{data} that contains the
#'   continuous values used for the report
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$text2} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
benford2 <- function(
    data,
    var) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("benford2 requires jmvcore to be installed (restart may be required)")

    if ( ! missing(var)) var <- jmvcore::resolveQuo(jmvcore::enquo(var))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(var), var, NULL))


    options <- benford2Options$new(
        var = var)

    analysis <- benford2Class$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

