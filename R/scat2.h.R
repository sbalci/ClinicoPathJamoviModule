
# This file is automatically generated, you probably don't want to edit this

scat2Options <- if (requireNamespace('jmvcore')) R6::R6Class(
    "scat2Options",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dep = NULL,
            group = NULL,
            x = NULL,
            y = NULL,
            marg = "none",
            line = "none",
            se = FALSE, ...) {

            super$initialize(
                package='ClinicoPath',
                name='scat2',
                requiresData=TRUE,
                ...)

            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor"),
                default=NULL)
            private$..x <- jmvcore::OptionVariable$new(
                "x",
                x,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..y <- jmvcore::OptionVariable$new(
                "y",
                y,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..marg <- jmvcore::OptionList$new(
                "marg",
                marg,
                options=list(
                    "none",
                    "dens",
                    "box"),
                default="none")
            private$..line <- jmvcore::OptionList$new(
                "line",
                line,
                options=list(
                    "none",
                    "linear",
                    "smooth"),
                default="none")
            private$..se <- jmvcore::OptionBool$new(
                "se",
                se,
                default=FALSE)

            self$.addOption(private$..dep)
            self$.addOption(private$..group)
            self$.addOption(private$..x)
            self$.addOption(private$..y)
            self$.addOption(private$..marg)
            self$.addOption(private$..line)
            self$.addOption(private$..se)
        }),
    active = list(
        dep = function() private$..dep$value,
        group = function() private$..group$value,
        x = function() private$..x$value,
        y = function() private$..y$value,
        marg = function() private$..marg$value,
        line = function() private$..line$value,
        se = function() private$..se$value),
    private = list(
        ..dep = NA,
        ..group = NA,
        ..x = NA,
        ..y = NA,
        ..marg = NA,
        ..line = NA,
        ..se = NA)
)

scat2Results <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        plot1 = function() private$.items[["plot1"]],
        scat2 = function() private$.items[["scat2"]],
        scat3 = function() private$.items[["scat3"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="scat2")
            self$add(jmvcore::Image$new(
                options=options,
                name="plot1",
                width=600,
                height=450,
                renderFun=".plot"))
            self$add(jmvcore::Image$new(
                options=options,
                name="scat2",
                title="Scatter Plot",
                renderFun=".scat2",
                clearWith=list(
                    "x",
                    "y",
                    "group",
                    "marg",
                    "line",
                    "se")))
            self$add(jmvcore::Image$new(
                options=options,
                name="scat3",
                title="New Plot",
                renderFun=".scat2",
                clearWith=list(
                    "x",
                    "y",
                    "group",
                    "marg",
                    "line",
                    "se")))}))

scat2Base <- if (requireNamespace('jmvcore')) R6::R6Class(
    "scat2Base",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'scat2',
                version = c(1,0,0),
                options = options,
                results = scat2Results$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE)
        }))

#' scat2
#'
#' Function for making ggstatsplot plots.
#'
#' @examples
#' set.seed(1337)
#'
#' X <- rnorm(100)
#' Y <- 0.5*X + rnorm(100)
#' dat <- data.frame(X = X, Y = Y)
#'
#' scat(dat, x = 'X', y = 'Y', line = 'linear', se = TRUE, marg = 'dens')
#'
#' @param data the data as a data frame
#' @param dep .
#' @param group a string naming the variable from \code{data} that represents
#'   the grouping variable
#' @param x a string naming the variable from \code{data} that contains the x
#'   coordinates of the points in the plot, variable must be numeric
#' @param y a string naming the variable from \code{data} that contains the y
#'   coordinates of the points in the plot, variable must be numeric
#' @param marg \code{none} (default), \code{dens}, or \code{box}, provide
#'   respectively no plots, density plots, or box plots on the axes
#' @param line \code{none} (default), \code{linear}, or \code{smooth}, provide
#'   respectively no regression line, a linear regression line, or a smoothed
#'   regression line
#' @param se \code{TRUE} or \code{FALSE} (default), show the standard error
#'   for the regression line
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$plot1} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$scat2} \tab \tab \tab \tab \tab a scatter plot \cr
#'   \code{results$scat3} \tab \tab \tab \tab \tab new plot \cr
#' }
#'
#' @export
scat2 <- function(
    data,
    dep,
    group = NULL,
    x,
    y,
    marg = "none",
    line = "none",
    se = FALSE) {

    if ( ! requireNamespace('jmvcore'))
        stop('scat2 requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if ( ! missing(x)) x <- jmvcore::resolveQuo(jmvcore::enquo(x))
    if ( ! missing(y)) y <- jmvcore::resolveQuo(jmvcore::enquo(y))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL),
            `if`( ! missing(group), group, NULL),
            `if`( ! missing(x), x, NULL),
            `if`( ! missing(y), y, NULL))

    for (v in group) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- scat2Options$new(
        dep = dep,
        group = group,
        x = x,
        y = y,
        marg = marg,
        line = line,
        se = se)

    analysis <- scat2Class$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
