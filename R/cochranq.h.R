
# This file is automatically generated, you probably don't want to edit this

cochranqOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "cochranqOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            variables = NULL,
            id = NULL,
            method = "asymptotic",
            alpha = 0.05,
            posthoc = TRUE,
            correction = "bonferroni",
            effect_size = TRUE,
            confidence_level = 0.95,
            show_pattern = TRUE,
            show_assumptions = TRUE,
            clinical_interpretation = TRUE, ...) {

            super$initialize(
                package="ClinicoPath",
                name="cochranq",
                requiresData=TRUE,
                ...)

            private$..variables <- jmvcore::OptionVariables$new(
                "variables",
                variables,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..id <- jmvcore::OptionVariable$new(
                "id",
                id,
                suggested=list(
                    "nominal"),
                permitted=list(
                    "factor",
                    "numeric"))
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "asymptotic",
                    "exact",
                    "monte_carlo"),
                default="asymptotic")
            private$..alpha <- jmvcore::OptionNumber$new(
                "alpha",
                alpha,
                min=0.01,
                max=0.1,
                default=0.05)
            private$..posthoc <- jmvcore::OptionBool$new(
                "posthoc",
                posthoc,
                default=TRUE)
            private$..correction <- jmvcore::OptionList$new(
                "correction",
                correction,
                options=list(
                    "bonferroni",
                    "holm",
                    "fdr",
                    "none"),
                default="bonferroni")
            private$..effect_size <- jmvcore::OptionBool$new(
                "effect_size",
                effect_size,
                default=TRUE)
            private$..confidence_level <- jmvcore::OptionNumber$new(
                "confidence_level",
                confidence_level,
                min=0.8,
                max=0.99,
                default=0.95)
            private$..show_pattern <- jmvcore::OptionBool$new(
                "show_pattern",
                show_pattern,
                default=TRUE)
            private$..show_assumptions <- jmvcore::OptionBool$new(
                "show_assumptions",
                show_assumptions,
                default=TRUE)
            private$..clinical_interpretation <- jmvcore::OptionBool$new(
                "clinical_interpretation",
                clinical_interpretation,
                default=TRUE)

            self$.addOption(private$..variables)
            self$.addOption(private$..id)
            self$.addOption(private$..method)
            self$.addOption(private$..alpha)
            self$.addOption(private$..posthoc)
            self$.addOption(private$..correction)
            self$.addOption(private$..effect_size)
            self$.addOption(private$..confidence_level)
            self$.addOption(private$..show_pattern)
            self$.addOption(private$..show_assumptions)
            self$.addOption(private$..clinical_interpretation)
        }),
    active = list(
        variables = function() private$..variables$value,
        id = function() private$..id$value,
        method = function() private$..method$value,
        alpha = function() private$..alpha$value,
        posthoc = function() private$..posthoc$value,
        correction = function() private$..correction$value,
        effect_size = function() private$..effect_size$value,
        confidence_level = function() private$..confidence_level$value,
        show_pattern = function() private$..show_pattern$value,
        show_assumptions = function() private$..show_assumptions$value,
        clinical_interpretation = function() private$..clinical_interpretation$value),
    private = list(
        ..variables = NA,
        ..id = NA,
        ..method = NA,
        ..alpha = NA,
        ..posthoc = NA,
        ..correction = NA,
        ..effect_size = NA,
        ..confidence_level = NA,
        ..show_pattern = NA,
        ..show_assumptions = NA,
        ..clinical_interpretation = NA)
)

cochranqResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "cochranqResults",
    inherit = jmvcore::Group,
    active = list(
        instructions = function() private$.items[["instructions"]],
        dataInfo = function() private$.items[["dataInfo"]],
        cochranQTest = function() private$.items[["cochranQTest"]],
        effectSize = function() private$.items[["effectSize"]],
        responsePatterns = function() private$.items[["responsePatterns"]],
        pairwiseComparisons = function() private$.items[["pairwiseComparisons"]],
        marginalFrequencies = function() private$.items[["marginalFrequencies"]],
        assumptionTesting = function() private$.items[["assumptionTesting"]],
        clinicalInterpretation = function() private$.items[["clinicalInterpretation"]],
        responsePatternPlot = function() private$.items[["responsePatternPlot"]],
        marginalProportionsPlot = function() private$.items[["marginalProportionsPlot"]],
        pairwiseComparisonPlot = function() private$.items[["pairwiseComparisonPlot"]],
        methodExplanation = function() private$.items[["methodExplanation"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Cochran's Q Test for Paired Categorical Data")
            self$add(jmvcore::Html$new(
                options=options,
                name="instructions",
                title="Analysis Instructions",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="dataInfo",
                title="Data Summary",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "variables",
                    "id"),
                columns=list(
                    list(
                        `name`="characteristic", 
                        `title`="Characteristic", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="cochranQTest",
                title="Cochran's Q Test Results",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "variables",
                    "method",
                    "alpha"),
                columns=list(
                    list(
                        `name`="statistic", 
                        `title`="Q Statistic", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="df", 
                        `title`="df", 
                        `type`="integer"),
                    list(
                        `name`="p_value", 
                        `title`="p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="significance", 
                        `title`="Significance", 
                        `type`="text"),
                    list(
                        `name`="conclusion", 
                        `title`="Conclusion", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="effectSize",
                title="Effect Size Measures",
                visible="(effect_size)",
                rows=1,
                clearWith=list(
                    "effect_size",
                    "variables"),
                columns=list(
                    list(
                        `name`="measure", 
                        `title`="Effect Size", 
                        `type`="text"),
                    list(
                        `name`="value", 
                        `title`="Value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="confidence_interval", 
                        `title`="Confidence Interval", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="responsePatterns",
                title="Response Pattern Analysis",
                visible="(show_pattern)",
                rows=1,
                clearWith=list(
                    "show_pattern",
                    "variables"),
                columns=list(
                    list(
                        `name`="pattern", 
                        `title`="Response Pattern", 
                        `type`="text"),
                    list(
                        `name`="frequency", 
                        `title`="Frequency", 
                        `type`="integer"),
                    list(
                        `name`="percentage", 
                        `title`="Percentage", 
                        `type`="number", 
                        `format`="zto:2"),
                    list(
                        `name`="clinical_meaning", 
                        `title`="Clinical Interpretation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="pairwiseComparisons",
                title="Post-hoc Pairwise Comparisons",
                visible="(posthoc)",
                rows=1,
                clearWith=list(
                    "posthoc",
                    "correction",
                    "variables"),
                columns=list(
                    list(
                        `name`="comparison", 
                        `title`="Comparison", 
                        `type`="text"),
                    list(
                        `name`="mcnemar_statistic", 
                        `title`="McNemar \u03C7\u00B2", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="p_value_raw", 
                        `title`="Raw p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="p_value_adjusted", 
                        `title`="Adjusted p-value", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="significance", 
                        `title`="Significance", 
                        `type`="text"),
                    list(
                        `name`="effect_size", 
                        `title`="Effect Size", 
                        `type`="number", 
                        `format`="zto:4"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="marginalFrequencies",
                title="Marginal Frequencies",
                visible=TRUE,
                rows=1,
                clearWith=list(
                    "variables"),
                columns=list(
                    list(
                        `name`="variable", 
                        `title`="Variable", 
                        `type`="text"),
                    list(
                        `name`="positive_responses", 
                        `title`="Positive Responses", 
                        `type`="integer"),
                    list(
                        `name`="negative_responses", 
                        `title`="Negative Responses", 
                        `type`="integer"),
                    list(
                        `name`="proportion_positive", 
                        `title`="Proportion Positive", 
                        `type`="number", 
                        `format`="zto:4"),
                    list(
                        `name`="confidence_interval", 
                        `title`="CI for Proportion", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="assumptionTesting",
                title="Assumption Assessment",
                visible="(show_assumptions)",
                rows=1,
                clearWith=list(
                    "show_assumptions",
                    "variables"),
                columns=list(
                    list(
                        `name`="assumption", 
                        `title`="Assumption", 
                        `type`="text"),
                    list(
                        `name`="status", 
                        `title`="Status", 
                        `type`="text"),
                    list(
                        `name`="details", 
                        `title`="Details", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Recommendation", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="clinicalInterpretation",
                title="Clinical Interpretation Guide",
                visible="(clinical_interpretation)",
                rows=1,
                clearWith=list(
                    "clinical_interpretation",
                    "variables",
                    "alpha"),
                columns=list(
                    list(
                        `name`="aspect", 
                        `title`="Clinical Aspect", 
                        `type`="text"),
                    list(
                        `name`="finding", 
                        `title`="Statistical Finding", 
                        `type`="text"),
                    list(
                        `name`="interpretation", 
                        `title`="Clinical Interpretation", 
                        `type`="text"),
                    list(
                        `name`="recommendation", 
                        `title`="Clinical Recommendation", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="responsePatternPlot",
                title="Response Pattern Visualization",
                visible="(show_pattern)",
                requiresData=TRUE,
                width=800,
                height=600,
                renderFun=".plotResponsePatterns",
                clearWith=list(
                    "show_pattern",
                    "variables")))
            self$add(jmvcore::Image$new(
                options=options,
                name="marginalProportionsPlot",
                title="Marginal Proportions Plot",
                visible=TRUE,
                requiresData=TRUE,
                width=700,
                height=500,
                renderFun=".plotMarginalProportions",
                clearWith=list(
                    "variables")))
            self$add(jmvcore::Image$new(
                options=options,
                name="pairwiseComparisonPlot",
                title="Pairwise Comparison Heatmap",
                visible="(posthoc)",
                requiresData=TRUE,
                width=600,
                height=600,
                renderFun=".plotPairwiseComparisons",
                clearWith=list(
                    "posthoc",
                    "correction",
                    "variables")))
            self$add(jmvcore::Html$new(
                options=options,
                name="methodExplanation",
                title="Method and Clinical Context",
                visible=TRUE))}))

cochranqBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "cochranqBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "cochranq",
                version = c(1,0,0),
                options = options,
                results = cochranqResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Cochran's Q Test for Paired Categorical Data
#'
#' Cochran's Q test for analyzing paired categorical (binary) data with more 
#' than two  time points or conditions. This is the extension of McNemar's 
#' test for repeated  measures with binary outcomes, commonly used in clinical 
#' studies to test for  changes in treatment response or diagnostic agreement 
#' across multiple time points.
#' 
#'
#' @examples
#' data \%>\%
#' cochranq(
#'     variables = c("baseline", "month1", "month3", "month6"),
#'     id = "patient_id",
#'     method = "asymptotic"
#' )
#'
#' @param data the data as a data frame
#' @param variables Binary variables measured at different time points or
#'   conditions (minimum 3 variables required)
#' @param id Optional subject identifier for paired data structure validation
#' @param method Method for calculating p-values
#' @param alpha Alpha level for hypothesis testing
#' @param posthoc Perform pairwise post-hoc comparisons when Q test is
#'   significant
#' @param correction Multiple comparison correction method for post-hoc tests
#' @param effect_size Include effect size measures (Kendall's W coefficient of
#'   concordance)
#' @param confidence_level Confidence level for confidence intervals
#' @param show_pattern Show detailed response pattern analysis and frequencies
#' @param show_assumptions Test assumptions for Cochran's Q test validity
#' @param clinical_interpretation Provide clinical interpretation guidance for
#'   results
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$instructions} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$dataInfo} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$cochranQTest} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$effectSize} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$responsePatterns} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$pairwiseComparisons} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$marginalFrequencies} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$assumptionTesting} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$clinicalInterpretation} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$responsePatternPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$marginalProportionsPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$pairwiseComparisonPlot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$methodExplanation} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$dataInfo$asDF}
#'
#' \code{as.data.frame(results$dataInfo)}
#'
#' @export
cochranq <- function(
    data,
    variables,
    id,
    method = "asymptotic",
    alpha = 0.05,
    posthoc = TRUE,
    correction = "bonferroni",
    effect_size = TRUE,
    confidence_level = 0.95,
    show_pattern = TRUE,
    show_assumptions = TRUE,
    clinical_interpretation = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("cochranq requires jmvcore to be installed (restart may be required)")

    if ( ! missing(variables)) variables <- jmvcore::resolveQuo(jmvcore::enquo(variables))
    if ( ! missing(id)) id <- jmvcore::resolveQuo(jmvcore::enquo(id))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(variables), variables, NULL),
            `if`( ! missing(id), id, NULL))


    options <- cochranqOptions$new(
        variables = variables,
        id = id,
        method = method,
        alpha = alpha,
        posthoc = posthoc,
        correction = correction,
        effect_size = effect_size,
        confidence_level = confidence_level,
        show_pattern = show_pattern,
        show_assumptions = show_assumptions,
        clinical_interpretation = clinical_interpretation)

    analysis <- cochranqClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

