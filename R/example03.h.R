
# This file is automatically generated, you probably don't want to edit this

Example03Options <- if (requireNamespace('jmvcore')) R6::R6Class(
    "Example03Options",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dependent = NULL,
            group = NULL,
            equVar = TRUE, ...) {

            super$initialize(
                package='ClinicoPath',
                name='Example03',
                requiresData=TRUE,
                ...)

            private$..dependent <- jmvcore::OptionVariable$new(
                "dependent",
                dependent)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group)
            private$..equVar <- jmvcore::OptionBool$new(
                "equVar",
                equVar,
                default=TRUE)

            self$.addOption(private$..dependent)
            self$.addOption(private$..group)
            self$.addOption(private$..equVar)
        }),
    active = list(
        dependent = function() private$..dependent$value,
        group = function() private$..group$value,
        equVar = function() private$..equVar$value),
    private = list(
        ..dependent = NA,
        ..group = NA,
        ..equVar = NA)
)

Example03Results <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        ttest = function() private$.items[["ttest"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Example 3")
            self$add(jmvcore::Table$new(
                options=options,
                name="ttest",
                title="Independent Samples T-Test",
                rows=1,
                columns=list(
                    list(
                        `name`="var", 
                        `title`="", 
                        `content`="", 
                        `type`="text"),
                    list(
                        `name`="stat", 
                        `title`="statistic", 
                        `type`="number"),
                    list(
                        `name`="df", 
                        `title`="df", 
                        `type`="number"),
                    list(
                        `name`="p", 
                        `title`="p", 
                        `type`="number", 
                        `format`="zto,pvalue"))))}))

Example03Base <- if (requireNamespace('jmvcore')) R6::R6Class(
    "Example03Base",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'Example03',
                version = c(1,0,0),
                options = options,
                results = Example03Results$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Example 3
#'
#' 
#' @param data .
#' @param dependent .
#' @param group .
#' @param equVar .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$ttest} \tab \tab \tab \tab \tab a table \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$ttest$asDF}
#'
#' \code{as.data.frame(results$ttest)}
#'
#' @export
Example03 <- function(
    data,
    dependent,
    group,
    equVar = TRUE) {

    if ( ! requireNamespace('jmvcore'))
        stop('Example03 requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dependent)) dependent <- jmvcore::resolveQuo(jmvcore::enquo(dependent))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dependent), dependent, NULL),
            `if`( ! missing(group), group, NULL))


    options <- Example03Options$new(
        dependent = dependent,
        group = group,
        equVar = equVar)

    analysis <- Example03Class$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
