
# This file is automatically generated, you probably don't want to edit this

jjdotchartOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jjdotchartOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dep = NULL,
            group = NULL,
            grvar = NULL,
            testvalue = 0,
            typestatistics = "parametric",
            conflevel = 0.95,
            k = 2,
            resultssubtitle = TRUE,
            showSummaryTable = TRUE,
            centralityplotting = FALSE,
            centralitytype = "parametric",
            bfmessage = FALSE,
            originaltheme = FALSE,
            mytitle = "",
            xtitle = "",
            ytitle = "",
            plotwidth = 650,
            plotheight = 450, ...) {

            super$initialize(
                package="ClinicoPath",
                name="jjdotchart",
                requiresData=TRUE,
                ...)

            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep,
                default=NULL,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group,
                default=NULL,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..grvar <- jmvcore::OptionVariable$new(
                "grvar",
                grvar,
                default=NULL,
                suggested=list(
                    "ordinal",
                    "nominal"),
                permitted=list(
                    "factor"))
            private$..testvalue <- jmvcore::OptionNumber$new(
                "testvalue",
                testvalue,
                default=0)
            private$..typestatistics <- jmvcore::OptionList$new(
                "typestatistics",
                typestatistics,
                options=list(
                    "parametric",
                    "nonparametric",
                    "robust",
                    "bayes"),
                default="parametric")
            private$..conflevel <- jmvcore::OptionNumber$new(
                "conflevel",
                conflevel,
                default=0.95,
                min=0,
                max=1)
            private$..k <- jmvcore::OptionInteger$new(
                "k",
                k,
                default=2,
                min=0,
                max=5)
            private$..resultssubtitle <- jmvcore::OptionBool$new(
                "resultssubtitle",
                resultssubtitle,
                default=TRUE)
            private$..showSummaryTable <- jmvcore::OptionBool$new(
                "showSummaryTable",
                showSummaryTable,
                default=TRUE)
            private$..centralityplotting <- jmvcore::OptionBool$new(
                "centralityplotting",
                centralityplotting,
                default=FALSE)
            private$..centralitytype <- jmvcore::OptionList$new(
                "centralitytype",
                centralitytype,
                options=list(
                    "parametric",
                    "nonparametric",
                    "robust",
                    "bayes"),
                default="parametric")
            private$..bfmessage <- jmvcore::OptionBool$new(
                "bfmessage",
                bfmessage,
                default=FALSE)
            private$..originaltheme <- jmvcore::OptionBool$new(
                "originaltheme",
                originaltheme,
                default=FALSE)
            private$..mytitle <- jmvcore::OptionString$new(
                "mytitle",
                mytitle,
                default="")
            private$..xtitle <- jmvcore::OptionString$new(
                "xtitle",
                xtitle,
                default="")
            private$..ytitle <- jmvcore::OptionString$new(
                "ytitle",
                ytitle,
                default="")
            private$..plotwidth <- jmvcore::OptionInteger$new(
                "plotwidth",
                plotwidth,
                default=650,
                min=300,
                max=1200)
            private$..plotheight <- jmvcore::OptionInteger$new(
                "plotheight",
                plotheight,
                default=450,
                min=300,
                max=800)

            self$.addOption(private$..dep)
            self$.addOption(private$..group)
            self$.addOption(private$..grvar)
            self$.addOption(private$..testvalue)
            self$.addOption(private$..typestatistics)
            self$.addOption(private$..conflevel)
            self$.addOption(private$..k)
            self$.addOption(private$..resultssubtitle)
            self$.addOption(private$..showSummaryTable)
            self$.addOption(private$..centralityplotting)
            self$.addOption(private$..centralitytype)
            self$.addOption(private$..bfmessage)
            self$.addOption(private$..originaltheme)
            self$.addOption(private$..mytitle)
            self$.addOption(private$..xtitle)
            self$.addOption(private$..ytitle)
            self$.addOption(private$..plotwidth)
            self$.addOption(private$..plotheight)
        }),
    active = list(
        dep = function() private$..dep$value,
        group = function() private$..group$value,
        grvar = function() private$..grvar$value,
        testvalue = function() private$..testvalue$value,
        typestatistics = function() private$..typestatistics$value,
        conflevel = function() private$..conflevel$value,
        k = function() private$..k$value,
        resultssubtitle = function() private$..resultssubtitle$value,
        showSummaryTable = function() private$..showSummaryTable$value,
        centralityplotting = function() private$..centralityplotting$value,
        centralitytype = function() private$..centralitytype$value,
        bfmessage = function() private$..bfmessage$value,
        originaltheme = function() private$..originaltheme$value,
        mytitle = function() private$..mytitle$value,
        xtitle = function() private$..xtitle$value,
        ytitle = function() private$..ytitle$value,
        plotwidth = function() private$..plotwidth$value,
        plotheight = function() private$..plotheight$value),
    private = list(
        ..dep = NA,
        ..group = NA,
        ..grvar = NA,
        ..testvalue = NA,
        ..typestatistics = NA,
        ..conflevel = NA,
        ..k = NA,
        ..resultssubtitle = NA,
        ..showSummaryTable = NA,
        ..centralityplotting = NA,
        ..centralitytype = NA,
        ..bfmessage = NA,
        ..originaltheme = NA,
        ..mytitle = NA,
        ..xtitle = NA,
        ..ytitle = NA,
        ..plotwidth = NA,
        ..plotheight = NA)
)

jjdotchartResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jjdotchartResults",
    inherit = jmvcore::Group,
    active = list(
        todo = function() private$.items[["todo"]],
        notices = function() private$.items[["notices"]],
        summary = function() private$.items[["summary"]],
        plot2 = function() private$.items[["plot2"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Dot Chart (Summary vs Reference Value)",
                refs=list(
                    "ggplot2",
                    "ggstatsplot",
                    "statsExpressions",
                    "ClinicoPathJamoviModule"),
                clearWith=list(
                    "dep",
                    "group",
                    "grvar",
                    "testvalue",
                    "typestatistics",
                    "conflevel",
                    "k",
                    "resultssubtitle",
                    "showSummaryTable",
                    "centralityplotting",
                    "centralitytype",
                    "bfmessage",
                    "originaltheme",
                    "mytitle",
                    "xtitle",
                    "ytitle",
                    "plotwidth",
                    "plotheight"))
            self$add(jmvcore::Html$new(
                options=options,
                name="todo",
                title="To Do"))
            self$add(jmvcore::Html$new(
                options=options,
                name="notices",
                title="Notices"))
            self$add(jmvcore::Table$new(
                options=options,
                name="summary",
                title="Group Summaries (one row per plotted point)",
                visible="(showSummaryTable)",
                rows=0,
                columns=list(
                    list(
                        `name`="grp", 
                        `title`="Group", 
                        `type`="text"),
                    list(
                        `name`="n", 
                        `title`="N obs", 
                        `type`="integer"),
                    list(
                        `name`="value", 
                        `title`="Summary", 
                        `type`="number"),
                    list(
                        `name`="sd", 
                        `title`="SD", 
                        `type`="number"),
                    list(
                        `name`="ci_low", 
                        `title`="Lower", 
                        `type`="number"),
                    list(
                        `name`="ci_high", 
                        `title`="Upper", 
                        `type`="number"),
                    list(
                        `name`="vsref", 
                        `title`="vs Reference", 
                        `type`="number"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot2",
                title="`${dep} by {group}, split by {grvar}`",
                width=800,
                height=450,
                renderFun=".plot2",
                requiresData=TRUE,
                visible="(grvar)"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="`${dep} by {group}`",
                width=650,
                height=450,
                renderFun=".plot",
                requiresData=TRUE))}))

jjdotchartBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "jjdotchartBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "ClinicoPath",
                name = "jjdotchart",
                version = c(1,0,52),
                options = options,
                results = jjdotchartResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Dot Chart (Summary vs Reference Value)
#'
#' Cleveland dot chart: one summary point per group, ordered by value, with
#' a confidence interval and a reference line. Wraps
#' ggstatsplot::ggdotplotstats and ggstatsplot::grouped_ggdotplotstats.
#' 
#' IMPORTANT - what is tested. Every observation in a group is collapsed to a
#' SINGLE summary value, and the test is a ONE-SAMPLE test of those k summary
#' values against your Reference Value. The sample size of the test is
#' therefore the NUMBER OF GROUPS, not the number of patients: 120 patients
#' in 3 groups gives a one-sample t-test with n = 3 and 2 degrees of freedom.
#' It answers "are the group summaries, taken together, different from this
#' value?" - NOT "do these groups differ from each other".
#' 
#' Which summary is plotted follows the Statistical Test you choose, and the
#' test then runs on those same values: parametric plots the group MEANS,
#' nonparametric the MEDIANS, robust the 20 percent TRIMMED MEANS, Bayesian
#' the MAP estimates. Verified on skewed data where they differ sharply
#' (mean 33.77 vs median 9.75 for the same group), so switching test type
#' genuinely changes the picture, not just the caption.
#' 
#' To compare groups WITH EACH OTHER using every observation, use
#' "Box-Violin Plots to Compare Between Groups" or
#' "Horizontal Box-Violin Comparison" instead.
#' 
#' Best suited to many labels each contributing one meaningful summary -
#' mean turnaround time across 20 laboratories, median biomarker by centre -
#' which is what a Cleveland dot plot is designed for. With only two or three
#' groups the test has 1-2 degrees of freedom and is of little value, even
#' though the chart itself is still a fair picture of the group averages.
#' 
#' @param data The data as a data frame.
#' @param dep The continuous variable to summarise. Each group is reduced to
#'   one point on the chart - its mean, median, trimmed mean or MAP estimate,
#'   whichever matches the Statistical Test selected below.
#' @param group The grouping variable. Every level becomes one point on the
#'   chart, and the number of levels is the sample size of the statistical test.
#' @param grvar Optional variable producing a separate dot chart for each of
#'   its levels.
#' @param testvalue The value the k group summaries are tested against. Choose
#'   something clinically meaningful on the scale of your measurement - an upper
#'   limit of normal, a target turnaround time, a published benchmark. The
#'   default of 0 is rarely the right choice: for a measurement that is not
#'   centred near zero it makes the standardised effect enormous, and the
#'   statistics package then fails to compute a confidence interval for the
#'   effect size and returns NO statistics at all. The analysis detects this and
#'   says so rather than showing an empty result.
#' @param typestatistics The one-sample test comparing the k group summaries
#'   to the Reference Value. This also decides WHICH summary is plotted for each
#'   group - mean, median, 20 percent trimmed mean or MAP estimate respectively
#'   - so changing it moves the points, not just the caption.
#' @param conflevel Confidence level for the intervals, 0.95 giving 95 percent
#'   intervals.
#' @param k Number of decimal places for the reported statistics.
#' @param resultssubtitle Show the test result as a subtitle. Defaults to TRUE
#'   here, unlike the sibling plot analyses: the comparison against the
#'   Reference Value is the whole point of this chart, so hiding it leaves a
#'   figure whose title makes a claim nothing on the plot supports.
#' @param showSummaryTable Show the per-group table behind the chart - the
#'   number of observations, the plotted summary value, and its spread. This is
#'   what makes the aggregation auditable: it states how many observations each
#'   single plotted point represents. The summary column is labelled with the
#'   statistic actually used, which changes with the Statistical Test.
#' @param centralityplotting Draw a SECOND vertical line at the centre of the
#'   k plotted summaries - i.e. the average of the group averages. This is not
#'   the Reference Value; the Reference Value line is always drawn and is
#'   labelled. Off by default deliberately. The statistics package draws this
#'   line by default and draws NO line at the reference value, so the only
#'   vertical line on an unmodified figure sits at the centre of the data while
#'   the user believes it marks the threshold they typed in (measured: identical
#'   line position for reference values of 0, 12 and 999). This analysis inverts
#'   that - the reference line is always present, and this optional second line
#'   is drawn in a different style and named in the caption.
#' @param centralitytype Which centre to use for the optional second line,
#'   when 'Also mark the centre of the plotted points' is ticked. It affects
#'   that line ONLY - the plotted points always follow the Statistical Test.
#'   Leave it matching your Statistical Test unless you have a specific reason.
#' @param bfmessage Show the Bayes factor caption when the Bayesian test is
#'   selected.
#' @param originaltheme Use the original ggstatsplot theme rather than
#'   jamovi's default.
#' @param mytitle Main title for the plot. Leave blank for no title.
#' @param xtitle Label for the horizontal axis, which carries the measurement
#'   values. Leave blank to use the variable name.
#' @param ytitle Label for the vertical axis, which carries the group labels.
#'   Leave blank to use the variable name.
#' @param plotwidth Width of the plot in pixels.
#' @param plotheight Height of the plot in pixels. Increase it when there are
#'   many groups, since each one needs a labelled row.
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$todo} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$notices} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$summary} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$plot2} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$summary$asDF}
#'
#' \code{as.data.frame(results$summary)}
#'
#' @export
jjdotchart <- function(
    data,
    dep = NULL,
    group = NULL,
    grvar = NULL,
    testvalue = 0,
    typestatistics = "parametric",
    conflevel = 0.95,
    k = 2,
    resultssubtitle = TRUE,
    showSummaryTable = TRUE,
    centralityplotting = FALSE,
    centralitytype = "parametric",
    bfmessage = FALSE,
    originaltheme = FALSE,
    mytitle = "",
    xtitle = "",
    ytitle = "",
    plotwidth = 650,
    plotheight = 450) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("jjdotchart requires jmvcore to be installed (restart may be required)")

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if ( ! missing(grvar)) grvar <- jmvcore::resolveQuo(jmvcore::enquo(grvar))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL),
            `if`( ! missing(group), group, NULL),
            `if`( ! missing(grvar), grvar, NULL))

    for (v in group) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])
    for (v in grvar) if (v %in% names(data)) data[[v]] <- as.factor(data[[v]])

    options <- jjdotchartOptions$new(
        dep = dep,
        group = group,
        grvar = grvar,
        testvalue = testvalue,
        typestatistics = typestatistics,
        conflevel = conflevel,
        k = k,
        resultssubtitle = resultssubtitle,
        showSummaryTable = showSummaryTable,
        centralityplotting = centralityplotting,
        centralitytype = centralitytype,
        bfmessage = bfmessage,
        originaltheme = originaltheme,
        mytitle = mytitle,
        xtitle = xtitle,
        ytitle = ytitle,
        plotwidth = plotwidth,
        plotheight = plotheight)

    analysis <- jjdotchartClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

