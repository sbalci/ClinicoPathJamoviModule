
# This file is automatically generated, you probably don't want to edit this

tsENTOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "tsENTOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            y1 = NULL,
            Ent_edim = 3,
            Ent_r = 1, ...) {

            super$initialize(
                package='ClinicoPath',
                name='tsENT',
                requiresData=TRUE,
                ...)

            private$..y1 <- jmvcore::OptionVariable$new(
                "y1",
                y1)
            private$..Ent_edim <- jmvcore::OptionNumber$new(
                "Ent_edim",
                Ent_edim,
                default=3)
            private$..Ent_r <- jmvcore::OptionNumber$new(
                "Ent_r",
                Ent_r,
                default=1)

            self$.addOption(private$..y1)
            self$.addOption(private$..Ent_edim)
            self$.addOption(private$..Ent_r)
        }),
    active = list(
        y1 = function() private$..y1$value,
        Ent_edim = function() private$..Ent_edim$value,
        Ent_r = function() private$..Ent_r$value),
    private = list(
        ..y1 = NA,
        ..Ent_edim = NA,
        ..Ent_r = NA)
)

tsENTResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        tsdesc = function() private$.items[["tsdesc"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Entropy & Roughness")
            self$add(jmvcore::Table$new(
                options=options,
                name="tsdesc",
                title="Entropy & Roughness",
                rows=1,
                columns=list(
                    list(
                        `name`="var", 
                        `title`="", 
                        `type`="text"),
                    list(
                        `name`="N", 
                        `type`="integer", 
                        `title`="Valid N"),
                    list(
                        `name`="na", 
                        `type`="integer", 
                        `title`="NA values"),
                    list(
                        `name`="edim", 
                        `type`="number"),
                    list(
                        `name`="factor", 
                        `type`="number"),
                    list(
                        `name`="SD", 
                        `type`="number"),
                    list(
                        `name`="radius", 
                        `type`="number"),
                    list(
                        `name`="sampEnt", 
                        `title`="Sample Entropy", 
                        `type`="number"),
                    list(
                        `title`="Approximate Entropy", 
                        `name`="apprEnt", 
                        `type`="number"),
                    list(
                        `title`="local VAR", 
                        `name`="locVar", 
                        `type`="number"),
                    list(
                        `title`="global VAR", 
                        `name`="globVar", 
                        `type`="number"),
                    list(
                        `title`="Relative Roughness", 
                        `name`="RR", 
                        `type`="number"))))}))

tsENTBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "tsENTBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'tsENT',
                version = c(1,0,0),
                options = options,
                results = tsENTResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Entropy & Roughness
#'
#' 
#' @param data .
#' @param y1 .
#' @param Ent_edim .
#' @param Ent_r .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$tsdesc} \tab \tab \tab \tab \tab a table \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$tsdesc$asDF}
#'
#' \code{as.data.frame(results$tsdesc)}
#'
#' @export
tsENT <- function(
    data,
    y1,
    Ent_edim = 3,
    Ent_r = 1) {

    if ( ! requireNamespace('jmvcore'))
        stop('tsENT requires jmvcore to be installed (restart may be required)')

    if ( ! missing(y1)) y1 <- jmvcore::resolveQuo(jmvcore::enquo(y1))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(y1), y1, NULL))


    options <- tsENTOptions$new(
        y1 = y1,
        Ent_edim = Ent_edim,
        Ent_r = Ent_r)

    analysis <- tsENTClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
