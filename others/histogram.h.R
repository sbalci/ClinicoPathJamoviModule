
# This file is automatically generated, you probably don't want to edit this

histogramOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "histogramOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dep = NULL,
            anchor = 0,
            binwidth = 1, ...) {

            super$initialize(
                package='ClinicoPath',
                name='histogram',
                requiresData=TRUE,
                ...)

            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep)
            private$..anchor <- jmvcore::OptionNumber$new(
                "anchor",
                anchor,
                default=0)
            private$..binwidth <- jmvcore::OptionNumber$new(
                "binwidth",
                binwidth,
                default=1)

            self$.addOption(private$..dep)
            self$.addOption(private$..anchor)
            self$.addOption(private$..binwidth)
        }),
    active = list(
        dep = function() private$..dep$value,
        anchor = function() private$..anchor$value,
        binwidth = function() private$..binwidth$value),
    private = list(
        ..dep = NA,
        ..anchor = NA,
        ..binwidth = NA)
)

histogramResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Histogram")
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="Histogram Plot",
                width=400,
                height=300,
                renderFun=".plot"))}))

histogramBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "histogramBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'histogram',
                version = c(1,0,0),
                options = options,
                results = histogramResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Histogram
#'
#' 
#' @param data .
#' @param dep .
#' @param anchor .
#' @param binwidth .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
histogram <- function(
    data,
    dep,
    anchor = 0,
    binwidth = 1) {

    if ( ! requireNamespace('jmvcore'))
        stop('histogram requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL))


    options <- histogramOptions$new(
        dep = dep,
        anchor = anchor,
        binwidth = binwidth)

    analysis <- histogramClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
