
# This file is automatically generated, you probably don't want to edit this

modellingSurvivalOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "modellingSurvivalOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            times = NULL,
            status = NULL,
            deps = NULL,
            strata = NULL,
            forestyn = FALSE,
            diagnosticsyn = FALSE,
            survivaltreeyn = FALSE,
            plotvar = NULL,
            ciyn = FALSE,
            timeunits = "None", ...) {

            super$initialize(
                package='ClinicoPath',
                name='modellingSurvival',
                requiresData=TRUE,
                ...)

            private$..times <- jmvcore::OptionVariable$new(
                "times",
                times,
                suggested=list(
                    "continuous"))
            private$..status <- jmvcore::OptionVariable$new(
                "status",
                status,
                suggested=list(
                    "continuous"))
            private$..deps <- jmvcore::OptionVariables$new(
                "deps",
                deps)
            private$..strata <- jmvcore::OptionVariable$new(
                "strata",
                strata)
            private$..forestyn <- jmvcore::OptionBool$new(
                "forestyn",
                forestyn,
                default=FALSE)
            private$..diagnosticsyn <- jmvcore::OptionBool$new(
                "diagnosticsyn",
                diagnosticsyn,
                default=FALSE)
            private$..survivaltreeyn <- jmvcore::OptionBool$new(
                "survivaltreeyn",
                survivaltreeyn,
                default=FALSE)
            private$..plotvar <- jmvcore::OptionVariable$new(
                "plotvar",
                plotvar)
            private$..ciyn <- jmvcore::OptionBool$new(
                "ciyn",
                ciyn,
                default=FALSE)
            private$..timeunits <- jmvcore::OptionList$new(
                "timeunits",
                timeunits,
                options=list(
                    "None",
                    "Days",
                    "Weeks",
                    "Months",
                    "Years"),
                default="None")

            self$.addOption(private$..times)
            self$.addOption(private$..status)
            self$.addOption(private$..deps)
            self$.addOption(private$..strata)
            self$.addOption(private$..forestyn)
            self$.addOption(private$..diagnosticsyn)
            self$.addOption(private$..survivaltreeyn)
            self$.addOption(private$..plotvar)
            self$.addOption(private$..ciyn)
            self$.addOption(private$..timeunits)
        }),
    active = list(
        times = function() private$..times$value,
        status = function() private$..status$value,
        deps = function() private$..deps$value,
        strata = function() private$..strata$value,
        forestyn = function() private$..forestyn$value,
        diagnosticsyn = function() private$..diagnosticsyn$value,
        survivaltreeyn = function() private$..survivaltreeyn$value,
        plotvar = function() private$..plotvar$value,
        ciyn = function() private$..ciyn$value,
        timeunits = function() private$..timeunits$value),
    private = list(
        ..times = NA,
        ..status = NA,
        ..deps = NA,
        ..strata = NA,
        ..forestyn = NA,
        ..diagnosticsyn = NA,
        ..survivaltreeyn = NA,
        ..plotvar = NA,
        ..ciyn = NA,
        ..timeunits = NA)
)

modellingSurvivalResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        modsurvTable1 = function() private$.items[["modsurvTable1"]],
        modsurvTable2 = function() private$.items[["modsurvTable2"]],
        plot4 = function() private$.items[["plot4"]],
        plot2 = function() private$.items[["plot2"]],
        plot3 = function() private$.items[["plot3"]],
        plot1 = function() private$.items[["plot1"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Cox Proportional Hazard Model")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Cox proportional hazard model"))
            self$add(jmvcore::Table$new(
                options=options,
                name="modsurvTable1",
                title="Likelihood Ratio Test",
                clearWith=list(
                    "times",
                    "status",
                    "deps"),
                rows=1,
                columns=list(
                    list(
                        `name`="test", 
                        `title`="Test Statistic", 
                        `type`="number"),
                    list(
                        `name`="df", 
                        `title`="DF", 
                        `type`="integer"),
                    list(
                        `name`="pvalue", 
                        `title`="P-Value", 
                        `type`="number", 
                        `format`="zto,pvalue"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="modsurvTable2",
                title="Model Summary",
                clearWith=list(
                    "times",
                    "status",
                    "deps"),
                columns=list(
                    list(
                        `name`="term", 
                        `title`="Term", 
                        `type`="text", 
                        `content`="($key)", 
                        `visible`="(deps)"),
                    list(
                        `name`="beta", 
                        `title`="Beta", 
                        `type`="number"),
                    list(
                        `name`="sebeta", 
                        `title`="SE(Beta)", 
                        `type`="number"),
                    list(
                        `name`="z", 
                        `title`="Z", 
                        `type`="number"),
                    list(
                        `name`="pvalue", 
                        `title`="P-Value", 
                        `type`="number", 
                        `format`="zto,pvalue"),
                    list(
                        `name`="hr", 
                        `title`="HR", 
                        `type`="number"),
                    list(
                        `name`="hrlower", 
                        `title`="HR Lower", 
                        `type`="number"),
                    list(
                        `name`="hrupper", 
                        `title`="HR Upper", 
                        `type`="number"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot4",
                title="Forest plot to visualize Cox model",
                width=600,
                height=500,
                renderFun=".plot4"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot2",
                title="Testing proportional hazards assumption",
                width=600,
                height=500,
                renderFun=".plot2"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot3",
                title="Survival tree",
                width=600,
                height=500,
                renderFun=".plot3"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot1",
                title="Estimation of the survival probabilities",
                width=600,
                height=500,
                renderFun=".plot1"))}))

modellingSurvivalBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "modellingSurvivalBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'modellingSurvival',
                version = c(1,0,0),
                options = options,
                results = modellingSurvivalResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Cox Proportional Hazard Model
#'
#' 
#' @param data .
#' @param times .
#' @param status .
#' @param deps .
#' @param strata .
#' @param forestyn .
#' @param diagnosticsyn .
#' @param survivaltreeyn .
#' @param plotvar .
#' @param ciyn .
#' @param timeunits .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$modsurvTable1} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$modsurvTable2} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$plot4} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plot2} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plot3} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$plot1} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$modsurvTable1$asDF}
#'
#' \code{as.data.frame(results$modsurvTable1)}
#'
#' @export
modellingSurvival <- function(
    data,
    times,
    status,
    deps,
    strata,
    forestyn = FALSE,
    diagnosticsyn = FALSE,
    survivaltreeyn = FALSE,
    plotvar,
    ciyn = FALSE,
    timeunits = "None") {

    if ( ! requireNamespace('jmvcore'))
        stop('modellingSurvival requires jmvcore to be installed (restart may be required)')

    if ( ! missing(times)) times <- jmvcore::resolveQuo(jmvcore::enquo(times))
    if ( ! missing(status)) status <- jmvcore::resolveQuo(jmvcore::enquo(status))
    if ( ! missing(deps)) deps <- jmvcore::resolveQuo(jmvcore::enquo(deps))
    if ( ! missing(strata)) strata <- jmvcore::resolveQuo(jmvcore::enquo(strata))
    if ( ! missing(plotvar)) plotvar <- jmvcore::resolveQuo(jmvcore::enquo(plotvar))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(times), times, NULL),
            `if`( ! missing(status), status, NULL),
            `if`( ! missing(deps), deps, NULL),
            `if`( ! missing(strata), strata, NULL),
            `if`( ! missing(plotvar), plotvar, NULL))


    options <- modellingSurvivalOptions$new(
        times = times,
        status = status,
        deps = deps,
        strata = strata,
        forestyn = forestyn,
        diagnosticsyn = diagnosticsyn,
        survivaltreeyn = survivaltreeyn,
        plotvar = plotvar,
        ciyn = ciyn,
        timeunits = timeunits)

    analysis <- modellingSurvivalClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
