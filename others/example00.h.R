
# This file is automatically generated, you probably don't want to edit this

Example00Options <- if (requireNamespace('jmvcore')) R6::R6Class(
    "Example00Options",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dependent = NULL,
            group = NULL,
            equVar = TRUE, ...) {

            super$initialize(
                package='ClinicoPath',
                name='Example00',
                requiresData=TRUE,
                ...)

            private$..dependent <- jmvcore::OptionVariable$new(
                "dependent",
                dependent)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group)
            private$..equVar <- jmvcore::OptionBool$new(
                "equVar",
                equVar,
                default=TRUE)

            self$.addOption(private$..dependent)
            self$.addOption(private$..group)
            self$.addOption(private$..equVar)
        }),
    active = list(
        dependent = function() private$..dependent$value,
        group = function() private$..group$value,
        equVar = function() private$..equVar$value),
    private = list(
        ..dependent = NA,
        ..group = NA,
        ..equVar = NA)
)

Example00Results <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Example 0")}))

Example00Base <- if (requireNamespace('jmvcore')) R6::R6Class(
    "Example00Base",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'ClinicoPath',
                name = 'Example00',
                version = c(1,0,0),
                options = options,
                results = Example00Results$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Example 0
#'
#' 
#' @param data .
#' @param dependent .
#' @param group .
#' @param equVar .
#' @return A results object containing:
#' \tabular{llllll}{
#' }
#'
#' @export
Example00 <- function(
    data,
    dependent,
    group,
    equVar = TRUE) {

    if ( ! requireNamespace('jmvcore'))
        stop('Example00 requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dependent)) dependent <- jmvcore::resolveQuo(jmvcore::enquo(dependent))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dependent), dependent, NULL),
            `if`( ! missing(group), group, NULL))


    options <- Example00Options$new(
        dependent = dependent,
        group = group,
        equVar = equVar)

    analysis <- Example00Class$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
