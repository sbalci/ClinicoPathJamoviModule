% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.buildSurvivalFormula}
\alias{.buildSurvivalFormula}
\title{Build a survival model formula from variable names}
\usage{
.buildSurvivalFormula(
  time_var,
  outcome_var,
  predictors,
  survival_type = "standard",
  start_var = NULL,
  stop_var = NULL,
  strata_vars = NULL,
  interaction_terms = NULL
)
}
\arguments{
\item{time_var}{Character. Time variable name (or start time for
\code{"standard"}/\code{"interval"} types).}

\item{outcome_var}{Character. Event/outcome variable name.}

\item{predictors}{Character vector of predictor (main-effect) variable
names.}

\item{survival_type}{One of \code{"standard"}, \code{"counting"}, \code{"interval"}.}

\item{start_var}{Character. Start-time variable name (required when
\code{survival_type = "counting"}).}

\item{stop_var}{Character. Stop-time variable name (required when
\code{survival_type} is \code{"counting"} or \code{"interval"}).}

\item{strata_vars}{Character vector of stratification variable names.}

\item{interaction_terms}{Character vector of already-escaped, \code{:}-joined
interaction terms (e.g. \code{"`Arm`:`Bio`"}) appended to the right-hand
side after main effects and before strata.}
}
\value{
A parsed formula object (see \code{.asSurvivalFormula()}).
}
\description{
Consolidated helper used across the survival-analysis backends (e.g.
\code{multisurvival.b.R}) to assemble a \code{survival::Surv(...) ~ ...} formula
from raw variable names, with safe escaping of non-syntactic names via
\code{.escapeVariableNames()} and safe parsing via \code{.asSurvivalFormula()}.
}
