% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardizedratio.h.R
\name{standardizedratio}
\alias{standardizedratio}
\title{Standardized Incidence / Mortality Ratio (SIR / SMR)}
\usage{
standardizedratio(
  data,
  inputMode = "rate",
  observed,
  personTime = NULL,
  refRate = NULL,
  expected = NULL,
  stratum = NULL,
  ratioType = "sir",
  conf_level = 0.95,
  perStratum = TRUE,
  showPlot = TRUE,
  showSummary = FALSE,
  showExplanation = FALSE
)
}
\arguments{
\item{data}{The data as a data frame (one row per stratum).}

\item{inputMode}{Whether expected events are computed from a reference rate
times person-time, or supplied directly as a column of expected counts.}

\item{observed}{Observed number of events in each stratum.}

\item{personTime}{Person-time at risk in each stratum (used when expected
events are computed from a reference rate).}

\item{refRate}{Reference-population event rate for each stratum, expressed
per person-time unit (used with inputMode = "rate").}

\item{expected}{Directly supplied expected events per stratum (used with
inputMode = "expected").}

\item{stratum}{Optional stratum label used to report a per-stratum SIR/SMR
table.}

\item{ratioType}{Whether to label results as a standardized incidence or
mortality ratio.}

\item{conf_level}{Confidence level for the exact Poisson interval.}

\item{perStratum}{Report the SIR/SMR for each stratum in addition to the
overall value.}

\item{showPlot}{Display a forest plot of the per-stratum and overall
SIR/SMR.}

\item{showSummary}{Display a plain-language summary of the result.}

\item{showExplanation}{Display an explanation of indirect standardization.}
}
\value{
A results object containing:
\tabular{llllll}{
\code{results$todo} \tab \tab \tab \tab \tab a html \cr
\code{results$overallTable} \tab \tab \tab \tab \tab a table \cr
\code{results$stratumTable} \tab \tab \tab \tab \tab a table \cr
\code{results$plot} \tab \tab \tab \tab \tab an image \cr
\code{results$summary} \tab \tab \tab \tab \tab a html \cr
\code{results$explanation} \tab \tab \tab \tab \tab a html \cr
}

Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:

\code{results$overallTable$asDF}

\code{as.data.frame(results$overallTable)}
}
\description{
Computes standardized incidence ratios (SIR) or standardized mortality
ratios (SMR) by indirect standardization. For each stratum the number of
expected events is obtained by applying reference-population rates to the
observed person-time; the SIR/SMR is the ratio of total observed to total
expected events. Exact Poisson confidence intervals and a test against the
null value of 1 are provided. This is the standard analysis for registry,
cohort, and occupational studies comparing an observed event count with
population expectation.
}
\examples{
\donttest{
# Long data: one row per stratum with observed events, person-time,
# and a reference rate (events per person-time unit).
standardizedratio(
    data = mydata,
    observed = "obs_events",
    personTime = "person_years",
    refRate = "reference_rate")
}
}
