% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aeplot.h.R
\name{aeplot}
\alias{aeplot}
\title{Adverse Events Butterfly Plot}
\usage{
aeplot(
  data,
  inputMode = "patient",
  subjectID = NULL,
  aeTerm = NULL,
  armVar = NULL,
  gradeVar = NULL,
  gradeThreshold = 3,
  aeTermS = NULL,
  testAll = NULL,
  testHigh = NULL,
  controlAll = NULL,
  controlHigh = NULL,
  barShape = "inside",
  colorScheme = "nejm",
  showValues = FALSE,
  topN = 0
)
}
\arguments{
\item{data}{.}

\item{inputMode}{'patient' computes adverse-event incidence from
patient-level rows; 'summary' reads pre-computed all-grade and high-grade
percentages.}

\item{subjectID}{Subject identifier used as the incidence denominator
(distinct subjects).}

\item{aeTerm}{Adverse-event preferred term (one row per subject-event).}

\item{armVar}{Optional treatment arm. When omitted the plot shows a single
arm.}

\item{gradeVar}{Optional numeric severity grade used to derive the
high-grade split.}

\item{gradeThreshold}{Events with grade greater than or equal to this value
count as high grade.}

\item{aeTermS}{Adverse-event term column for pre-summarized input.}

\item{testAll}{Test-arm all-grade incidence percentage.}

\item{testHigh}{Test-arm high-grade incidence percentage.}

\item{controlAll}{Control-arm all-grade incidence percentage (optional).}

\item{controlHigh}{Control-arm high-grade incidence percentage (optional).}

\item{barShape}{'inside' overlays high-grade on all-grade; 'outside' stacks
the two.}

\item{colorScheme}{Journal-style color palette (via ggsci) for the test
arm.}

\item{showValues}{Print the all-grade percentage next to each bar.}

\item{topN}{Keep only the N most frequent terms (by test-arm all-grade); 0
shows all.}
}
\value{
A results object containing:
\tabular{llllll}{
\code{results$instructions} \tab \tab \tab \tab \tab a html \cr
\code{results$freqTable} \tab \tab \tab \tab \tab a table \cr
\code{results$plot} \tab \tab \tab \tab \tab an image \cr
\code{results$interpretation} \tab \tab \tab \tab \tab a html \cr
}

Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:

\code{results$freqTable$asDF}

\code{as.data.frame(results$freqTable)}
}
\description{
Back-to-back (butterfly) bar plot of adverse-event frequencies by preferred
term, comparing a test arm against an optional control arm and splitting
each
bar into all-grade and high-grade (e.g. grade >= 3) severity.
Accepts patient-level data (incidence computed internally) or
pre-summarized
percentages. Inspired by the Jamovi-TrialPlots module by highwind.
}
