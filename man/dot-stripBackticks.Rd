% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.stripBackticks}
\alias{.stripBackticks}
\title{Strip formula backticks from design-matrix column names}
\usage{
.stripBackticks(x)
}
\arguments{
\item{x}{A matrix (its \code{colnames} are cleaned) or a character vector.}
}
\value{
The same object with backticks removed and names made unique.
}
\description{
\code{model.matrix()} builds its column names from the terms of a formula, and
\code{terms()} DEPARSES a non-syntactic data-frame column name -- so a column
called \verb{Ki-67 (\%)} arrives as \code{`Ki-67 (\%)`} (backticks at both ends) and
a factor \verb{Tumor Grade} with level \code{Low} arrives as \verb{`Tumor Grade`Low}
(the closing backtick in the MIDDLE, which is why an anchored \code{^}|\code{$} strip
is not enough). jamovi variable names routinely contain spaces, hyphens,
parentheses and percent signs, and \code{jmvcore} deliberately restores those raw
names into \code{self$data}, so this is the normal case, not an exotic one.
}
\details{
Left alone the backticks are printed verbatim in results tables and plot
labels, and they break every lookup that matches a design-matrix column
against the original variable name (\code{==}, \code{\%in\%}, \code{match()}, \code{startsWith()}),
which silently drops values or falls back to a wrong default.

Backticks are quoting, never part of a name, so they are removed outright.
Stripping can in principle collide two distinct columns onto one name (a
numeric \verb{Tumor GradeLow} beside the factor dummy above), so the result is
de-duplicated -- every downstream consumer looks columns up by name.
}
\keyword{internal}
