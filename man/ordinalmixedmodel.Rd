% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinalmixedmodel.h.R
\name{ordinalmixedmodel}
\alias{ordinalmixedmodel}
\title{Ordinal Mixed Models (Cumulative Link)}
\usage{
ordinalmixedmodel(
  data,
  dep,
  fixedFactors = NULL,
  fixedCovs = NULL,
  randomTerms,
  link = "logit",
  threshold = "flexible",
  propOddsTest = TRUE,
  oddsRatios = TRUE,
  confInt = TRUE,
  confLevel = 0.95,
  randomEffectsTable = TRUE,
  thresholdsTable = TRUE,
  modelFit = TRUE,
  conditionMeans = FALSE
)
}
\arguments{
\item{data}{the data as a data frame}

\item{dep}{Ordinal dependent variable (e.g., Likert scale, tumor grade,
severity level)}

\item{fixedFactors}{Categorical fixed-effect predictors}

\item{fixedCovs}{Continuous fixed-effect predictors}

\item{randomTerms}{Random effects grouping variables (e.g., subject ID,
rater, site)}

\item{link}{Link function for the cumulative model}

\item{threshold}{Threshold parameterization}

\item{propOddsTest}{Test the proportional odds (parallel lines) assumption
using nominal_test}

\item{oddsRatios}{Display exponentiated coefficients as odds ratios}

\item{confInt}{Show confidence intervals for coefficients}

\item{confLevel}{Confidence level for intervals}

\item{randomEffectsTable}{Show random effects variance components}

\item{thresholdsTable}{Show threshold (intercept) coefficients}

\item{modelFit}{Show AIC, BIC, log-likelihood, and deviance}

\item{conditionMeans}{Show predicted marginal probabilities for each factor
level}
}
\value{
A results object containing:
\tabular{llllll}{
\code{results$instructions} \tab \tab \tab \tab \tab a html \cr
\code{results$modelFitTable} \tab \tab \tab \tab \tab a table \cr
\code{results$fixedEffects} \tab \tab \tab \tab \tab a table \cr
\code{results$thresholds} \tab \tab \tab \tab \tab a table \cr
\code{results$randomEffects} \tab \tab \tab \tab \tab a table \cr
\code{results$propOddsTestTable} \tab \tab \tab \tab \tab a table \cr
\code{results$conditionProbs} \tab \tab \tab \tab \tab a table \cr
\code{results$methodExplanation} \tab \tab \tab \tab \tab a html \cr
}

Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:

\code{results$modelFitTable$asDF}

\code{as.data.frame(results$modelFitTable)}
}
\description{
Cumulative link mixed models (CLMM) for ordinal response data with random
effects. Appropriate for Likert-scale data, ordered categorical outcomes
(e.g., tumor grades, disease severity), and repeated-measures designs with
ordinal endpoints. Uses the ordinal package for fitting cumulative link
mixed models with logit, probit, or complementary log-log links.
}
\examples{
\donttest{
data |>
ordinalmixedmodel(
    dep = "rating",
    fixedFactors = "scanner",
    randomTerms = "pathologist"
)
}
}
