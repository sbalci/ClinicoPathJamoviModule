% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnosticmeta.h.R
\name{diagnosticmeta}
\alias{diagnosticmeta}
\title{Diagnostic Test Meta-Analysis for Pathology}
\usage{
diagnosticmeta(
  data,
  study = NULL,
  true_positives = NULL,
  false_positives = NULL,
  false_negatives = NULL,
  true_negatives = NULL,
  covariate = NULL,
  bivariate_analysis = TRUE,
  hsroc_analysis = FALSE,
  meta_regression = FALSE,
  heterogeneity_analysis = FALSE,
  publication_bias = FALSE,
  confidence_level = 95,
  method = "reml",
  zero_cell_correction = "none",
  forest_plot = FALSE,
  sroc_plot = FALSE,
  funnel_plot = FALSE,
  show_individual_studies = FALSE,
  show_interpretation = FALSE,
  show_methodology = FALSE,
  show_analysis_summary = FALSE,
  color_palette = "standard",
  show_plot_explanations = FALSE
)
}
\arguments{
\item{data}{the data as a data frame}

\item{study}{Variable containing unique study identifiers}

\item{true_positives}{Number of true positive results in each study}

\item{false_positives}{Number of false positive results in each study}

\item{false_negatives}{Number of false negative results in each study}

\item{true_negatives}{Number of true negative results in each study}

\item{covariate}{Optional covariate for meta-regression analysis}

\item{bivariate_analysis}{Perform bivariate random-effects meta-analysis}

\item{hsroc_analysis}{Perform Holling proportional-hazards summary ROC
analysis}

\item{meta_regression}{Perform meta-regression with specified covariate}

\item{heterogeneity_analysis}{Perform heterogeneity analysis including
I-squared and Q statistics}

\item{publication_bias}{Assess publication bias using Deeks' funnel plot
test}

\item{confidence_level}{Confidence level for all intervals: the pooled
estimates and likelihood ratios, the prediction interval for a future
study, the per-study Wilson intervals, and the confidence and prediction
regions on the SROC plot.}

\item{method}{Estimator for the between-study variance. REML is
recommended. The bivariate model uses this method directly; the univariate
heterogeneity and meta-regression models map it to the metafor equivalent,
so 'Method of Moments' gives DerSimonian-Laird and 'Variance components'
gives the Hedges estimator in those two tables. 'Method of Moments' is the
multivariate extension of DerSimonian-Laird (via mvmeta), so choosing it
gives a DerSimonian-Laird-type estimator in the bivariate model as well;
REML is preferred for diagnostic accuracy data because the moment
estimators are non-iterative and truncate negative variance components to
zero.}

\item{zero_cell_correction}{Method for handling zero cells in 2x2 tables.
'none' (recommended) applies no correction to the data itself; the
bivariate model still adds 0.5 to studies with a zero cell at fitting time
(mada's 'single' correction), while the univariate heterogeneity and
meta-regression models exclude those studies instead, so they rest on fewer
studies. Both are disclosed in the output. Under 'none' the
publication-bias path behaves differently from the rest: because a zero
cell survives to that point, Deeks' test and the funnel plot add 0.5 to
EVERY study, not only the affected ones (correcting only the affected
studies would build a size-related trend into the test's own outcome
variable), and no asymmetry verdict is reported at all when more than a
quarter of the studies have a zero cell. The other three settings leave no
zero cell, so neither of those steps applies and a verdict is always given.
'constant' adds 0.5 to all four cells of affected studies before any
analysis. 'zero_cells' adds 0.5 only to the zero cells themselves.
'reciprocal_n' adds 1/N to all cells of affected studies, where N is the
study's total sample size. (The former option keys 'treatment_arm' and
'empirical' were renamed: they did not implement the procedures those names
denote in Sweeting et al. 2004.)}

\item{forest_plot}{Generate forest plot for sensitivity and specificity}

\item{sroc_plot}{Generate summary receiver operating characteristic plot}

\item{funnel_plot}{Generate funnel plot for publication bias assessment}

\item{show_individual_studies}{Display results for individual studies in
summary tables}

\item{show_interpretation}{Display clinical interpretation guidelines and
recommendations}

\item{show_methodology}{Display detailed methodology and statistical
approach information}

\item{show_analysis_summary}{Display natural language summary of analysis
results}

\item{color_palette}{Color palette for all plots - choose color-blind safe
options for accessibility}

\item{show_plot_explanations}{Display detailed explanations for all plots
including interpretation guidance}
}
\value{
A results object containing:
\tabular{llllll}{
\code{results$instructions} \tab \tab \tab \tab \tab a html \cr
\code{results$notices} \tab \tab \tab \tab \tab a html \cr
\code{results$summary} \tab \tab \tab \tab \tab a html \cr
\code{results$about} \tab \tab \tab \tab \tab a html \cr
\code{results$bivariateresults} \tab \tab \tab \tab \tab a table \cr
\code{results$hsrocresults} \tab \tab \tab \tab \tab a table \cr
\code{results$heterogeneity} \tab \tab \tab \tab \tab a table \cr
\code{results$metaregression} \tab \tab \tab \tab \tab a table \cr
\code{results$publicationbias} \tab \tab \tab \tab \tab a table \cr
\code{results$individualstudies} \tab \tab \tab \tab \tab a table \cr
\code{results$forestplot} \tab \tab \tab \tab \tab an image \cr
\code{results$srocplot} \tab \tab \tab \tab \tab an image \cr
\code{results$funnelplot} \tab \tab \tab \tab \tab an image \cr
\code{results$interpretation} \tab \tab \tab \tab \tab a html \cr
\code{results$forestplot_explanation} \tab \tab \tab \tab \tab a html \cr
\code{results$srocplot_explanation} \tab \tab \tab \tab \tab a html \cr
\code{results$funnelplot_explanation} \tab \tab \tab \tab \tab a html \cr
}

Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:

\code{results$bivariateresults$asDF}

\code{as.data.frame(results$bivariateresults)}
}
\description{
Comprehensive meta-analysis of diagnostic test accuracy studies designed
for
pathology research. Performs bivariate random-effects modeling,
proportional-hazards SROC analysis,
meta-regression, and publication bias assessment for AI algorithm
validation
and biomarker diagnostic accuracy synthesis.
}
