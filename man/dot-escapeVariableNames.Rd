% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_utils.R
\name{.escapeVariableNames}
\alias{.escapeVariableNames}
\title{Backtick-quote variable names for use in a formula string}
\usage{
.escapeVariableNames(var_names)
}
\arguments{
\item{var_names}{Character vector of raw variable names.}
}
\value{
Character vector, quoted where quoting is needed. Length and order
are preserved, so it is safe in \code{paste()}/\code{paste0()} alongside other
parallel vectors.
}
\description{
The inverse of \code{\link[=.stripBackticks]{.stripBackticks()}}: takes raw jamovi variable names and
returns them quoted wherever R would otherwise fail to parse them as a
symbol. Use it when BUILDING a formula string, never as a \code{data[[...]]} key
-- a backticked name is not a column name, and the lookup returns \code{NULL}.
}
\details{
This delegates to \code{\link[jmvcore:composeTerm]{jmvcore::composeTerm()}} rather than testing
\code{grepl("[^a-zA-Z0-9._]", x)} and wrapping in backticks by hand, which is what
it did until 2026-09-06. That hand-rolled rule quoted only names containing a
character outside \verb{[A-Za-z0-9._]}, and so silently produced formulas that do
not parse -- or, worse, parse into something else:
\itemize{
\item \verb{1stGrade} -- leading digit, no "special" character, left unquoted; the
formula fails to parse.
\item \code{if}, \code{for}, \code{function} -- reserved words, left unquoted; parse error.
\item \verb{a`b} -- wrapped to produce \verb{`a`b`}, three backticks; parse error.
\item \code{TRUE} -- left unquoted, so the term parses as the literal \code{TRUE} rather
than as the column. That one is the dangerous case: it fails silently.
}

\code{jmvcore::composeTerm()} is jamovi's own escaper, handles all four, and
agrees with the old rule on every name the old rule got right -- so this is a
strict improvement, not a behaviour change, for existing analyses.

Matching backticked names against fitted-model coefficients (as
\code{multisurvival-interactions.R} does) stays correct: \code{coxph()} derives its
coefficient names by deparsing the same terms, and \code{composeTerm()}'s output
has been verified to match those names for plain, spaced, punctuated and
digit-leading variable names alike.
}
\keyword{internal}
