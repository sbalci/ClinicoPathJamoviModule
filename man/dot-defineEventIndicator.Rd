% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_utils.R
\name{.defineEventIndicator}
\alias{.defineEventIndicator}
\title{Build the survival event indicator from a raw outcome column}
\usage{
.defineEventIndicator(
  outcome,
  outcomeLevel = NULL,
  multievent = FALSE,
  analysistype = "overall",
  dod = NULL,
  dooc = NULL,
  awd = NULL,
  awod = NULL,
  outcome_name = "outcome"
)
}
\arguments{
\item{outcome}{The raw outcome vector (factor, character, numeric or logical).}

\item{outcomeLevel}{The level representing the event, for the single-event
path. Honoured for numeric outcomes too, not only factors.}

\item{multievent}{Logical. Use the four-category mapping.}

\item{analysistype}{One of \code{"overall"}, \code{"cause"}, \code{"dfs"}, \code{"compete"}.}

\item{dod, dooc, awd, awod}{Level labels for Dead of Disease, Dead of Other
Causes, Alive with Disease, Alive without Disease.}

\item{outcome_name}{Display name of the outcome variable, used in messages.}
}
\value{
A list with \code{status} (0/1 or 0/1/2, \code{NA} preserved), \code{status_factor}
(Censored/Event/Competing, only when competing), the counts \code{n_event},
\code{n_censored}, \code{n_competing}, \code{n_missing}, the labels \code{event_label} and
\code{censored_labels}, \code{competing_labels}, \code{n_levels}, \code{has_competing},
\code{estimand}, and \code{error}
(\code{NULL}, or a ready-to-display message the caller should reject with).
}
\description{
Single source of truth for turning a user's outcome variable into the 0/1
(or 0/1/2 competing-risk) status vector consumed by \code{survival::Surv()}.
Replaces five near-identical \code{.definemyoutcome()} blocks that had drifted
apart in their validation.
}
\details{
Semantics, stated explicitly because they are easy to get wrong:
\itemize{
\item On the single-event-level path every level that is \emph{not} the selected
event level becomes 0 (right-censored). Labels alone cannot establish
whether this is ordinary censoring or a competing terminal event. In the
latter case Kaplan-Meier outputs describe net/cause-specific survival,
not absolute event risk. The caller is expected to surface \code{estimand} and
\code{censored_labels} to the user.
\item \code{NA} is never converted to a value. It stays \code{NA} and the row is
dropped downstream by \code{jmvcore::naOmit()} (complete-case analysis).
Coding it 0 would fabricate censoring.
\item In \code{multievent} mode every observed level must be assigned to one of
the four categories. An unmapped level is an error, never a silent \code{NA}
that later gets deleted along with the patient.
}
}
\keyword{internal}
