% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/error_handling.R
\name{safe_execute}
\alias{safe_execute}
\title{Safe Execution Wrapper}
\usage{
safe_execute(
  fn,
  function_name = "unknown",
  clinical_context = "",
  default_value = NULL
)
}
\arguments{
\item{fn}{A zero-argument function whose body is the code to run safely.
Pass \verb{function() \{ ... your code ... \}}.}

\item{function_name}{Name of the calling function (for log messages).}

\item{clinical_context}{Clinical context for the operation (for log messages).}

\item{default_value}{Value to place in the \code{result} slot on error.}
}
\value{
A list with \code{success}, \code{result}, and either \code{warnings}/\code{errors}
counters (on success) or \code{error_info} (on failure).
}
\description{
Safely execute code with enhanced error/warning capture.
The argument is a zero-argument function (a "thunk"), not an unevaluated
expression. Earlier versions accepted an unevaluated expression and ran
the runtime evaluator on it; that contract made the helper a string-to-code
pathway whenever a caller was tempted to feed it a parsed user string.
The current contract removes that risk by requiring callers to commit
to a function value at the call site.
}
