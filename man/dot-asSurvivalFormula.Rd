% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_utils.R
\name{.asSurvivalFormula}
\alias{.asSurvivalFormula}
\title{Build a survival formula safely via jmvcore::asFormula}
\usage{
.asSurvivalFormula(x, env = parent.frame())
}
\arguments{
\item{x}{A character formula string (e.g. \code{"survival::Surv(t, d) ~ x"}).}

\item{env}{Environment to attach to the returned formula. Defaults to the
calling frame, which is what downstream re-evaluation needs.}
}
\value{
A parsed formula object.
}
\description{
Wraps \code{jmvcore::asFormula} with the function allow-list extended to cover
common survival modelling helpers (\code{Surv}, \code{strata}, \code{cluster}, \code{frailty},
\code{tt}, \code{pspline}, \code{ns}, \code{bs}, \code{I}, \code{const}, \code{finegray}). Use this instead of
base \code{stats::as.formula()} in survival / Cox / Fine-Gray paths so the formula
goes through jmvcore's allow-listed parser.
}
\details{
The returned formula's environment is set to the CALLER's frame. \code{asFormula}
otherwise leaves it pointing at its own evaluation frame, which does not hold
the caller's \code{mydata}. Model fitting still works, because \code{coxph(fml, data=)}
is handed the data directly — but any method that later re-evaluates the
model call does not get it. \code{cox.zph()} is the visible casualty: ticking
"Proportional hazards assumption" aborted the entire analysis with
"object 'mydata' not found", taking every other result down with it.
}
\keyword{internal}
