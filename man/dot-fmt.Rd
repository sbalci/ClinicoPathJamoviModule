% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.fmt}
\alias{.fmt}
\title{Interpolate a translated string without risking an unbounded substitution loop}
\usage{
.fmt(fmt, ...)
}
\arguments{
\item{fmt}{Format string, normally wrapped in \code{.()}.}

\item{...}{Named placeholder values.}
}
\value{
The interpolated string.
}
\description{
\code{jmvcore::format()} re-scans the ENTIRE string from position 1 after each
substitution. If a substituted value contains its own placeholder -- e.g.
\code{jmvcore::format("LR ({value})", value = "x {value} y")} -- the substituter finds
that placeholder again, substitutes again, and never terminates. The loop runs in
code that does not poll R's interrupt handler, so it survives \code{setTimeLimit()} and
has to be SIGKILLed; inside jamovi it freezes the analysis engine rather than
raising an error.
}
\details{
Two realistic ways in: a translator copies a \code{{placeholder}} into the msgstr of the
very string that placeholder belongs to, or a dataset carries a column/level named
literally \code{{n}} that is then interpolated by name.

This wrapper is a pass-through. When no supplied value contains a brace -- the
overwhelming majority of calls -- it delegates untouched and the output is
byte-identical to calling \code{jmvcore::format()} directly. Only when a value actually
contains a \verb{\{} are that value's braces neutralised, so a pathological input
degrades to slightly different text instead of hanging.

Verified trigger conditions (R, jmvcore 2.7.x): a value containing its OWN
placeholder name hangs; a value containing a DIFFERENT supplied name substitutes and
terminates; an UNKNOWN \code{{name}} renders as an ellipsis; a bare \code{{ }} is left literal.
}
\keyword{internal}
