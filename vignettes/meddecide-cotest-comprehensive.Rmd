---
title: "Comprehensive Guide: Co-Testing Analysis"
author: "ClinicoPath jamovi Module Team"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Comprehensive Guide: Co-Testing Analysis}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  eval = FALSE
)
```

# Introduction

The **`cotest`** analysis (Co-Testing Analysis) provides clinicopathological researchers and biostatisticians with intuitive, publication-ready tools in jamovi and R.

## Key Capabilities

- Analyses two diagnostic tests applied in parallel: both are performed on the same subject at the same time, before either result is known, and the two results are then combined. Reports the post-test probability of disease for every result combination (either test positive, test 1 only, test 2 only, both positive, both negative), optionally allowing the two tests to be conditionally dependent. For tests performed one after another, where the second is ordered only after a particular first result, use Sequential Test Analysis instead. Note that Sequential Test Analysis also offers a parallel strategy, but its combined figures assume conditional independence; this analysis is the one that models conditional dependence numerically.
- Seamless integration between jamovi graphical interface and automated R syntax generation.
- Rigorous handling of missing data, small cohorts, and multi-variable stratification.

# Example Workflow in R

```r
library(ClinicoPathJamoviModule)

# Load package test dataset
data("cotest_test_data", package = "ClinicoPathJamoviModule", envir = environment())

# Run analysis with default settings
# ClinicoPathJamoviModule::cotest(
#   data = cotest_test_data
# )
```

# Clinical & Statistical Interpretation

Outputs generated by `cotest` include summary tables, diagnostic indicators, and high-resolution plots tailored for oncology, pathology, and clinical trial reporting.

