---
title: "Comprehensive Guide: Single Variable Quality Check"
author: "ClinicoPath jamovi Module Team"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Comprehensive Guide: Single Variable Quality Check}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  eval = FALSE
)
```

# Introduction

The **`checkdata`** analysis (Single Variable Quality Check) provides clinicopathological researchers and biostatisticians with intuitive, publication-ready tools in jamovi and R.

## Key Capabilities

- Screens a single variable for data-quality problems before it is used in analysis: completeness and the shape of its missingness, consensus outlier detection (at n = 10 or more a point is flagged only when at least 2 methods agree; below n = 10 single-method flags are shown, labelled informative-only and excluded from the grade; the MAD-based method is unavailable when there are 3 or fewer complete values or the MAD is zero, leaving 2 methods), distribution summaries, duplicate or repeated values, and optional plausibility checks for common clinical measurements. Reports a heuristic letter grade with a transparent component-by-component penalty breakdown. The grade uses rule-of-thumb thresholds and is not an externally validated quality metric; treat it as a screening aid, not a verdict.
- Seamless integration between jamovi graphical interface and automated R syntax generation.
- Rigorous handling of missing data, small cohorts, and multi-variable stratification.

# Example Workflow in R

```r
library(ClinicoPath)

# Load package test dataset
data("checkdata_test", package = "ClinicoPath", envir = environment())

# Run analysis with default settings (add the required variable arguments
# for this analysis; see ?checkdata)
# ClinicoPath::checkdata(
#   data = checkdata_test
# )
```

# Clinical & Statistical Interpretation

Outputs generated by `checkdata` include summary tables, diagnostic indicators, and high-resolution plots tailored for oncology, pathology, and clinical trial reporting.

