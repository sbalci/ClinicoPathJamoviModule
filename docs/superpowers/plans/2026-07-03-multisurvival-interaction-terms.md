# Multivariable Survival Interaction Terms — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Cox proportional-hazards interaction terms (e.g. `Treatment * Biomarker`) to the jSurvival *Multivariable Survival Analysis* (`multisurvival`) module, with an interaction (effect-modification) test table and within-subgroup hazard-ratio table for predictive-biomarker analysis.

**Architecture:** One new `Terms` option (`interactions`) feeds a jamovi model-builder UI (VariableSupplier + `Supplier format: term` + a small events JS, mirroring jmvbaseR's ANOVA). The risky logic — display-label→real-name mapping, interaction-coefficient extraction, and relevel-and-refit subgroup HRs — lives in **pure package-level functions** in a new `R/multisurvival-interactions.R`, unit-tested with testthat without the jamovi harness. The `multisurvival.b.R` R6 class calls these helpers, injects interaction terms into the Cox formula and every finalfit `explanatory` vector, and populates two new result tables.

**Tech Stack:** R6, jmvcore (Terms options, `composeTerms`, `asFormula`), `survival` (coxph, relevel-refit), finalfit / survminer (existing HR table + forest plot), testthat, jamovi `.a/.u/.r.yaml` + events JS.

## Global Constraints

Copied verbatim from the spec — every task's requirements implicitly include these.

- **No new package dependencies.** Use only `survival`, `jmvcore`, `stats`, and packages already in `Imports`. No `emmeans`/`marginaleffects`.
- **Formula safety:** all formulas go through `.asSurvivalFormula()` (wraps `jmvcore::asFormula` with the hardened 2.7.27 allow-list `c("Surv","strata","cluster","frailty","tt","pspline","ns","bs","I","const","finegray")`). Do not call base `as.formula`. The `:` / `*` interaction operators need no allow-list additions.
- **Variable-name escaping:** use `.escapeVariableNames()` (`R/utils.R:61`) — backticks names containing anything outside `[A-Za-z0-9._]`.
- **`.a.yaml` `type: Terms`** must carry only `name/title/type/default/description` — no other sub-properties (jamovi compiler rejects unknown properties).
- **Output serialization:** new outputs are `type: Table` — never `jmvcore::Notice` inserted via `insert(999,)` (protobuf "attempt to apply non-function" trap). Prose messages use the existing `private$.addHtmlMessage()` helper or table `setNote()`.
- **Do NOT hand-edit** `R/multisurvival.h.R` or `man/multisurvival.Rd` — they are regenerated by `jmvtools::prepare()` / `devtools::document()`.
- **JamoviTest routing:** while under modification, `jamovi/multisurvival.a.yaml` line 4 `menuGroup: Survival` becomes `menuGroup: SurvivalT` (Task 5). The user reverts it manually after testing.
- **UI dialect:** `jus: '3.0'`, `compilerMode: tame` (match existing `multisurvival.u.yaml`).
- **Backward compatibility:** `interactions` defaults to empty; with no interactions, all existing code paths and outputs must be unchanged, and the two new tables hidden.
- **Convention:** for a 2-way term `A:B`, **focal = first-listed (A)**, **moderator = second-listed (B)**. Within-subgroup HRs report the focal effect within each moderator level.
- **Branch first:** create a feature branch off `master` before the first commit (repo is on `master`; user's git hygiene rule).

---

### Task 0: Feature branch

**Files:** none (git only)

- [ ] **Step 1: Create and switch to a feature branch**

```bash
cd /Users/serdarbalci/Documents/GitHub/ClinicoPathJamoviModule
git checkout -b feature/multisurvival-interactions
```

- [ ] **Step 2: Confirm the spec is present (it is committed or on disk)**

Run: `ls docs/superpowers/specs/2026-07-03-multisurvival-interaction-terms-design.md`
Expected: the file path prints (no error).

---

### Task 1: Extend `.buildSurvivalFormula` to accept interaction terms

**Files:**
- Modify: `R/utils.R` (function `.buildSurvivalFormula`, currently duplicated at `R/multisurvival.b.R:306`; the authoritative copy used by the module is the top-level one — confirm which is in scope, see Step 0)
- Test: `tests/testthat/test-multisurvival-interactions.R` (create)

> **Note on the duplicate:** `.buildSurvivalFormula` exists both at `R/multisurvival.b.R:306` and possibly `R/utils.R`. `grep -n "buildSurvivalFormula <- function" R/*.R` in the design showed only `R/multisurvival.b.R:306`. Edit that definition. (There is only one; the grep confirmed it.)

**Interfaces:**
- Produces: `.buildSurvivalFormula(time_var, outcome_var, predictors, survival_type="standard", start_var=NULL, stop_var=NULL, strata_vars=NULL, interaction_terms=NULL)` — `interaction_terms` is a character vector of ALREADY-ESCAPED, `:`-joined interaction strings (e.g. `` "`Arm`:`Bio`" ``) appended to the RHS after main effects and before strata; returns a formula via `.asSurvivalFormula`.

- [ ] **Step 0: Locate the definition**

Run: `grep -n "buildSurvivalFormula <- function" R/multisurvival.b.R`
Expected: `306:.buildSurvivalFormula <- function(...`

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test-multisurvival-interactions.R` with:

```r
# Fast, harness-free unit tests for multisurvival interaction helpers.
# Source only the two self-contained helper files (no devtools::load_all,
# no R6/jamovi harness needed) — see feedback_testing_strategy.
suppressMessages({
  library(testthat)
  library(survival)
  library(jmvcore)
})
# Resolve repo root from the test file location.
.root <- normalizePath(file.path(dirname(dirname(getwd()))), mustWork = FALSE)
if (!file.exists(file.path(.root, "R", "utils.R")))
  .root <- normalizePath(".", mustWork = FALSE)
source(file.path(.root, "R", "utils.R"))
source(file.path(.root, "R", "multisurvival-interactions.R"))

test_that(".buildSurvivalFormula appends escaped interaction terms", {
  f <- .buildSurvivalFormula(
    time_var = "mytime", outcome_var = "myoutcome",
    predictors = c("arm", "bio"),
    interaction_terms = "`arm`:`bio`"
  )
  rhs <- as.character(f)[3]
  expect_true(grepl("arm", rhs))
  expect_true(grepl("bio", rhs))
  expect_true(grepl(":", rhs))
})

test_that(".buildSurvivalFormula still works with no interactions", {
  f <- .buildSurvivalFormula("mytime", "myoutcome", c("arm", "bio"))
  rhs <- as.character(f)[3]
  expect_false(grepl(":", rhs))
  expect_true(grepl("arm", rhs) && grepl("bio", rhs))
})

test_that(".buildSurvivalFormula keeps strata after interactions", {
  f <- .buildSurvivalFormula("mytime", "myoutcome", c("arm"),
                             strata_vars = "site",
                             interaction_terms = "`arm`:`bio`")
  rhs <- as.character(f)[3]
  expect_true(grepl("strata\\(", rhs))
  expect_true(grepl(":", rhs))
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: FAIL — `.buildSurvivalFormula` has no `interaction_terms` argument, and `R/multisurvival-interactions.R` does not exist yet (source error). (This proves the tests are wired.)

- [ ] **Step 3: Edit `.buildSurvivalFormula`**

Replace the current RHS-building block (`R/multisurvival.b.R:306`–end of function) so the signature gains `interaction_terms = NULL` and interactions are appended. New function body:

```r
.buildSurvivalFormula <- function(time_var, outcome_var, predictors, survival_type = "standard", start_var = NULL, stop_var = NULL, strata_vars = NULL, interaction_terms = NULL) {
  # Escape all variable names for safe formula construction
  escaped_time <- .escapeVariableNames(time_var)
  escaped_outcome <- .escapeVariableNames(outcome_var)
  escaped_predictors <- .escapeVariableNames(predictors)

  # Build left-hand side based on survival type
  lhs <- switch(survival_type,
    "standard" = paste0("survival::Surv(", escaped_time, ", ", escaped_outcome, ")"),
    "counting" = {
      if (is.null(start_var) || is.null(stop_var)) {
        jmvcore::reject("Start and stop variables required for counting process format")
      }
      escaped_start <- .escapeVariableNames(start_var)
      escaped_stop <- .escapeVariableNames(stop_var)
      paste0("survival::Surv(", escaped_start, ", ", escaped_stop, ", ", escaped_outcome, ")")
    },
    "interval" = {
      if (is.null(stop_var)) {
        jmvcore::reject("Stop time variable required for interval censoring")
      }
      escaped_stop <- .escapeVariableNames(stop_var)
      paste0("survival::Surv(", escaped_time, ", ", escaped_stop, ", ", escaped_outcome, ")")
    },
    jmvcore::reject("Unknown survival type: ", survival_type)
  )

  # Build right-hand side: main effects + interaction terms (already escaped)
  main_terms <- if (length(escaped_predictors) == 0) character(0) else escaped_predictors
  int_terms  <- if (length(interaction_terms) == 0) character(0) else interaction_terms
  rhs_terms  <- c(main_terms, int_terms)

  if (length(rhs_terms) == 0) {
    rhs <- "1"  # Null model
  } else {
    rhs <- paste(rhs_terms, collapse = " + ")
  }

  # Add stratification if specified (applies whether or not predictors exist)
  if (!is.null(strata_vars) && length(strata_vars) > 0) {
    escaped_strata <- .escapeVariableNames(strata_vars)
    strata_term <- paste0("strata(", paste(escaped_strata, collapse = ", "), ")")
    rhs <- if (identical(rhs, "1")) strata_term else paste(rhs, strata_term, sep = " + ")
  }

  formula_string <- paste0(lhs, " ~ ", rhs)
  return(.asSurvivalFormula(formula_string))
}
```

- [ ] **Step 4: Run the formula test (still expect the source/other tests to fail — helper file absent)**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: still errors on `source(".../R/multisurvival-interactions.R")` (missing file). The three `.buildSurvivalFormula` tests are defined but cannot run until Task 2 creates the helper file. Proceed to Task 2, then re-run.

- [ ] **Step 5: Commit**

```bash
git add R/multisurvival.b.R tests/testthat/test-multisurvival-interactions.R
git commit -m "feat(multisurvival): add interaction_terms arg to .buildSurvivalFormula"
```

---

### Task 2: Pure helpers — name mapping and term building

**Files:**
- Create: `R/multisurvival-interactions.R`
- Test: `tests/testthat/test-multisurvival-interactions.R` (append)

**Interfaces:**
- Produces `.mapInteractionTerms(interactions, all_labels)` → `list` of character vectors of REAL column names. `interactions` is `self$options$interactions` (a list of display-label character vectors); `all_labels` is `labelled::var_label(mydata_labelled)` (named list: names = real columns, values = display labels).
- Produces `.interactionTermsForFormula(real_terms)` → character vector of escaped `:`-joined terms for the Cox formula.
- Produces `.interactionTermsForFinalfit(real_terms)` → character vector of raw `:`-joined terms for finalfit `explanatory` vectors.
- Produces `.interactionModeratorInfo(real_term, data)` → `list(focal, moderator, twoway, categorical_moderator)`.

- [ ] **Step 1: Write the failing tests (append to the test file)**

```r
test_that(".mapInteractionTerms maps display labels to real names", {
  all_labels <- list(arm = "Treatment Arm", bio = "Biomarker", age = "Age")
  interactions <- list(c("Treatment Arm", "Biomarker"))
  out <- .mapInteractionTerms(interactions, all_labels)
  expect_equal(out, list(c("arm", "bio")))
})

test_that(".mapInteractionTerms passes through names already real / unlabelled", {
  all_labels <- list(arm = "Treatment Arm")
  out <- .mapInteractionTerms(list(c("arm", "unlabelled_col")), all_labels)
  expect_equal(out, list(c("arm", "unlabelled_col")))
})

test_that(".mapInteractionTerms returns empty list for NULL/empty", {
  expect_equal(.mapInteractionTerms(NULL, list(a = "A")), list())
  expect_equal(.mapInteractionTerms(list(), list(a = "A")), list())
})

test_that(".interactionTermsForFormula escapes and joins with colon", {
  out <- .interactionTermsForFormula(list(c("arm", "weird name")))
  expect_equal(out, "arm:`weird name`")
})

test_that(".interactionTermsForFinalfit joins raw with colon", {
  out <- .interactionTermsForFinalfit(list(c("arm", "bio"), c("arm", "age")))
  expect_equal(out, c("arm:bio", "arm:age"))
})

test_that(".interactionModeratorInfo identifies focal, moderator, categorical", {
  d <- data.frame(arm = factor(c("a","b")), bio = factor(c("x","y")), age = c(1.0, 2.0))
  info <- .interactionModeratorInfo(c("arm", "bio"), d)
  expect_equal(info$focal, "arm")
  expect_equal(info$moderator, "bio")
  expect_true(info$twoway)
  expect_true(info$categorical_moderator)

  info2 <- .interactionModeratorInfo(c("arm", "age"), d)   # continuous moderator
  expect_false(info2$categorical_moderator)

  info3 <- .interactionModeratorInfo(c("arm", "bio", "age"), d)  # 3-way
  expect_false(info3$twoway)
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: FAIL — `could not find function ".mapInteractionTerms"` etc.

- [ ] **Step 3: Create `R/multisurvival-interactions.R`**

```r
#' Multivariable-survival interaction helpers
#'
#' Pure, harness-free helpers used by `multisurvivalClass` to build Cox
#' interaction terms and to summarise effect modification. Kept out of the R6
#' class so they can be unit-tested directly (see
#' tests/testthat/test-multisurvival-interactions.R). All depend only on
#' `survival`, `stats`, and `.escapeVariableNames()` (R/utils.R).

# Map interaction terms from jamovi display labels to real column names.
# `interactions`: list of character vectors (self$options$interactions).
# `all_labels`  : named list (names = real columns, values = display labels).
.mapInteractionTerms <- function(interactions, all_labels) {
  if (is.null(interactions) || length(interactions) == 0)
    return(list())
  lapply(interactions, function(term) {
    vapply(term, function(component) {
      real <- names(all_labels)[match(component, all_labels)]
      if (length(real) == 0 || is.na(real)) component else real
    }, character(1), USE.NAMES = FALSE)
  })
}

# Escaped, colon-joined interaction terms for the Cox model formula RHS.
.interactionTermsForFormula <- function(real_terms) {
  if (length(real_terms) == 0) return(character(0))
  vapply(real_terms, function(components) {
    paste(.escapeVariableNames(components), collapse = ":")
  }, character(1), USE.NAMES = FALSE)
}

# Raw colon-joined interaction terms for finalfit `explanatory` vectors
# (main effects are passed to finalfit unescaped, so match that).
.interactionTermsForFinalfit <- function(real_terms) {
  if (length(real_terms) == 0) return(character(0))
  vapply(real_terms, paste, character(1), collapse = ":", USE.NAMES = FALSE)
}

# Describe a single interaction term: focal (first), moderator (second),
# whether it is 2-way, and whether the moderator is categorical.
.interactionModeratorInfo <- function(real_term, data) {
  twoway <- length(real_term) == 2
  focal <- real_term[1]
  moderator <- if (twoway) real_term[2] else NA_character_
  cat_mod <- twoway && !is.na(moderator) &&
    (is.factor(data[[moderator]]) || is.character(data[[moderator]]))
  list(focal = focal, moderator = moderator,
       twoway = twoway, categorical_moderator = cat_mod)
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: PASS — all Task-1 and Task-2 tests green (Task 1's three formula tests now run because the source file exists).

- [ ] **Step 5: Commit**

```bash
git add R/multisurvival-interactions.R tests/testthat/test-multisurvival-interactions.R
git commit -m "feat(multisurvival): pure interaction name-mapping and term builders"
```

---

### Task 3: Pure helper — interaction (effect-modification) test table

**Files:**
- Modify: `R/multisurvival-interactions.R`
- Test: `tests/testthat/test-multisurvival-interactions.R` (append)

**Interfaces:**
- Produces `.interactionTestTable(cox_model, conf_level = 0.95)` → `data.frame(term, hr, ci_lower, ci_upper, p)` with one row per interaction coefficient (coefficient names containing `:`), or `NULL` if none.

- [ ] **Step 1: Write the failing test**

```r
test_that(".interactionTestTable extracts interaction-coefficient HRs matching coxph", {
  set.seed(1)
  n <- 400
  arm <- factor(sample(c("ctrl","trt"), n, TRUE))
  bio <- factor(sample(c("neg","pos"), n, TRUE))
  lp  <- ifelse(arm=="trt", -0.2, 0) + ifelse(bio=="pos", 0.1, 0) +
         ifelse(arm=="trt" & bio=="pos", -0.9, 0)
  time <- rexp(n, exp(lp)); status <- rbinom(n, 1, 0.7)
  d <- data.frame(time, status, arm, bio)
  fit <- survival::coxph(survival::Surv(time, status) ~ arm * bio, data = d)

  tab <- .interactionTestTable(fit, conf_level = 0.95)
  expect_s3_class(tab, "data.frame")
  expect_true(any(grepl(":", tab$term)))

  # HR of the interaction row equals exp(coef) from coxph
  sm <- summary(fit)$coefficients
  int_name <- rownames(sm)[grepl(":", rownames(sm))][1]
  expect_equal(tab$hr[tab$term == int_name], unname(exp(sm[int_name, "coef"])),
               tolerance = 1e-8)
  expect_equal(tab$p[tab$term == int_name], unname(sm[int_name, "Pr(>|z|)"]),
               tolerance = 1e-8)
})

test_that(".interactionTestTable returns NULL when no interaction present", {
  set.seed(2); n <- 100
  d <- data.frame(time = rexp(n), status = rbinom(n,1,.7),
                  arm = factor(sample(c("a","b"), n, TRUE)))
  fit <- survival::coxph(survival::Surv(time, status) ~ arm, data = d)
  expect_null(.interactionTestTable(fit))
})
```

- [ ] **Step 2: Run to verify it fails**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: FAIL — `could not find function ".interactionTestTable"`.

- [ ] **Step 3: Add `.interactionTestTable` to `R/multisurvival-interactions.R`**

```r
# One row per interaction coefficient (name contains ":"): HR, CI, p.
.interactionTestTable <- function(cox_model, conf_level = 0.95) {
  sm <- summary(cox_model)$coefficients
  if (is.null(sm) || nrow(sm) == 0) return(NULL)
  cn <- rownames(sm)
  int_idx <- which(grepl(":", cn))
  if (length(int_idx) == 0) return(NULL)
  cis <- suppressWarnings(stats::confint(cox_model, level = conf_level))
  if (is.null(dim(cis))) cis <- matrix(cis, ncol = 2,
                                       dimnames = list(cn, NULL))
  data.frame(
    term     = cn[int_idx],
    hr       = exp(unname(sm[int_idx, "coef"])),
    ci_lower = exp(unname(cis[int_idx, 1])),
    ci_upper = exp(unname(cis[int_idx, 2])),
    p        = unname(sm[int_idx, "Pr(>|z|)"]),
    stringsAsFactors = FALSE
  )
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add R/multisurvival-interactions.R tests/testthat/test-multisurvival-interactions.R
git commit -m "feat(multisurvival): interaction effect-modification test table helper"
```

---

### Task 4: Pure helper — within-subgroup HRs via relevel-and-refit

**Files:**
- Modify: `R/multisurvival-interactions.R`
- Test: `tests/testthat/test-multisurvival-interactions.R` (append)

**Interfaces:**
- Produces `.computeSubgroupHRs(cox_formula, data, focal, moderator, conf_level = 0.95)` → `data.frame(interaction, moderator_level, focal_effect, hr, ci_lower, ci_upper, p)` (one row per moderator level × non-reference focal level), or `NULL` if the moderator is not a usable factor or all refits fail. Reference-level rows are omitted (a reference row has no HR).

- [ ] **Step 1: Write the failing test (compare to a manual relevel-and-refit ground truth)**

```r
test_that(".computeSubgroupHRs matches manual relevel-and-refit", {
  set.seed(3)
  n <- 600
  arm <- factor(sample(c("ctrl","trt"), n, TRUE))
  bio <- factor(sample(c("neg","pos"), n, TRUE))
  lp  <- ifelse(arm=="trt", -0.2, 0) + ifelse(arm=="trt" & bio=="pos", -0.9, 0)
  time <- rexp(n, exp(lp)); status <- rbinom(n, 1, 0.7)
  d <- data.frame(time, status, arm, bio)
  f <- survival::Surv(time, status) ~ arm * bio

  sub <- .computeSubgroupHRs(f, d, focal = "arm", moderator = "bio",
                             conf_level = 0.95)
  expect_s3_class(sub, "data.frame")
  expect_setequal(sub$moderator_level, c("neg","pos"))

  # Ground truth: relevel bio to "pos", refit, read arm coefficient.
  d2 <- d; d2$bio <- relevel(d2$bio, ref = "pos")
  fit2 <- survival::coxph(f, data = d2)
  hr_pos <- unname(exp(coef(fit2)["armtrt"]))
  got <- sub$hr[sub$moderator_level == "pos" & sub$focal_effect == "trt"]
  expect_equal(got, hr_pos, tolerance = 1e-6)
})

test_that(".computeSubgroupHRs returns NULL for non-factor moderator", {
  set.seed(4); n <- 200
  d <- data.frame(time = rexp(n), status = rbinom(n,1,.7),
                  arm = factor(sample(c("a","b"), n, TRUE)),
                  age = rnorm(n))
  f <- survival::Surv(time, status) ~ arm * age
  expect_null(.computeSubgroupHRs(f, d, focal = "arm", moderator = "age"))
})
```

- [ ] **Step 2: Run to verify it fails**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: FAIL — `could not find function ".computeSubgroupHRs"`.

- [ ] **Step 3: Add `.computeSubgroupHRs` to `R/multisurvival-interactions.R`**

```r
# Within-subgroup HRs for a 2-way interaction with a CATEGORICAL moderator.
# Relevels `moderator` to each level, refits the SAME full model, and reads the
# focal main-effect coefficient(s) — i.e. the fully-adjusted focal effect within
# that subgroup. Focal may be categorical (one row per non-reference level) or
# continuous (single "per unit" row). Returns NULL if moderator is not a usable
# factor. Reference focal level yields no row (no contrast).
.computeSubgroupHRs <- function(cox_formula, data, focal, moderator,
                                conf_level = 0.95) {
  modvec <- data[[moderator]]
  if (!is.factor(modvec)) {
    if (is.character(modvec)) modvec <- factor(modvec) else return(NULL)
  }
  mod_levels <- levels(modvec)
  if (length(mod_levels) < 2) return(NULL)

  # Reconstruct focal coefficient names exactly (avoids prefix collisions).
  fvec <- data[[focal]]
  focal_is_cat <- is.factor(fvec) || is.character(fvec)
  if (focal_is_cat) {
    flev <- levels(factor(fvec))
    focal_coef_names <- paste0(focal, flev[-1])   # coxph names: <var><level>
    focal_effect_lab <- flev[-1]
  } else {
    focal_coef_names <- focal
    focal_effect_lab <- focal
  }

  z <- stats::qnorm(1 - (1 - conf_level) / 2)
  rows <- list()
  for (b in mod_levels) {
    d2 <- data
    d2[[moderator]] <- stats::relevel(factor(d2[[moderator]]), ref = b)
    fit <- tryCatch(survival::coxph(cox_formula, data = d2),
                    error = function(e) NULL,
                    warning = function(w) suppressWarnings(
                      survival::coxph(cox_formula, data = d2)))
    if (is.null(fit)) next
    sm <- summary(fit)$coefficients
    for (i in seq_along(focal_coef_names)) {
      nm <- focal_coef_names[i]
      if (!nm %in% rownames(sm)) next
      est <- sm[nm, "coef"]; se <- sm[nm, "se(coef)"]
      rows[[length(rows) + 1]] <- data.frame(
        interaction     = paste0(focal, " * ", moderator),
        moderator_level = b,
        focal_effect    = focal_effect_lab[i],
        hr              = exp(est),
        ci_lower        = exp(est - z * se),
        ci_upper        = exp(est + z * se),
        p               = sm[nm, "Pr(>|z|)"],
        stringsAsFactors = FALSE
      )
    }
  }
  if (length(rows) == 0) return(NULL)
  do.call(rbind, rows)
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: PASS — all helper tests green.

- [ ] **Step 5: Commit**

```bash
git add R/multisurvival-interactions.R tests/testthat/test-multisurvival-interactions.R
git commit -m "feat(multisurvival): within-subgroup HR helper via relevel-and-refit"
```

---

### Task 5: `.a.yaml` — `interactions` option + JamoviTest routing

**Files:**
- Modify: `jamovi/multisurvival.a.yaml`

- [ ] **Step 1: Add the `interactions` option**

Insert after the `contexpl` option block (which ends before `# Multievent ----`, around `jamovi/multisurvival.a.yaml:360`):

```yaml
# Interactions ----

    - name: interactions
      title: Interaction Terms
      type: Terms
      default:
      description:
        R: >
          Interaction (crossed) terms added to the Cox model, built from
          variables already selected as explanatory or continuous explanatory
          variables. Each term tests effect modification — e.g. Treatment x
          Biomarker for predictive-biomarker analysis. For a 2-way term the
          first variable is the focal effect and the second is the moderator.
```

- [ ] **Step 2: Apply the JamoviTest `T` suffix to `menuGroup`**

Change `jamovi/multisurvival.a.yaml:4` from `menuGroup: Survival` to `menuGroup: SurvivalT`. Leave `menuSubgroup` untouched.

- [ ] **Step 3: Verify the module still prepares**

Run:
```bash
Rscript -e 'Sys.unsetenv("ELECTRON_RUN_AS_NODE"); jmvtools::prepare()' 2>&1 | tail -20
```
Expected: completes with **no errors/warnings** for `multisurvival`; `R/multisurvival.h.R` regenerates and now defines an `interactions` option accessor. (Per feedback_jmvtools_electron_node_hijack, `Sys.unsetenv` guards against the VS Code Node hijack.)

- [ ] **Step 4: Confirm the option reached the header**

Run: `grep -n "interactions" R/multisurvival.h.R | head`
Expected: at least one match (generated accessor / options field).

- [ ] **Step 5: Commit**

```bash
git add jamovi/multisurvival.a.yaml R/multisurvival.h.R
git commit -m "feat(multisurvival): add interactions Terms option; route to JamoviTest"
```

---

### Task 6: `.u.yaml` model builder + events JS

**Files:**
- Modify: `jamovi/multisurvival.u.yaml`
- Create: `jamovi/js/multisurvival.events.js`

**Interfaces:**
- Consumes: `interactions` option (Task 5), existing `explanatory` and `contexpl` VariablesListBoxes.
- Produces: a working model-builder UI where the term pool is the union of `explanatory` + `contexpl`; users build interaction terms via the "Interactions" transfer button.

> **Reference (known-working):** jmvbaseR `jamovi/anova.u.yaml` + `jamovi/js/anova.events.js` (read during design). Our events JS is simpler: it only *populates the term pool* and *prunes stale terms*; it does NOT auto-add main effects (those come from `explanatory`/`contexpl`).

- [ ] **Step 1: Add the events reference to the u.yaml header**

At the top of `jamovi/multisurvival.u.yaml`, after `compilerMode: tame` (line 5) and before `children:` (line 6), insert:

```yaml
events:
  update: './multisurvival.events::update'
```

- [ ] **Step 2: Wire change events on the existing variable boxes**

On the `explanatory` VariablesListBox (`jamovi/multisurvival.u.yaml:32-34`) and the `contexpl` VariablesListBox (`:38-40`), add an `events` block so the term pool updates when selections change. After each box's `isTarget: true`, add:

For `explanatory`:
```yaml
          - type: VariablesListBox
            name: explanatory
            isTarget: true
            events:
              change: './multisurvival.events::onChange_predictors'
```
For `contexpl`:
```yaml
          - type: VariablesListBox
            name: contexpl
            isTarget: true
            events:
              change: './multisurvival.events::onChange_predictors'
```

- [ ] **Step 3: Add the model-builder CollapseBox**

Immediately after the existing top `VariableSupplier` block closes (after the `contexpl` TargetLayoutBox, before the `Advanced Elapsed Time Options` CollapseBox at `:41`), insert:

```yaml
  - type: CollapseBox
    label: Interaction Terms
    collapsed: true
    stretchFactor: 1
    children:
      - type: Supplier
        name: interactionSupplier
        label: Available Predictors
        persistentItems: true
        stretchFactor: 1
        format: term
        higherOrders: true
        events:
          update: './multisurvival.events::onUpdate_interactionSupplier'
        children:
          - type: TargetLayoutBox
            transferAction: interactions
            label: 'Interaction Terms:'
            children:
              - type: ListBox
                name: interactions
                valueFilter: unique
                isTarget: true
                itemDropBehaviour: emptyspace
                template:
                  type: TermLabel
```

- [ ] **Step 4: Create `jamovi/js/multisurvival.events.js`**

```js
// Populates the interaction model-builder's term pool from the union of the
// `explanatory` and `contexpl` variable boxes, and prunes interaction terms
// that reference a variable no longer selected. Main effects are NOT added
// here — they come from the `explanatory`/`contexpl` options directly.
// Reference: jmvbaseR anova.events.js.

const events = {
    update: function(ui) {
        syncInteractionPool(ui, this);
    },

    onChange_predictors: function(ui) {
        syncInteractionPool(ui, this);
    },

    onUpdate_interactionSupplier: function(ui) {
        let vars = collectPredictors(ui, this);
        ui.interactionSupplier.setValue(
            this.valuesToItems(vars, FormatDef.variable));
    }
};

let collectPredictors = function(ui, context) {
    let a = context.cloneArray(ui.explanatory.value(), []);
    let b = context.cloneArray(ui.contexpl.value(), []);
    return a.concat(b);
};

let syncInteractionPool = function(ui, context) {
    let vars = collectPredictors(ui, context);

    ui.interactionSupplier.setValue(
        context.valuesToItems(vars, FormatDef.variable));

    // Prune interaction terms that reference removed variables.
    let varsDiff = context.findChanges("predictorList", vars, true,
                                       FormatDef.variable);
    let termsList = context.cloneArray(ui.interactions.value(), []);
    let changed = false;

    for (let i = 0; i < varsDiff.removed.length; i++) {
        for (let j = 0; j < termsList.length; j++) {
            if (FormatDef.term.contains(termsList[j], varsDiff.removed[i])) {
                termsList.splice(j, 1);
                changed = true;
                j -= 1;
            }
        }
    }

    if (changed)
        ui.interactions.setValue(termsList);
};

module.exports = events;
```

- [ ] **Step 5: Prepare and verify compilation**

Run:
```bash
Rscript -e 'Sys.unsetenv("ELECTRON_RUN_AS_NODE"); jmvtools::prepare()' 2>&1 | tail -25
```
Expected: no errors/warnings; the events JS is picked up (no "unknown event" complaints).

- [ ] **Step 6: Manual UI smoke test (empirical — the one uncertain integration)**

Load the module in jamovi (or `jmvtools::install()` into the JamoviTest module), open *Multivariable Survival Analysis* (now under the `SurvivalT` menu group), assign two variables to Explanatory, expand **Interaction Terms**, select both in the pool, click the **Interactions** transfer button, and confirm a `varA:varB` term appears in the Interaction Terms box.

Expected: the interaction term is created and persists. If the pool does not populate, the fallback is a self-sourcing `VariableSupplier` where the user re-drags variables into the interaction box (document the fallback in the commit message and notify the reviewer before proceeding).

- [ ] **Step 7: Commit**

```bash
git add jamovi/multisurvival.u.yaml jamovi/js/multisurvival.events.js
git commit -m "feat(multisurvival): interaction model-builder UI + events JS"
```

---

### Task 7: `.r.yaml` — two result tables

**Files:**
- Modify: `jamovi/multisurvival.r.yaml`

**Interfaces:**
- Produces result items `interactionTest` (Table) and `subgroupHR` (Table), each `visible: (interactions)` and refreshed on the same clearWith keys as the model. Column names below are consumed by Task 8's `setRow`/`addRow` calls — keep them identical.

- [ ] **Step 1: Add the two tables**

Append to `jamovi/multisurvival.r.yaml` (top-level `items:` list; match the file's existing indentation and `clearWith` style — copy the `clearWith` list from the existing main HR result item):

```yaml
    - name: interactionTest
      title: 'Interaction (Effect-Modification) Test'
      type: Table
      visible: (interactions)
      clearWith:
        - explanatory
        - contexpl
        - interactions
        - outcome
        - elapsedtime
        - outcomeLevel
      columns:
        - name: term
          title: 'Interaction'
          type: text
        - name: hr
          title: 'HR'
          type: number
        - name: ci_lower
          title: 'Lower'
          type: number
          superTitle: '95% CI'
        - name: ci_upper
          title: 'Upper'
          type: number
          superTitle: '95% CI'
        - name: p
          title: 'p'
          type: number
          format: zto,pvalue

    - name: subgroupHR
      title: 'Within-Subgroup Hazard Ratios'
      type: Table
      visible: (interactions)
      clearWith:
        - explanatory
        - contexpl
        - interactions
        - outcome
        - elapsedtime
        - outcomeLevel
      columns:
        - name: interaction
          title: 'Interaction'
          type: text
          combineBelow: true
        - name: moderator_level
          title: 'Subgroup'
          type: text
        - name: focal_effect
          title: 'Focal level'
          type: text
        - name: hr
          title: 'HR'
          type: number
        - name: ci_lower
          title: 'Lower'
          type: number
          superTitle: '95% CI'
        - name: ci_upper
          title: 'Upper'
          type: number
          superTitle: '95% CI'
        - name: p
          title: 'p'
          type: number
          format: zto,pvalue
```

- [ ] **Step 2: Verify the exact `clearWith` keys exist as options**

Run: `grep -nE "name: (explanatory|contexpl|outcome|elapsedtime|outcomeLevel)$" jamovi/multisurvival.a.yaml`
Expected: all five names appear (they are existing options; this guards against a typo that would make `clearWith` a silent no-op).

- [ ] **Step 3: Prepare and verify**

Run:
```bash
Rscript -e 'Sys.unsetenv("ELECTRON_RUN_AS_NODE"); jmvtools::prepare()' 2>&1 | tail -20
```
Expected: no errors/warnings; `R/multisurvival.h.R` regenerates with `interactionTest` and `subgroupHR` result accessors.

- [ ] **Step 4: Commit**

```bash
git add jamovi/multisurvival.r.yaml R/multisurvival.h.R
git commit -m "feat(multisurvival): add interaction test and subgroup HR result tables"
```

---

### Task 8: Wire interactions into `multisurvival.b.R`

**Files:**
- Modify: `R/multisurvival.b.R`

**Interfaces:**
- Consumes: `.mapInteractionTerms`, `.interactionTermsForFormula`, `.interactionTermsForFinalfit`, `.interactionModeratorInfo`, `.interactionTestTable`, `.computeSubgroupHRs` (Tasks 2–4); extended `.buildSurvivalFormula` (Task 1); result tables `interactionTest`, `subgroupHR` (Task 7).
- Produces: interaction terms in the fitted Cox model, in every finalfit `explanatory` vector, and populated interaction/subgroup tables.

This task has several injection sites. Do them in sub-steps and prepare once at the end; there is no cheap unit test for the R6 method, so verification is the integration script in Task 9 plus a `jmvtools::prepare()` compile check here.

- [ ] **Step 1: Inject interactions into the main Cox model formula**

At `R/multisurvival.b.R:2312-2322`, replace:

```r
        # Build formula parts (exclude strata from covariates)
        formula_parts <- c(myexplanatory, mycontexpl)

        # Build Cox regression formula using consolidated function with proper strata
        coxformula <- .buildSurvivalFormula(
          time_var = "mytime",
          outcome_var = "myoutcome",
          predictors = formula_parts,
          survival_type = "standard",
          strata_vars = mystratvar
        )
```

with:

```r
        # Build formula parts (exclude strata from covariates)
        formula_parts <- c(myexplanatory, mycontexpl)

        # Map interaction terms (display labels -> real names) and build the
        # escaped, colon-joined terms for the Cox formula RHS.
        real_interactions <- .mapInteractionTerms(self$options$interactions, all_labels)
        interaction_terms_cox <- .interactionTermsForFormula(real_interactions)

        # Build Cox regression formula using consolidated function with proper strata
        coxformula <- .buildSurvivalFormula(
          time_var = "mytime",
          outcome_var = "myoutcome",
          predictors = formula_parts,
          survival_type = "standard",
          strata_vars = mystratvar,
          interaction_terms = interaction_terms_cox
        )
```

> `all_labels` is already defined just below at `:2344-2345`; move those two lines (`mydata_labelled <- cleaneddata$mydata_labelled` and `all_labels <- labelled::var_label(mydata_labelled)`) to **above** the `real_interactions` line so `all_labels` exists when mapping. (They have no earlier dependency.)

- [ ] **Step 2: Populate the interaction tables after the model fit**

After the standard/Fine-Gray `cox_model` fit block ends (`R/multisurvival.b.R:2466`, the closing `}` of the `else` that fits the standard model) and before the competing-risk info message at `:2468`, insert:

```r
        # Populate interaction / effect-modification output
        if (length(self$options$interactions) > 0) {
          private$.populateInteractionTables(
            cox_model = cox_model,
            cox_formula = coxformula,
            data = mydata,
            real_interactions = real_interactions,
            conf_level = 0.95,
            is_finegray = (self$options$multievent && self$options$analystype_is_compete <- (self$options$analysistype == 'compete'))
          )
        }
```

> Simplify the `is_finegray` expression to avoid the inline assignment — use:
> ```r
>             is_finegray = (self$options$multievent && self$options$analysistype == 'compete')
> ```

- [ ] **Step 2b: Add the `.populateInteractionTables` private method**

Add this method to the `private = list(...)` block of `multisurvivalClass` (place it near other private helpers, e.g. after the method containing the model fit; any location inside `private=list()` is fine). It calls the pure helpers and writes rows:

```r
        .populateInteractionTables = function(cox_model, cox_formula, data,
                                              real_interactions, conf_level,
                                              is_finegray) {
          # --- Interaction (effect-modification) test table ---
          itab <- tryCatch(
            .interactionTestTable(cox_model, conf_level = conf_level),
            error = function(e) NULL
          )
          if (!is.null(itab) && nrow(itab) > 0) {
            t <- self$results$interactionTest
            for (i in seq_len(nrow(itab))) {
              t$addRow(rowKey = i, values = list(
                term     = itab$term[i],
                hr       = itab$hr[i],
                ci_lower = itab$ci_lower[i],
                ci_upper = itab$ci_upper[i],
                p        = itab$p[i]
              ))
            }
            t$setNote("emkey",
              "Each row tests whether the focal effect differs across the moderator (effect modification). A significant p indicates the effect is modified.")
          }

          # --- Within-subgroup hazard ratios ---
          sg <- self$results$subgroupHR
          if (is_finegray) {
            sg$setNote("fg",
              "Within-subgroup hazard ratios are disabled in competing-risks (Fine-Gray) mode; interpret the interaction coefficient above instead.")
            return(invisible(NULL))
          }

          rowKey <- 0
          skipped_continuous <- FALSE
          skipped_highorder <- FALSE
          for (term in real_interactions) {
            info <- .interactionModeratorInfo(term, data)
            if (!info$twoway) { skipped_highorder <- TRUE; next }
            if (!info$categorical_moderator) { skipped_continuous <- TRUE; next }
            sub <- tryCatch(
              .computeSubgroupHRs(cox_formula, data,
                                  focal = info$focal, moderator = info$moderator,
                                  conf_level = conf_level),
              error = function(e) NULL
            )
            if (is.null(sub)) next
            for (i in seq_len(nrow(sub))) {
              rowKey <- rowKey + 1
              sg$addRow(rowKey = rowKey, values = list(
                interaction     = sub$interaction[i],
                moderator_level = sub$moderator_level[i],
                focal_effect    = sub$focal_effect[i],
                hr              = sub$hr[i],
                ci_lower        = sub$ci_lower[i],
                ci_upper        = sub$ci_upper[i],
                p               = sub$p[i]
              ))
            }
          }
          notes <- character(0)
          if (skipped_continuous)
            notes <- c(notes, "Interactions with a continuous moderator are shown as a coefficient in the table above; per-subgroup HRs require a categorical moderator.")
          if (skipped_highorder)
            notes <- c(notes, "Within-subgroup HRs are computed for 2-way interactions only.")
          if (length(notes) > 0)
            sg$setNote("sgnote", paste(notes, collapse = " "))
          invisible(NULL)
        },
```

- [ ] **Step 3: Inject interactions into the main finalfit HR table**

At `R/multisurvival.b.R:6299-6300`, replace:

```r
  # Combine all explanatory variables
  explanatory_formula <- c(myexplanatory, mycontexpl)
```

with:

```r
  # Combine all explanatory variables (+ interaction terms)
  explanatory_formula <- c(myexplanatory, mycontexpl)
  if (length(self$options$interactions) > 0) {
    .all_labels_ff <- labelled::var_label(cleaneddata$mydata_labelled)
    explanatory_formula <- c(
      explanatory_formula,
      .interactionTermsForFinalfit(
        .mapInteractionTerms(self$options$interactions, .all_labels_ff))
    )
  }
```

> Confirm `cleaneddata$mydata_labelled` is in scope at this location (the design trace showed `cleaneddata$myexplanatory_labelled` used at `:6291`, so `cleaneddata` is in scope). If `mydata_labelled` is not a field of `cleaneddata` here, derive labels from the in-scope labelled frame used to build `myexplanatory` (grep upward for `var_label(` within this method and reuse that object).

- [ ] **Step 4: Inject interactions into the `hr_plot` explanatory vector**

At `R/multisurvival.b.R:3845-3848` the `finalfit::hr_plot(... explanatory = formula2 ...)` call is fed by `formula2`. Locate where `formula2` is built (grep `formula2 <-` within ~60 lines above `:3848`) and append interaction terms right after its definition:

```r
        # append interaction terms so the HR forest plot shows them
        if (length(self$options$interactions) > 0) {
          .all_labels_hp <- labelled::var_label(cleaneddata$mydata_labelled)
          formula2 <- c(
            formula2,
            .interactionTermsForFinalfit(
              .mapInteractionTerms(self$options$interactions, .all_labels_hp))
          )
        }
```

> If `formula2` is a single `+`-joined string rather than a vector at this site, instead append `" + <term>"` strings: `formula2 <- paste(c(formula2, .interactionTermsForFinalfit(...)), collapse = " + ")`. Inspect the exact construction before editing.

- [ ] **Step 5: Verify no other finalfit HR site is missed**

Run: `grep -n "explanatory = \|explanatory_formula\|finalfit::finalfit\|finalfit::hr_plot" R/multisurvival.b.R`
Expected: the sites you edited (main finalfit table ~`:6310`, hr_plot ~`:3848`) plus the KM `surv_plot` (`:4265`, intentionally NOT changed) and any commented-out blocks. Confirm every *active, non-KM* finalfit HR site now includes interaction terms. `ggforest` (`:3995/3997`) needs no change — it reads `cox_model` directly.

- [ ] **Step 6: Prepare / document to compile**

Run:
```bash
Rscript -e 'Sys.unsetenv("ELECTRON_RUN_AS_NODE"); jmvtools::prepare()' 2>&1 | tail -20
Rscript -e 'devtools::document()' 2>&1 | tail -15
```
Expected: both complete with no errors. (`document()` also picks up `R/multisurvival-interactions.R`.)

- [ ] **Step 7: Re-run the helper unit tests (guard against regressions)**

Run: `Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add R/multisurvival.b.R R/multisurvival.h.R man/multisurvival.Rd
git commit -m "feat(multisurvival): fit interactions, populate effect-modification + subgroup tables"
```

---

### Task 9: Integration validation script

**Files:**
- Create: `tests/validate_multisurvival_interactions.R`

**Interfaces:**
- Consumes: `.mapInteractionTerms`, `.interactionTermsForFormula`, `.buildSurvivalFormula`, `.interactionTestTable`, `.computeSubgroupHRs`, `survival::coxph`.
- Produces: a runnable script asserting the module's interaction pathway reproduces `survival::coxph(Surv ~ A*B)` exactly (ground truth).

- [ ] **Step 1: Create the validation script (mirrors `tests/validate_agreement_allpairs.R` convention)**

```r
#!/usr/bin/env Rscript
# Validation for multisurvival interaction terms.
# Generates a Treatment x Biomarker predictive-biomarker dataset with a KNOWN
# interaction, then checks that the module's interaction pathway reproduces a
# direct survival::coxph(Surv ~ arm * bio + age) fit, and that the
# relevel-and-refit subgroup HRs match manual releveling.
# Usage:  Rscript tests/validate_multisurvival_interactions.R
suppressMessages({
  library(survival)
  library(jmvcore)
  devtools::load_all(".", quiet = TRUE)
})

set.seed(20260703)
n <- 800
arm <- factor(sample(c("control", "treatment"), n, TRUE))
bio <- factor(sample(c("neg", "pos"), n, TRUE))
age <- round(rnorm(n, 60, 10), 1)
# True model: treatment helps only biomarker-positive patients (interaction).
lp <- -0.15 * (arm == "treatment") + 0.10 * (bio == "pos") +
      -0.90 * (arm == "treatment" & bio == "pos") + 0.02 * (age - 60)
time <- rexp(n, rate = exp(lp))
status <- rbinom(n, 1, 0.7)
d <- data.frame(mytime = time, myoutcome = status, arm = arm, bio = bio, age = age)
labelled::var_label(d) <- list(mytime = "Time", myoutcome = "Event",
                               arm = "Treatment Arm", bio = "Biomarker", age = "Age")
all_labels <- labelled::var_label(d)

# --- Module pathway: map display labels -> real, build formula, fit ---
interactions <- list(c("Treatment Arm", "Biomarker"))   # as jamovi delivers
real_int <- .mapInteractionTerms(interactions, all_labels)
stopifnot(identical(real_int, list(c("arm", "bio"))))
int_cox <- .interactionTermsForFormula(real_int)
f_mod <- .buildSurvivalFormula("mytime", "myoutcome",
                               predictors = c("arm", "bio", "age"),
                               interaction_terms = int_cox)
fit_mod <- survival::coxph(f_mod, data = d)

# --- Ground truth: direct coxph ---
fit_ref <- survival::coxph(Surv(mytime, myoutcome) ~ arm * bio + age, data = d)

stopifnot(all.equal(unname(coef(fit_mod)), unname(coef(fit_ref)), tolerance = 1e-8))
cat("OK: module formula reproduces coxph(Surv ~ arm*bio + age)\n")

# --- Interaction test table ---
itab <- .interactionTestTable(fit_mod, conf_level = 0.95)
sm <- summary(fit_ref)$coefficients
int_name <- rownames(sm)[grepl(":", rownames(sm))][1]
stopifnot(abs(itab$hr[grepl(":", itab$term)][1] - exp(sm[int_name, "coef"])) < 1e-8)
cat(sprintf("OK: interaction HR = %.3f (p = %.4f)\n",
            itab$hr[grepl(":", itab$term)][1], itab$p[grepl(":", itab$term)][1]))

# --- Within-subgroup HRs vs manual relevel-refit ---
sub <- .computeSubgroupHRs(f_mod, d, focal = "arm", moderator = "bio")
d_pos <- d; d_pos$bio <- relevel(d_pos$bio, ref = "pos")
hr_pos <- unname(exp(coef(survival::coxph(f_mod, data = d_pos))["armtreatment"]))
got_pos <- sub$hr[sub$moderator_level == "pos"]
stopifnot(abs(got_pos - hr_pos) < 1e-6)
cat(sprintf("OK: subgroup HR (bio=pos) = %.3f matches manual relevel-refit\n", got_pos))

cat("\nALL INTERACTION VALIDATIONS PASSED\n")
```

- [ ] **Step 2: Run the validation script**

Run: `Rscript tests/validate_multisurvival_interactions.R 2>&1 | tail -15`
Expected: ends with `ALL INTERACTION VALIDATIONS PASSED` and no `stopifnot` failures.

- [ ] **Step 3: Commit**

```bash
git add tests/validate_multisurvival_interactions.R
git commit -m "test(multisurvival): validation script for interaction pathway vs coxph"
```

---

### Task 10: Documentation fix

**Files:**
- Modify: `README.Rmd` and/or the relevant vignette(s) (whichever contains the interaction claim)

- [ ] **Step 1: Find the inaccurate claim**

Run:
```bash
grep -rniE "interaction terms|include interactions|interactions if needed" README.Rmd vignettes/ 2>/dev/null
```
Expected: locate the "Interaction Terms: Include interactions if needed" phrasing (or similar). If it appears only on the pkgdown website and not in-repo, note that the site regenerates from `README.Rmd`/vignettes and add the corrected usage note there.

- [ ] **Step 2: Replace the aspirational bullet with an accurate description**

Wherever the model-options list mentions interactions, replace with text like:

```
- **Interaction Terms:** expand *Interaction Terms* and build crossed terms
  (e.g. Treatment × Biomarker) from variables already selected as explanatory
  or continuous predictors. For a 2-way term the first variable is the focal
  effect and the second is the moderator. Results include an interaction
  (effect-modification) test and within-subgroup hazard ratios — the standard
  approach for predictive-biomarker analysis in clinical trials.
```

- [ ] **Step 3: Regenerate README if edited**

Run: `Rscript -e 'devtools::build_readme()' 2>&1 | tail -5` (only if `README.Rmd` was changed; `README.md` is generated — never edit it directly).
Expected: `README.md` regenerates without error.

- [ ] **Step 4: Commit**

```bash
git add README.Rmd README.md vignettes/ 2>/dev/null; git commit -m "docs(multisurvival): correct interaction-terms claim to match implementation"
```

---

### Task 11: Final verification

**Files:** none (verification only)

- [ ] **Step 1: Full compile + document**

Run:
```bash
Rscript -e 'Sys.unsetenv("ELECTRON_RUN_AS_NODE"); jmvtools::prepare()' 2>&1 | tail -20
Rscript -e 'devtools::document()' 2>&1 | tail -10
```
Expected: both clean (no errors/warnings for `multisurvival`).

- [ ] **Step 2: Unit tests + validation script**

Run:
```bash
Rscript -e 'testthat::test_file("tests/testthat/test-multisurvival-interactions.R")'
Rscript tests/validate_multisurvival_interactions.R 2>&1 | tail -5
```
Expected: all testthat tests PASS; validation ends with `ALL INTERACTION VALIDATIONS PASSED`.

- [ ] **Step 3: Backward-compatibility check**

Confirm that with `interactions` empty, the Cox formula is byte-identical to before:
```bash
Rscript -e '
  Sys.unsetenv("ELECTRON_RUN_AS_NODE"); suppressMessages(devtools::load_all(".", quiet=TRUE));
  f <- .buildSurvivalFormula("mytime","myoutcome", c("arm","bio"));
  cat(deparse(f), "\n")'
```
Expected: `survival::Surv(mytime, myoutcome) ~ arm + bio` — no `:` term, identical to pre-change output.

- [ ] **Step 4: Manual end-to-end in jamovi (JamoviTest module)**

Install to the JamoviTest module, open the analysis, fit `Surv(time,event) ~ arm * bio`, and confirm: (a) the interaction row appears in the HR table, (b) the *Interaction (Effect-Modification) Test* table shows the interaction HR/p, (c) the *Within-Subgroup Hazard Ratios* table shows treatment HRs for each biomarker level, (d) the forest plot renders the interaction row, (e) the interaction p-value matches `tests/validate_multisurvival_interactions.R`.

- [ ] **Step 5: Update NEWS.md**

Add an entry under the current `DESCRIPTION` version describing the new interaction-terms feature (per CLAUDE.md "Update NEWS.md when there is version change" — if the version is bumped, use the current `DESCRIPTION` version).

- [ ] **Step 6: Final commit**

```bash
git add NEWS.md
git commit -m "docs: note multisurvival interaction terms in NEWS"
```

- [ ] **Step 7: Revert JamoviTest routing (hand back to user)**

Remind the user (do NOT auto-do unless asked) to revert `jamovi/multisurvival.a.yaml:4` `menuGroup: SurvivalT` → `menuGroup: Survival` and re-run `jmvtools::prepare()` before release.

---

## Self-Review

**Spec coverage:**
- Motivation / interaction≠stratification → context only (no task needed).
- Full Model-Terms builder → Tasks 5 (option), 6 (UI + events JS). ✓
- Interaction in Cox model + finalfit table + hr_plot + ggforest → Task 8 steps 1,3,4,5. ✓
- Effect-modification test table → Tasks 3, 7, 8-step-2b. ✓
- Within-subgroup HRs (relevel-refit; skip continuous-moderator & 3-way; Fine-Gray disabled) → Tasks 4, 8-step-2b. ✓
- Serialization-safe Tables → Task 7. ✓
- Guardrails (factor levels, convergence tryCatch, EPV unchanged, backward compat) → Task 4 (NULL guards), Task 8 (tryCatch, is_finegray), Task 11-step-3. ✓
- Documentation fix → Task 10. ✓
- JamoviTest T-suffix → Task 5-step-2; revert reminder Task 11-step-7. ✓
- Verification story (prepare/document/validation/manual) → Tasks 9, 11. ✓

**Placeholder scan:** No "TBD"/"handle edge cases"-style placeholders; every code step shows complete code. Two steps (8-step-4 `formula2` shape, 8-step-3 `mydata_labelled` scope) ask the implementer to confirm an in-scope object before editing — these are genuine "inspect the exact local variable" checks, not deferred design, and each gives the concrete fallback.

**Type consistency:** Function names used identically across tasks — `.mapInteractionTerms`, `.interactionTermsForFormula`, `.interactionTermsForFinalfit`, `.interactionModeratorInfo`, `.interactionTestTable`, `.computeSubgroupHRs`, `.buildSurvivalFormula(..., interaction_terms=)`, `.populateInteractionTables`. Table/column names in Task 7 (`interactionTest`/`subgroupHR` and their columns) match the `addRow` values in Task 8-step-2b. Data-frame column names returned by `.interactionTestTable` (term/hr/ci_lower/ci_upper/p) and `.computeSubgroupHRs` (interaction/moderator_level/focal_effect/hr/ci_lower/ci_upper/p) match their consumers.
