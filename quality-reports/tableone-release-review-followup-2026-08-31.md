# Table One — follow-up release review

2026-08-31. Requested skill: `release-review-function`; `prepare-translation`
also applied to the new labels. This pass supersedes the current verdict in the
[preceding release review](tableone-release-review-2026-08-31.md), whose earlier
statistical/data-flow evidence remains relevant. All work is scoped to Table One.
Generated files and other modules were not edited or regenerated by the agent.

## 1. Overall verdict

The regenerated 1.0.8 files matched the previously verified fixture. Fresh probes
nevertheless exposed valid-category failures and ambiguous summary labels not
covered by the previous 810 assertions. Those confirmed issues are repaired;
all **884 assertions now pass**, including independent counts and file-backed
framework save/load. The analysis remains appropriate for overall-cohort
descriptives, not clinical inference or treatment decisions. **Not ready for
release:** the new 1.0.9 revision stamp requires user regeneration, desktop export
and `.omv` checks are outstanding, and plain-text R export has a documented
jmvcore limitation. The older Descriptives sibling still needs synchronization
if that is the intended shipping module.

## 2. Findings

### Critical

No new wrong-number or unsafe clinical-inference defect confirmed. Numerical
reference cases, canonical NA-factor exclusion and input-shape protections pass.

### Major

1. **Fixed: a recorded category named Total prevented janitor output.**
   `R/tableone.b.R:552`. Appending the total row duplicated a factor level; the
   analysis displayed a formatting failure instead of the requested frequency
   table. The presentation column is now converted to character only after
   tabulation, and a colliding grand-total label becomes `Total (all cases)`.
   A suffix handles collisions with that fallback too. Ordered/factor/text
   categories, unused Total levels, missing rows and exclusion are tested.
2. **Fixed: recorded categories and missing-summary rows shared labels.**
   `R/tableone.b.R:438`, `R/tableone.b.R:477`, `R/tableone.b.R:556`.
   Literal Unknown in t2, N-Miss in t3, and NA in t4 could look like the
   missing-value summary. Summary rows now use a unique `Missing (NA)` fallback
   when necessary. Literal categories are never renamed, merged or excluded.
   Non-colliding outputs retain their original labels and statistical defaults.

### Moderate

**Unresolved framework limitation: styled HTML remains in plain-text R exports.**
`R/tableone.b.R:464` and `R/tableone.b.R:669` feed valid styled HTML to Html items.
Installed jmvcore 2.7.38's `Html$asString()` uses an `htmlToText()` regex that
removes plain tags but not tags with attributes. Direct calls reproduce raw
`<td style=...>`/`<th style=...>` fragments for t2/t4. Shared jmvcore behavior was
not patched, and table styling was not stripped to accommodate its text converter.
Use the HTML result or t1 when clean plain text is required. This does **not**
prove that the separate desktop clipboard/Word/PDF exporters fail. A scoped TODO
records the upstream/text-export follow-up and required desktop verification.

### Minor

- The prior 1.0.8 regeneration was verified, but this newly confirmed repair is
  material. `jamovi/tableone.a.yaml:6` now declares **1.0.9**. The generated
  `R/tableone.h.R:196` remains **1.0.8** until the user regenerates. The backend
  repair is tested; the new revision identifier is not yet active.
- Added localized fallback labels, About guidance and release notes under the
  current package version 1.0.8.04. No public option or statistical default changed.

## 3. Changes made

- Added category-label collection and deterministic collision-free summary-label
  helpers. Applied them to gtsummary missing text, arsenal Nmiss labels and janitor
  missing/total rows. Only presentation labels are modified.
- Added `test-tableone-label-collisions.R`: 68 assertions for literal/reserved
  categories, repeated fallback collisions, translated labels, unchanged input
  data and actual framework `.save()`/`.load()` through temporary files.
- Added three complete EN/TR catalog messages: two labels and one About sentence.
  The scoped inventory now contains 147 messages. Existing unrelated catalog
  entries were not rewritten; no external localization publication occurred.
- Updated developer/testing/localization documentation, NEWS and a scoped TODO
  for the framework plain-text limitation.
- Bumped only the analysis source version to 1.0.9. User regeneration is requested
  rather than editing generated files. Other dirty work and the sibling repository
  were preserved. No commit, installation into the user's library or deployment.

## 4. Statistical verification

Independent scratch script:
`/tmp/tableone-rereview-20260831-Aw8LPD/verify-statistics.R`.
It compares sum/count arithmetic, the sample-SD formula and base R quantiles with
actual rendered tables. New regression expectations are literal counts computed
from the declared fixture, not copied from the implementation.

| Reference | Expected | Actual |
| --- | --- | --- |
| x = c(1:19,100), mean/SD | 14.5 / 20.856653 | t1 14.50 (20.86); t3 14.5 (20.9) |
| t1 median, type-7 quartiles | 10.5; 5.75, 15.25 | 10.50 [5.75, 15.25] |
| t2 type-2 quartiles | 5.5, 10.5, 15.5 | Exact raw agreement; default display 11 (6,16) |
| Eight B among 18 recorded, 20 total | 44.4444% valid; 40% all | t1/t3 44.4%; t2 44%; t4 40.0% and 44.4% |
| Complete-case subset x=1:18 | N=18, mean 9.5, SD 5.338539 | N=18; 9.50 (5.34) |
| NA-factor case, retained x=2:30 | N=29, mean 16, SD 8.514693 | N=29; 16.00 (8.51) |
| Five recorded categories with four each, plus four missing | N=24; each recorded 4/24=16.6667%, valid 4/20=20%; four missing | Seven distinct rows including missing/total; 4, 16.7%, 20.0%; missing 4,16.7%,-; total 24,100%,100% |
| t2/t3 collision fixture: three categories of five plus five missing | Each category 5/15=33.3333%; missing count 5 | Category labels retain 5 (33%)/5 (33.3%); unique missing row is 5 |

The repair uses documented customization points:
[janitor totals name](https://sfirke.github.io/janitor/reference/adorn_totals.html),
[arsenal statistic labels](https://mayoverse.github.io/arsenal/reference/tableby.control.html),
and [gtsummary missing text](https://www.danieldsjoberg.com/gtsummary/reference/tbl_summary.html).
Runtime verification uses tableone 0.13.2, gtsummary 2.5.1, arsenal 3.7.1 and
janitor 2.2.1; online documentation is not evidence of an installed upgrade.

No confidence intervals, p-values, effect sizes or multiple-testing adjustment
are produced. The collision repairs do not change cohort eligibility or any
denominator. Original data identity and factor/ordered/text branches are tested.

## 5. Data-flow audit

| UI/input | Option (default) | Backend computation | Result |
| --- | --- | --- | --- |
| Dataset / R data argument | data (required) | Original shape checks, selected data, replicated frequency rows | Instructions or selected table and optional summaries |
| VariablesListBox | vars (NULL) | Normalize actual NA factors; omit unsupported/all-missing/style-ineligible variables | todo, selected table, assumptions, summary, reportSentence |
| ComboBox | sty (t1) | Four engines; t2/t3/t4 collision-safe summary labels | tablestyle1–4, declarative style visibility |
| Exclusion CheckBox | excl (false) | Complete-case deletion over eligible variables; preserve source N | Table, source/retained summaries and warnings |
| Summary CheckBox | showSummary (false) | Source missingness, included types, excluded/final N | summary, visible (showSummary) |
| About CheckBox | showAbout (false) | Scope, engines, denominator and label-collision explanations | about, visible (showAbout) |
| Report CheckBox | showReportSentence (false) | Copy-ready analyzed-cohort text after successful rendering | reportSentence, visible (showReportSentence) |
| Median CheckBox, enabled sty:t1 | nonnormal (false) | t1 median/Q1/Q3 print; diagnostic explanation follows setting | tablestyle1 and assumptions |

All eight options are used; Data is consumed through `self$data`. No undeclared
option reads. All nine results are populated or explicitly cleared through known
paths; all clearWith keys are valid. No events script, plot state, renderFun,
native Notice or Output-column item exists. No imperative visibility override
was added. `compilerMode: tame` and `ExplorationT` routing remain unchanged.

Policies without UI controls: overall-only analysis; no tests/imputation or
deduplication; frequency rows rather than survey inference; supported-type and
all-missing omissions; NA-factor normalization; t4 maximum 20 observed categories;
t1 binary second-level display; t2 automatic type/count-level selection;
engine-specific precision and quartiles; fixed screening thresholds; and now
collision-free summary labels. These are documented policies, not unused options.
Numerical diagnostic thresholds remain those audited in the preceding report.

## 6. Test results

Fresh installed fixture in `/tmp/tableone-rereview-20260831-Aw8LPD/library`
contains the current backend byte-for-byte, the user's generated 1.0.8 bindings,
real datasets and official-compiler EN/TR locales. Tests do not claim that the
pending 1.0.9 generated version has been validated.

| Test file | Assertions passed |
| --- | ---: |
| arguments | 66 |
| audit-fixes | 129 |
| basic | 31 |
| edge-cases | 52 |
| integration | 16 |
| label-collisions | 68 |
| release-review | 55 |
| review-fixes | 455 |
| tableone | 12 |
| **Total** | **884** |

- `Rscript /tmp/tableone-rereview-20260831-Aw8LPD/run-tests.R`: **884 passed,
  0 failures, 0 errors, 0 warnings, 0 skips**. Log: `tests.log`.
- `Rscript /tmp/tableone-rereview-20260831-Aw8LPD/verify-statistics.R`: PASS.
  Log: `statistics.log`.
- All four styles passed actual file-backed framework `.save()`/`.load()`;
  edited/filtered 12-row data replace saved 40-row report counts. This is not
  the complete desktop `.omv` container/UI workflow.
- Initial standalone collision test omitted jamovi's bundled R library and
  skipped RProtoBuf. The final runner includes that library; all save/load tests
  ran, with zero skips. No analysis patch was needed for this test setup issue.
- Official locale compiler produced all 147 scoped messages; real Turkish
  t2/t3/t4 outputs and all 111 distinct backend message markers are covered.
- State guard and theme scanners: zero findings. Duplicate-control check: pass.
  Scoped results-rendering contract: 6 pass; naming contract: 3 pass.
  References (six), direct imports and option/clearWith checks pass. No tracked
  release archives or case collisions. R parse and scoped whitespace checks pass.
- Real-client UI harness, using a dedicated temporary profile:
  `placeholder present = false ; title = Table One ; errors = undefined`.
  No schema/UI behavior was changed in this repair.
- The previously regenerated three Rd files and dataset help remain verified.
  The earlier broad module check had 0 errors, 6 warnings, 3 notes; it was not
  repeated here and is not a clean final release check.

Runtime: R 4.6.0, jmvcore 2.7.38, jmvtools 28.3, testthat 3.3.2.
No new dependency was introduced.

## 7. Remaining limitations

- Analysis source 1.0.9 and generated version 1.0.8 differ until user regeneration.
  The backend fixes are tested using the unchanged wrapper contract.
- The jmvcore plain-text R export limitation remains; use HTML/t1 meanwhile.
  Desktop clipboard/Word/PDF behavior, light/dark results and full `.omv`
  save/reopen must still be verified on the release build.
- The Descriptives sibling remains the older 1.0.7 implementation. No external
  synchronization, build or deployment was performed.
- Full package examples, codoc, tests and vignettes remain outside this focused
  run. The older broad-check warnings must be dispositioned for a module release.
- Turkish editorial/desktop review remains advisable. Upstream engine labels and
  errors are not all translated. Infinite numeric input still fails explicitly
  at framework validation; no silent finite-value recoding was introduced.

## 8. Release recommendation

**Not ready for release.** The new reserved-category defects are repaired, but:

1. The user should run the following to register analysis 1.0.9, then the focused
   tests should be repeated against that regenerated header:

   ```sh
   Rscript -e 'Sys.unsetenv("ELECTRON_RUN_AS_NODE"); jmvtools::prepare()'
   Rscript -e 'devtools::document()'
   ```

   This is a new material repair after the previously verified regeneration.
   Expect a module-wide generated diff. The skill reserves this action for the user.
2. Verify desktop result readability, export and full `.omv` reopening. Resolve or
   explicitly accept/document the plain-text R limitation for the chosen release.
3. Synchronize and verify the older Descriptives copy if shipping it; complete
   release-package checks and disposition the known unrelated warnings.

No further source or dependency change is proposed without confirming a defect.
