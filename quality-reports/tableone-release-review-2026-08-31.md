# Table One — release review

The later [follow-up release review](tableone-release-review-followup-2026-08-31.md)
supersedes this verdict: it repairs newly identified reserved-category label
collisions and requests registration of analysis version 1.0.9. The evidence
below records the preceding 1.0.8 review and user-regeneration verification.

Date: 2026-08-31. Scope: the umbrella module's `tableone` analysis and its
tests/documentation. Procedure: `release-review-function`, following the completed
[review repairs](tableone-review-fixes-2026-08-31.md). Neighbor `reportcat` was
read for family conventions, not changed. No generated files were edited or
regenerated by the agent. The user subsequently regenerated them; post-regeneration
verification on 2026-08-31 is included below.

## 1. Overall verdict

The repaired backend produces correct independently checked overall-cohort
descriptives and avoids the previously confirmed denominator, non-scalar-input,
unused-level and stale-output defects. Its stated clinical scope is appropriate:
descriptive summaries of records, not treatment advice, group comparisons or
validated assumption testing. **Not ready for release yet:** final desktop
export/save-reopen checks remain unverified. The regeneration blocker is resolved:
analysis version 1.0.8 and the new dataset help are verified in an installed
isolated fixture. This is not a full-module release sign-off. The regenerated
backend/header passed all 810 targeted assertions again, with no failures,
errors, warnings or skips.

## 2. Findings

### Critical

No new critical defect confirmed. The preceding repairs are verified, including
NA-factor normalization before exclusion (`R/tableone.b.R:137`), pre-selection
matrix/list rejection (`R/tableone.b.R:1043`), and observed-only janitor levels
(`R/tableone.b.R:521`). Prior defects and evidence remain in the linked repair
report rather than being presented as unresolved findings.

### Major

The separately shipped Descriptives copy does not contain the repaired backend:
`/Users/serdarbalci/Documents/GitHub/ClinicoPathDescriptives/R/tableone.b.R:1`.
Its source hash differs and its options retain the older scope description.
It was inspected read-only. Releasing that copy without the normal project
synchronization workflow would not release these repairs. This pass does not
authorize changes or deployment to that other repository.

### Moderate

**Analysis version collision — fixed and regenerated.** Both
umbrella and older Descriptives analyses claimed `1.0.7` despite material
behavior differences (`jamovi/tableone.a.yaml:6`; the sibling's corresponding
file also line 6). The umbrella source now declares `1.0.8`. The user-regenerated
base declares `c(1,0,8)` at `R/tableone.h.R:196`, and the installed analysis's
private version field confirms 1.0.8. This is distinct from the
package version, which consistently remains `1.0.8.04` in DESCRIPTION,
`jamovi/0000.yaml` and CITATION.cff. Test-menu routing was already `ExplorationT`
and was preserved.

### Minor

- **Missing dataset help — fixed and regenerated.** The earlier completed
  package check listed `tableone_test` among undocumented data objects. Added
  `R/data_tableone_docs.R:1` and its DESCRIPTION Collate entry. All 19 names,
  the 150-row dimension, tibble class and generator units were checked against
  the stored fixture/source. The user-generated `man/tableone_test.Rd` passes
  parsing/checkRd, and installed help lookup resolves `tableone_test`. The missing
  help entry is repaired; the entire package's documentation-warning count has
  not been rechecked and includes unrelated objects.
- **Quantile/precision explanation — fixed.** The styles legitimately differ
  in quartile algorithms and default rounding. The developer documentation
  now explains the verified example (`vignettes/tableone-documentation.md:35`),
  and eight additional assertions protect t2/t3 output
  (`tests/testthat/test-tableone-release-review.R:6`). No statistical default
  was changed.

## 3. Changes made

- Finished the previous repairs: actual NA factor levels share canonical
  missingness; matrix/array/list inputs cannot be flattened silently; unused
  levels cannot bypass the janitor size guard. Dichotomous labels, reporting,
  safe escaping and Turkish localization recommendations were also implemented.
- Recorded the completed broader package-check outcome in the repair report.
- Added eight numeric/denominator/precision assertions for gtsummary and arsenal,
  plus documentation of engine-specific quartiles and the testing requirement.
- Added scoped synthetic-dataset documentation and registered its source file.
- Bumped analysis source version to `1.0.8`; left generated files to the user,
  as required by the release-review skill. Existing generated files from the
  preceding repair were not changed in this pass.
- Preserved unrelated dirty files, package version, defaults and the sibling
  module. No commits, deployment, dependency upgrades or desktop installation.

## 4. Statistical verification

Scratch script: `/tmp/tableone-review-fixes-20260831-ldeJp9/verify-release.R`.
Output: `release-statistics.log` in that directory. It uses independently
specified records, sum/count arithmetic, the sample-SD formula and base R
quantiles; comparisons include actual rendered outputs, not only engine objects.

Main dataset: `x = c(1:19, 100)` and factor `g` with ten A, eight B and two
missing values. No random sampling is used.

| Quantity | Independent expectation | Actual result |
| --- | --- | --- |
| Mean; sample SD | 290/20 = 14.5; sqrt(sum((x-14.5)^2)/19) = 20.856653 | t1 `14.50 (20.86)`; t3 `14.5 (20.9)` |
| t1 median; type-7 Q1/Q3 | 10.5; 5.75/15.25 | `10.50 [5.75, 15.25]` |
| t2 median; type-2 Q1/Q3 | 10.5; 5.5/15.5 | Engine statistics exactly agree; default integer display `11 (6, 16)` |
| Recorded-value B denominator | 8/18 = 44.4444% | t1 `8 (44.4)`; t2 `8 (44%)`; t3 `8 (44.4%)` |
| Janitor B percentages | 8/20 = 40%; 8/18 = 44.4444% | `B 8 40.0% 44.4%` |
| Missing g | 2/20 = 10% | t1 Missing `10.0`; t2 Unknown `2`; t3 N-Miss `2`; t4 NA `2`, `10.0%` |
| Complete-case exclusion | N=18, x=1:18; mean 9.5, SD 5.338539 | t1 N=18; `9.50 (5.34)` |
| NA-factor regression | x=1:40, x[1]=NA; A rows 1–20, B rows 21–30, actual NA level rows 31–40; retained x=2:30 | N=29; mean 16; SD 8.514693; t1 `16.00 (8.51)`, B `10 (34.5)` |
| Same NA-factor selection, t4 | Numeric x omitted; only missing g excludes rows | N=30; tested across ordered/unordered factors and all four engines |

The [R quantile reference](https://stat.ethz.ch/R-manual/R-devel/library/stats/html/quantile.html)
describes the alternative algorithms. The
[gtsummary reference](https://www.danieldsjoberg.com/gtsummary/reference/tbl_summary.html)
documents type-2 percentiles, dichotomous counted levels and automatic categorical
typing. Online gtsummary documentation currently describes 2.6.0; runtime evidence
here is explicitly from installed 2.5.1, not an assumed upgrade. No CIs, p-values,
effect sizes, survival estimates or multiplicity calculations are produced, so
those gates are not applicable.

## 5. Data-flow audit

All eight options are accounted for; seven have UI controls. There are no unused
options or undeclared `self$options` reads. `data` is consumed as `self$data`,
not as an ordinary option value. Options retain their generated wrapper order.

| UI / input | `.a.yaml` option and default | Backend computation | `.r.yaml` destination |
| --- | --- | --- | --- |
| Active dataset / R argument | `data`, Data, required R input | Original shape validation; selected data and framework frequency-row expansion | Instructions or selected table; summary/report/quality |
| VariablesListBox | `vars`, Variables, NULL | Supported types and observed values; canonical NA factors; style eligibility; selected cohort | All nine items as applicable; empty selection shows `todo` |
| Table-style ComboBox | `sty`, List, t1 | t1 CreateTableOne; t2 tbl_summary; t3 tableby; t4 tabyl | Exactly one declaratively visible `tablestyle1`–`tablestyle4` |
| Missing-exclusion CheckBox | `excl`, Bool, false | naOmit over eligible variables only; source and retained counts kept separately | Table, `summary`, `reportSentence`, `assumptions`; `todo` if empty |
| Analysis-summary CheckBox | `showSummary`, Bool, false | Source N, complete-case count, exclusions and included types | `summary`, visible `(showSummary)` |
| About CheckBox | `showAbout`, Bool, false | Translated scope, denominator and input guidance; available before early returns | `about`, visible `(showAbout)` |
| Copy-ready CheckBox | `showReportSentence`, Bool, false | Source/retained cohort and included variable names; no engine-failure report | `reportSentence`, visible `(showReportSentence)` |
| Median CheckBox, enabled `(sty:t1)` | `nonnormal`, Bool, false | t1 print uses all continuous names; data-quality explanation follows setting | `tablestyle1`, `assumptions`; no numeric effect in other styles |

Result coverage: `todo` is always available for onboarding/omissions/errors;
`assumptions` follows `(vars)`; the four table items follow `sty`; the three
optional panels follow their respective checkboxes. All `clearWith` entries
name real options. `.clearOutputs()` also empties every item at each run, so
restored HTML cannot retain obsolete content. No visibility binding is replaced
by `setVisible(FALSE)`. There are no images, renderFun methods, events scripts,
fixed Table rows or plot state to audit.

Unexposed policies are intentional and disclosed: overall cohort only; no
grouping/tests/imputation; one row per case with no deduplication; replicated-row
frequency weights rather than survey inference; unsupported/all-missing omissions;
NA-factor normalization; the 20-observed-category t4 limit; t1 binary second-level
display; t2 type/value inference; engine-specific precision/quantiles. Diagnostic
thresholds are fixed screening heuristics (N<10/30, incomplete>50%/20%,
excluded>30%/10%, numeric distinct count<10 with >10 recorded observations,
and >80% unique text). Report-language cutoffs at 5%/20% are descriptive labels,
not validated clinical categories. None warrants an added option in this repair.

## 6. Test results

The installed isolated fixture contains the actual working backend and generated
bindings (byte-for-byte SHA-256 agreement), real engines, data and compiled EN/TR
locales. The user's installed module was not replaced. After user regeneration,
the actual current backend, header, schemas and all three help files were copied
into a fresh isolated package and installed successfully. No compiler or roxygen
regeneration was performed by the agent. The header differs from the preceding
tested header only in its version, and existing function/class help is unchanged.
The new dataset help was installed and checked as well.

| Focused test file | Passed assertions |
| --- | ---: |
| arguments | 66 |
| audit-fixes | 129 |
| basic | 31 |
| edge-cases | 52 |
| integration | 16 |
| release-review | 55 |
| review-fixes | 449 |
| tableone | 12 |
| **Total** | **810** |

Executed commands and outcomes:

- **Post-regeneration:** `Rscript /tmp/tableone-postregen-20260831-7ReXl5/verify.R`:
  **810 passed; 0 failed/error/warning/skipped**, log `verification.log` in that
  directory. Runtime version 1.0.8 confirmed; all three Rd files pass checkRd;
  installed dataset help resolves. The initial verification script queried a
  nonexistent public version field; correcting it to the actual private field
  resolved that harness error without changing analysis code.
- Post-regeneration results-rendering and naming contracts passed again (6 and
  3 assertions). Reference/import/clearWith and duplicate-control checks passed.
  The regenerated module entry agrees with source title, menu and description;
  NAMESPACE exports tableone and Collate includes the dataset documentation.
- `Rscript /tmp/tableone-review-fixes-20260831-ldeJp9/run-tests.R`:
  **810 passed; 0 failed/error/warning/skipped**, log `release-tests.log`.
- `Rscript /tmp/tableone-review-fixes-20260831-ldeJp9/verify-release.R`:
  independent arithmetic, raw t2 quartiles and rendered comparisons passed.
- `python3 tools/check_state_guards.py R/tableone.b.R`: 0 unguarded reads.
- `python3 tools/check_uyaml_duplicate_names.py jamovi/tableone.u.yaml`:
  no duplicate names; compilerMode is `tame`.
- `python3 tools/theme_safe_html.py R/tableone.b.R`: no changes proposed.
  Entity/visibility/warning/addRow scan found no forbidden live pattern.
- `TMPDIR=/tmp/tableone-review-fixes-20260831-ldeJp9/release-ui tools/ui_harness/render_ui.sh tableone`:
  **`HARNESS: placeholder present = false ; title = Table One ; errors = undefined`**.
  The sandbox first blocked local process startup; the approved local Chrome/server
  retry succeeded. This validates options-panel initialization, not full desktop use.
- Scoped repository results-rendering contract: **6 assertions passed**.
- Scoped analysis-file naming contract: **3 assertions passed**; exact class,
  wrapper, filenames and Collate spelling agree, and no tracked case collisions.
  No committed tar.gz/jmo archives. All six references resolve with title,
  author and URL. All direct dependencies are declared. Log `release-gates.log`
  intentionally records the pre-bump analysis version that triggered the repair.
- `Rscript /tmp/tableone-review-fixes-20260831-ldeJp9/check-data-doc.R`:
  new source parses; 19 fields, 150-row shape and Collate entry verified.
- Scoped `git diff --check`: passed for hand-authored changes. The initial
  inline data-doc validation command had a quoting error; the corrected scratch
  script above passed. It did not indicate a defect in the new source.
- Completed full-package tarball check with `--no-codoc --no-examples --no-tests
  --no-manual --no-vignettes --no-build-vignettes`: **0 errors, 6 warnings,
  3 notes**. Installation, byte compilation and namespace loading passed.
  See the preceding repair report for warning categories and the intentionally
  interrupted earlier run. This check predates this pass's help/version edits
  and does not establish a clean final package check.

Runtime: R 4.6.0; jmvcore 2.7.38; jmvtools 28.3; tableone 0.13.2;
gtsummary 2.5.1; arsenal 3.7.1; janitor 2.2.1; testthat 3.3.2.
The installed jamovi client used by the UI harness is 28.2.0.

## 7. Remaining limitations

- Actual desktop light/dark result readability, copying to Word/PDF, all option
  toggles, row-filter edits and `.omv` save/reopen remain required. Automated
  protobuf restoration and options-panel rendering cover only part of this path.
- The Descriptives sibling is older and was not synchronized or fully audited.
  Synchronization/rebuild is required if it is the intended release artifact.
- Full module tests, examples, codoc and vignette checks were not run. Other
  analyses' duplicate data, undocumented objects/arguments, non-ASCII code,
  unused imports and asset-path/vignette packaging issues were not repaired.
- Turkish runtime checks passed; native-speaker editorial and desktop review
  remain advisable. Third-party engine labels/errors may remain English.
- Infinite numeric values trigger jmvcore validation, even when a t4 selection
  would otherwise omit the numeric measurement. No silent recoding occurs.
- Tests cover installed dependency versions, not every historical/future release.
  No clinical-data validation study or survey-weight support is claimed.

## 8. Release recommendation

**Not ready for release.** Required next actions:

Regeneration and its focused verification are **complete**: the user ran the
generators, and the regenerated version/help plus all 810 assertions passed.
The agent respected the skill's user-owned regeneration rule. No additional
regeneration is requested for the verification/report update.

1. Complete the desktop checklist above against the actual release build.
2. If shipping ClinicoPathDescriptives, use the normal reviewed synchronization
   workflow and verify the copied revision/dependencies/tests there. Do not assume
   the currently older sibling contains these repairs.
3. Resolve or explicitly disposition the broader package warnings and run the
   release-level package checks; this one-analysis review cannot waive them.

Reassess readiness after these actions. No further confirmed wrong-number defect
was found in the tested repaired backend.
