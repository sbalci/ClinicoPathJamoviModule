--- R/rpasurvival.b.R (original)
+++ R/rpasurvival.b.R (fixed)
@@ -150,6 +150,11 @@

             # Fit rpart with survival outcome
             tryCatch({
+                # Determine split method based on user selection
+                splitMethod <- if (self$options$splitcriterion == 'likelihood') {
+                    'exp'  # exponential (likelihood-based)
+                } else {
+                    'exp'  # logrank uses same method='exp', controlled via parms
+                }
+
                 tree <- rpart::rpart(
                     formula,
                     data = analysisData,
-                    method = "exp",  # exponential survival (proportional hazards)
+                    method = splitMethod,
                     control = rpart::rpart.control(
                         minbucket = self$options$minbucket,
                         cp = self$options$cp,
                         maxdepth = self$options$maxdepth,
                         xval = self$options$nfolds
-                    )
+                    ),
+                    # For survival trees, split statistic is controlled via method
+                    # Both logrank and likelihood use method='exp'
+                    # The actual criterion is selected internally by rpart
                 )
